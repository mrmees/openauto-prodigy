build/
build-pi/
dist/
.cache/
CMakeLists.txt.user
*.o
*.so
*.a
*.deb
.DS_Store
.worktrees/

# Android SDK tools (local install)
build-tools/
cmdline-tools/
platform-tools/
platforms/
.knownPackages

# Local config
.mcp.json

# Test artifacts
Testing/

# Decompiled APK source + index database (367MB)
analysis-projection/
__pycache__/
.venv/

# Proto decode research output (moved to ../research-archive/)
tools/proto_decode_output/

# Local-only artifacts (kept for reference, not public) — root only;
# docs/archive/ is tracked history and must NOT be ignored
/archive/

# Private competitive-analysis / reference material — never public
docs/private/

# SDD scratch (briefs, reports, review packages) — machine-local
.superpowers/

# Codex review gate artifacts (verdicts + diff copies) — machine-local
reviews/
