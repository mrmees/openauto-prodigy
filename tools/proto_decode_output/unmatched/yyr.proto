// Decoded from APK class: yyr
// Field count: 3, range: 2-4

syntax = "proto2";

message yyr {
    optional bytes /* message ? */ d = 2;
    optional bytes /* message ? */ e = 3;
    optional bytes /* message ? */ f = 4;
}