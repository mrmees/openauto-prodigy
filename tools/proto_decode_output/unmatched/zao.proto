// Decoded from APK class: zao
// Field count: 3, range: 1-7

syntax = "proto2";

message zao {
    optional bytes /* message ? */ d = 1;
    optional bytes /* message ? */ e = 5;
    optional bytes /* message ? */ f = 7;
}