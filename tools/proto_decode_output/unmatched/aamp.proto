// Decoded from APK class: aamp
// Field count: 5, range: 1-5

syntax = "proto2";

message aamp {
    repeated aapd c = 1;  // msg: aapd
    optional aamo d = 2;  // msg: aamo
    optional bytes /* message ? */ g = 3;
    optional bytes e = 4;
    optional bytes /* message ? */ h = 5;
}