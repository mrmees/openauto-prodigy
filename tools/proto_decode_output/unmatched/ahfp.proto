// Decoded from APK class: ahfp
// Field count: 3, range: 1-3

syntax = "proto2";

message ahfp {
    optional int32 /* enum ? */ c = 1;
    optional int32 d = 2;
    optional bytes /* message ? */ f = 3;
}