// Decoded from APK class: wdm
// Field count: 5, range: 1-5

syntax = "proto2";

message wdm {
    optional string c = 1;
    optional string d = 2;
    optional string e = 3;
    optional wdr f = 4;  // enum: wdr
    optional int32 /* enum a */ g = 5;  // enum: a
}