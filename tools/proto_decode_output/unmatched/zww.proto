// Decoded from APK class: zww
// Field count: 3, range: 9-24

syntax = "proto2";

message zww {
    optional bytes /* message ? */ e = 9;
    optional bytes /* message ? */ d = 18;
    optional bytes /* message ? */ f = 24;
}