// Decoded from APK class: pim
// Field count: 5, range: 1-5

syntax = "proto2";

message pim {
    optional string c = 1;
    optional string d = 2;
    optional int32 /* enum ? */ e = 3;
    optional int32 /* enum ? */ g = 4;
    optional int32 /* enum ? */  = 5;
}