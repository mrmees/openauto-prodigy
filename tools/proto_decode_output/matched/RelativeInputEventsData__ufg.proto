// Decoded from APK class: ufg
// Field count: 3, range: 1-3

syntax = "proto2";

message ufg {
    repeated ahfw c = 1;  // msg: ahfw
    optional aaav d = 2;  // msg: aaav
    optional int32 /* enum a */ e = 3;  // enum: a
}

// === DIFF vs our proto ===
// + field 2: message d (msg: aaav)
// + field 3: enum e (enum: a)
