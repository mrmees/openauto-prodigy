// Decoded from APK class: aahi
// Field count: 5, range: 1-5

syntax = "proto2";

message aahi {
    optional aahd c = 1;  // msg: aahd
    optional aagr d = 2;  // msg: aagr
    optional string e = 3;
    optional string f = 4;
    optional string  = 5;
}

// === DIFF vs our proto ===
// + field 1: message c (msg: aahd)
// + field 2: message d (msg: aagr)
// + field 3: string e
