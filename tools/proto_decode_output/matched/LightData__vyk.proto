// Decoded from APK class: vyk
// Field count: 3, range: 1-3

syntax = "proto2";

message vyk {
    optional int32 /* enum a */ c = 1;  // enum: a
    optional int32 /* enum a */ d = 2;  // enum: a
    optional bool e = 3;
}