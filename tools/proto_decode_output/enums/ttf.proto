// Decoded from APK class: ttf

syntax = "proto2";

enum ttf {
    GMM_SERVER_ONLY = 0;
    CLEARCUT_SERVICE_ONLY = 1;
    DUAL_WRITE = 2;
    NON_LOG204_CLEARCUT_ONLY_LOG204_GMM_SERVER_ONLY = 3;
    NON_LOG204_CLEARCUT_ONLY_LOG204_DUAL_WRITE = 4;
}