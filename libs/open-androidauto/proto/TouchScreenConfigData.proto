/*
 * TouchScreenConfig — touch screen capability descriptor (vxz.java, AA v16.1).
 *
 * Used as repeated field in InputChannel (vya) field 2.
 * Contains display dimensions and display type enum.
 */

syntax="proto3";

import "DisplayTypeEnum.proto";

package oaa.proto.data;

message TouchScreenConfig
{
    optional int32 width = 1;
    optional int32 height = 2;
    optional enums.DisplayType.Enum display_type = 3;
}
