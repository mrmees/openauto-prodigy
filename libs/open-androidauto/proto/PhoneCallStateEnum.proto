/*
 * PhoneCallState — call state enum for PhoneStatusUpdate.
 *
 * APK class: wae.java (AA v16.1)
 * Used in PhoneCall field 1 (required).
 * Telecom state mapping in vuk.java.
 */

syntax="proto3";

package oaa.proto.enums;

message PhoneCallState
{
    enum Enum
    {
        UNKNOWN = 0;
        IN_CALL = 1;
        ON_HOLD = 2;
        INACTIVE = 3;
        INCOMING = 4;
        CONFERENCED = 5;
        MUTED = 6;
    }
}
