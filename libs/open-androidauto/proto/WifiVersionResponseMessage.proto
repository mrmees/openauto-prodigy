/*
 * WifiVersionResponse — BT WiFi version negotiation response.
 *
 * APK class: wdw.java (AA v16.1)
 * BT RFCOMM message type: ordinal 4 (MESSAGE_WIFI_VERSION_RESPONSE_BT)
 * Dispatched by ngh.java line 541.
 * Logged at ngh.java lines 593-603 with field names:
 *   majorVersion, minorVersion, deviceSerial, versionStatus, selectedWifiChannelType
 *
 * Sent by phone in response to WifiVersionRequest.
 */

syntax = "proto3";

import "SessionInfoData.proto";

package oaa.proto.messages;

message WifiVersionResponse {
    optional int32 major_version = 1;              // APK: b (int)
    optional int32 minor_version = 2;              // APK: c (int)
    optional string device_serial = 3;             // APK: e (String)
    optional sint32 version_status = 4;            // APK: f (int, default=1, sint32 via wck.p/viz.au)
    optional int32 selected_wifi_channel_type = 5; // APK: g (int) — 1=5GHz_ONLY, 2=24GHz_ONLY, 3=DUAL_BAND
    optional data.SessionInfo session_info = 6;    // APK: h (vvf)
    optional bytes extra_data = 7;                 // APK: i (zxq/ByteString)
}
