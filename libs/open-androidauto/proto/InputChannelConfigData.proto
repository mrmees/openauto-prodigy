/*
 * Input channel configuration with touch and input device settings.
 *
 * APK class: vya.java (AA v16.1)
 * Descriptor: version 1, 5 fields
 * Object[] refs: vxz (TouchScreenConfig), vxy (TouchPadConfig), vvs.m (enum verifier)
 * Note: zpn.java is a simpler variant with only 2 fields — vya is the full version.
 */

syntax="proto2";

import "TouchScreenConfigData.proto";
import "TouchPadConfigData.proto";

package oaa.proto.data;

// APK class: vya — full input channel config (5 fields)
message InputChannelConfig
{
    repeated int32 input_type = 1;                          // APK: packed int32 (input device types)
    repeated TouchScreenConfig touch_screen_configs = 2;    // APK: vxz.class (repeated message)
    repeated TouchPadConfig touchpad_configs = 3;           // APK: vxy.class (repeated message)
    repeated int32 key_configs = 4;                         // APK: repeated enum (vvs.m verifier)
    optional uint32 capabilities = 5;                       // APK: singular uint32
}
