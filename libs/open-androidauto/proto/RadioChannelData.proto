/*
 * Discovered via APK proto decoder (way.java / wax.java, AA v16.1)
 * Radio channel descriptor — deprecated but still in the protocol.
 */

syntax="proto3";

package oaa.proto.data;

// APK class: way — radio channel config container (1 field)
message RadioChannel
{
    repeated bytes radio_stations = 1;  // APK: wax sub-message
}
