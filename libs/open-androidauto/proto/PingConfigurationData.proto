/*
 * PingConfiguration — connection keepalive parameters.
 *
 * APK class: zyd.java (AA v16.1)
 * Sub-message of ConnectionConfiguration field 1.
 * Only 2 fields (was incorrectly 4 in previous version).
 */

syntax="proto2";

package oaa.proto.data;

message PingConfiguration {
    optional int64 ping_interval_ns = 1;    // APK: long b — was uint32 timeout_ms
    optional uint32 ping_timeout_ms = 2;    // APK: int c
    // fields 3-4 removed — do not exist in APK zyd.java
}
