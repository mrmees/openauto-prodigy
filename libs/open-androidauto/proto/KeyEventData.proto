/*
 * Discovered via APK proto decoder (vvh.java, AA v16.1)
 * Sub-message for keyboard/button key event with scan code.
 */

syntax="proto2";

package oaa.proto.data;

// APK class: vyi — key event with keycode and down/pressed state
message KeyEvent
{
    required uint32 keycode = 1;
    required bool is_pressed = 2;
    optional uint32 meta_state = 3;
    optional bool long_press = 4;
}
