---
phase: 05-static-grid-rendering-widget-revision
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - qml/widgets/ClockWidget.qml
  - qml/widgets/AAStatusWidget.qml
  - qml/widgets/NowPlayingWidget.qml
autonomous: true
requirements: [REV-01, REV-02, REV-03]

must_haves:
  truths:
    - "Clock widget shows time-only at 1x1, time+date at 2x1, time+date+day at 2x2+"
    - "AA Status widget shows icon-only at 1x1, icon+text at 2x1+"
    - "Now Playing widget shows compact strip at 2x1, full layout at 3x2+"
    - "All widgets use pixel-based width/height breakpoints, not isMainPane or widgetContext.paneSize"
    - "All widgets scale font sizes using UiMetrics tokens (no hardcoded pixel values)"
  artifacts:
    - path: "qml/widgets/ClockWidget.qml"
      provides: "3-tier responsive clock layout"
      contains: "showDate"
    - path: "qml/widgets/AAStatusWidget.qml"
      provides: "2-tier responsive AA status"
      contains: "showText"
    - path: "qml/widgets/NowPlayingWidget.qml"
      provides: "2-tier responsive now playing"
      contains: "isFullLayout"
  key_links:
    - from: "qml/widgets/ClockWidget.qml"
      to: "width/height"
      via: "pixel breakpoints"
      pattern: "width >= |height >="
    - from: "qml/widgets/AAStatusWidget.qml"
      to: "width/height"
      via: "pixel breakpoints"
      pattern: "width >= |showText"
    - from: "qml/widgets/NowPlayingWidget.qml"
      to: "width/height"
      via: "pixel breakpoints"
      pattern: "isFullLayout"
---

<objective>
Revise all three shipped widgets (Clock, AA Status, Now Playing) to use pixel-based width/height breakpoints instead of the legacy isMainPane boolean, enabling responsive layout across any grid cell size.

Purpose: The old isMainPane/widgetContext.paneSize logic is tied to the removed 3-pane system. Widgets need to adapt layout based on their actual allocated pixel dimensions in the new grid.

Output: Three widget QML files with responsive breakpoint tiers that render correctly at any grid span.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v0.5.3-phases/05-static-grid-rendering-widget-revision/05-CONTEXT.md
@.planning/milestones/v0.5.3-phases/05-static-grid-rendering-widget-revision/05-RESEARCH.md
@.planning/milestones/v0.5.3-phases/05-static-grid-rendering-widget-revision/05-01-SUMMARY.md

<interfaces>
<!-- Widget pixel dimensions at various grid spans (1024x600 display, 6x4 grid): -->
<!-- Container: ~984 x ~484 (after margins + dock) -->
<!-- cellWidth ~164px, cellHeight ~121px -->
<!-- 1x1: ~164x121 (minus 8px gutters = ~156x113 inner) -->
<!-- 2x1: ~328x121 (inner ~320x113) -->
<!-- 2x2: ~328x242 (inner ~320x234) -->
<!-- 3x2: ~492x242 (inner ~484x234) -->

<!-- Breakpoint thresholds (from research, Claude's discretion on exact values): -->
<!-- "show text" threshold: width >= ~250px (between 1-cell and 2-cell widths) -->
<!-- "show full/tall content" threshold: height >= ~200px (between 1-row and 2-row heights) -->

From qml/controls/UiMetrics.qml (font tokens):
```qml
readonly property int fontSmall:   Math.max(Math.round(22 * _fontScaleTotal), 16)
readonly property int fontBody:    Math.max(Math.round(28 * _fontScaleTotal), 20)
readonly property int fontTitle:   Math.max(Math.round(30 * _fontScaleTotal), 22)
readonly property int fontHeading: Math.max(Math.round(36 * _fontScaleTotal), 26)
readonly property int iconSize:    Math.round(36 * scale)
readonly property int iconSmall:   Math.round(20 * scale)
readonly property int spacing:     Math.round(8 * scale)
```

From CONTEXT.md (locked widget tier decisions):
- Clock 1x1: Large centered time only, bold weight, auto-sized to fill. No AM/PM, respects 24h toggle.
- Clock 2x1: Time + date (e.g. "March 12")
- Clock 2x2+: Time + date + day (e.g. "Wednesday") -- three distinct lines
- AA Status 1x1: Single connection state icon filling cell. Green=connected, grey=disconnected. Tap launches AA.
- AA Status 2x1+: Icon + status text ("Connected" / "Tap to connect")
- Now Playing 2x1: Track title (truncated) + play/pause button. Artist cut.
- Now Playing 3x2: Title, artist, prev/play/next controls -- full media card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise ClockWidget.qml with pixel breakpoints</name>
  <files>qml/widgets/ClockWidget.qml</files>
  <action>
Replace the entire isMainPane/widgetContext.paneSize logic with pixel-based breakpoints.

1. Remove the `isMainPane` property and all `widgetContext` references
2. Add breakpoint properties:
   - `readonly property bool showDate: width >= 250` (true when >= 2 cells wide)
   - `readonly property bool showDay: height >= 200` (true when >= 2 cells tall)
3. Remove the outer transparent Rectangle wrapper (unnecessary -- WidgetHost/grid provides background)

**Layout tiers (per user decision):**

- **1x1 (compact, ~156x113):** Large centered time only, bold weight. The time IS the widget. Use `fontHeading * 2.0` or similar to fill the cell. No AM/PM indicator.
- **2x1 (wide, ~320x113):** Time + date. Time slightly smaller than 1x1 to fit with date. Date shows month + day (e.g. "March 12").
- **2x2+ (full, ~320x234+):** Time + date + day of week on three distinct lines. Each line scales with available space.

Font sizing (use UiMetrics tokens, Claude's discretion on multipliers):
- 1x1 time: `UiMetrics.fontHeading * 2.0`, `Font.Bold`
- 2x1 time: `UiMetrics.fontHeading * 1.8`, `Font.Light`
- 2x2 time: `UiMetrics.fontHeading * 2.5`, `Font.Light`
- Date: `UiMetrics.fontTitle` (2x1) or `UiMetrics.fontTitle` (2x2)
- Day: `UiMetrics.fontBody`

Keep the existing Timer that updates every 1000ms. Keep the date/day format strings. The 24h toggle is already handled by `toLocaleTimeString` using the system locale -- the format "h:mm" respects locale's 24h preference.

Set `dateText.visible: showDate` and `dayText.visible: showDay`.

Center all content in the widget using `anchors.centerIn: parent` on the ColumnLayout.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -3</automated>
  </verify>
  <done>ClockWidget uses width/height breakpoints. No isMainPane or widgetContext references. Time-only at small sizes, time+date when wide, time+date+day when tall enough.</done>
</task>

<task type="auto">
  <name>Task 2: Revise AAStatusWidget.qml and NowPlayingWidget.qml with pixel breakpoints</name>
  <files>qml/widgets/AAStatusWidget.qml, qml/widgets/NowPlayingWidget.qml</files>
  <action>
**AAStatusWidget.qml:**

1. Remove `isMainPane` property and all `widgetContext` references
2. Add: `readonly property bool showText: width >= 250`
3. Remove the outer transparent Rectangle wrapper
4. Remove the "Android Auto" subtitle NormalText entirely (was only visible on isMainPane, replaced by showText tier)

Layout tiers (per user decision):
- **1x1 (icon only):** Single icon centered, filling the cell. Use `UiMetrics.iconSize * 2` for icon size. Green (`ThemeService.primary`) when connected, grey (`ThemeService.onSurfaceVariant`) when disconnected.
- **2x1+ (icon + text):** Icon at `UiMetrics.iconSize * 1.5` + status text ("Connected" / "Tap to connect"). Use a RowLayout for horizontal arrangement at this tier (icon left, text right). Or keep ColumnLayout but make icon smaller to fit text below.

Decision (Claude's discretion): Use ColumnLayout with icon above text for both tiers. At 1x1 the text is simply hidden and the icon is larger. At 2x1+ the icon shrinks and text appears below.

Keep the MouseArea for tap-to-connect and pressAndHold for context menu forwarding. The pressAndHold forwarding to `parent.requestContextMenu()` should stay as-is (it will work with the grid's Loader).

**NowPlayingWidget.qml:**

1. Remove `isMainPane` property and all `widgetContext` references
2. Add: `readonly property bool isFullLayout: width >= 400 && height >= 200`
3. Remove the outer transparent Rectangle wrapper
4. Replace the dual-visibility ColumnLayout/RowLayout pattern (visible: isMainPane / visible: !isMainPane) with a single layout that adapts:

Layout tiers (per user decision):
- **Compact (2x1, ~320x113):** Horizontal RowLayout: music icon + title (elided) + play/pause button. No artist, no prev/next. Keep it tight for the strip.
- **Full (3x2+, ~484x234+):** Centered ColumnLayout: music icon, title, artist, prev/play/next controls row. Same as the current isMainPane layout.

Font sizes: Use UiMetrics tokens (fontTitle for title in full, fontBody for title in compact, fontBody for artist, etc.)

Keep all BtAudioPlugin property bindings and null checks. Keep the typeof checks for BtAudioPlugin safety.

Keep play/pause/prev/next MouseArea touch targets with `-UiMetrics.spacing` negative margins for fat-finger tolerance.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>All three widgets use pixel breakpoints (no isMainPane, no widgetContext.paneSize). AA Status shows icon-only at small sizes, icon+text when wide. NowPlaying shows compact strip at small sizes, full controls when large. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd build && cmake --build . -j$(nproc)`
2. All tests pass: `cd build && ctest --output-on-failure`
3. No QML warnings about `widgetContext`, `paneSize`, or `isMainPane` at startup
4. Widgets render at appropriate breakpoint tiers for their grid cell sizes
5. Clock at 1x1 shows time only, at 2x1 shows time+date, at 2x2 shows time+date+day
6. AA Status at 1x1 shows icon only, at 2x1 shows icon+text
7. NowPlaying at 2x1 shows compact strip, at 3x2 shows full controls
</verification>

<success_criteria>
- Zero references to `isMainPane`, `widgetContext.paneSize`, or `WidgetPlacementModel` in any widget QML file
- All widgets use `width`/`height` properties for breakpoint decisions
- Font sizes use UiMetrics tokens (no hardcoded pixel values)
- All three widgets compile and render without QML warnings
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5.3-phases/05-static-grid-rendering-widget-revision/05-02-SUMMARY.md`
</output>
