---
phase: 03.3-m3-color-role-mapping-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/services/ThemeService.hpp
  - src/core/services/ThemeService.cpp
  - config/themes/default/theme.yaml
  - config/themes/amoled/theme.yaml
  - config/themes/ocean/theme.yaml
  - config/themes/ember/theme.yaml
  - config/themes/connected-device/theme.yaml
  - tests/test_theme_service.cpp
  - tests/data/themes/default/theme.yaml
  - qml/controls/NormalText.qml
  - qml/applications/android_auto/AndroidAutoMenu.qml
  - qml/applications/bt_audio/BtAudioView.qml
  - qml/applications/phone/IncomingCallOverlay.qml
  - qml/applications/phone/PhoneView.qml
  - qml/applications/settings/AASettings.qml
  - qml/applications/settings/AboutSettings.qml
  - qml/applications/settings/CompanionSettings.qml
  - qml/applications/settings/ConnectionSettings.qml
  - qml/applications/settings/ConnectivitySettings.qml
  - qml/applications/settings/DisplaySettings.qml
  - qml/applications/settings/EqSettings.qml
  - qml/applications/settings/SystemSettings.qml
  - qml/applications/settings/VideoSettings.qml
  - qml/components/ExitDialog.qml
  - qml/components/FirstRunBanner.qml
  - qml/components/GestureOverlay.qml
  - qml/components/NavbarPopup.qml
  - qml/components/Navbar.qml
  - qml/components/NotificationArea.qml
  - qml/components/PairingDialog.qml
  - qml/controls/EqBandSlider.qml
  - qml/controls/FullScreenPicker.qml
  - qml/controls/InfoBanner.qml
  - qml/controls/ReadOnlyField.qml
  - qml/controls/SectionHeader.qml
  - qml/controls/SegmentedButton.qml
  - qml/controls/SettingsListItem.qml
  - qml/controls/SettingsSlider.qml
  - qml/controls/SettingsToggle.qml
autonomous: true
requirements:
  - M3R-01
  - M3R-02

must_haves:
  truths:
    - "All QML files reference onSurface instead of textPrimary for primary text color"
    - "All QML files reference onSurfaceVariant instead of textSecondary for secondary text color"
    - "All QML files reference error/onError instead of red/onRed for destructive/error colors"
    - "All QML files reference tertiary instead of yellow for accent indicator color"
    - "ThemeService no longer exposes textPrimary, textSecondary, red, onRed, yellow, onYellow Q_PROPERTYs"
    - "Bundled theme YAMLs no longer contain deprecated custom token keys"
    - "Project builds and all tests pass"
  artifacts:
    - path: "src/core/services/ThemeService.hpp"
      provides: "ThemeService without deprecated custom Q_PROPERTYs"
      contains: "!textPrimary"
    - path: "config/themes/default/theme.yaml"
      provides: "Default theme without deprecated keys"
      contains: "!text-primary"
    - path: "qml/controls/NormalText.qml"
      provides: "NormalText using onSurface"
      contains: "onSurface"
  key_links:
    - from: "qml/**/*.qml"
      to: "src/core/services/ThemeService.hpp"
      via: "Q_PROPERTY bindings"
      pattern: "ThemeService\\.onSurface"
---

<objective>
Remove deprecated custom AA token Q_PROPERTYs from ThemeService (textPrimary, textSecondary, red, onRed, yellow, onYellow) and perform bulk QML rename of all ~170 references to their M3 equivalents. Clean deprecated keys from bundled theme YAMLs and AA token persistence sets. Update unit tests.

Purpose: Eliminate legacy custom color tokens so all QML references use standard M3 role names, making companion themes work without custom mapping.
Output: ThemeService with only M3 standard + success/onSuccess/barShadow derived properties; all QML files using M3 names; clean theme YAMLs; passing tests.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-m3-color-role-mapping-fix/03.3-CONTEXT.md
@.planning/phases/03.3-m3-color-role-mapping-fix/03.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated Q_PROPERTYs + clean YAML + update tests</name>
  <files>
    src/core/services/ThemeService.hpp,
    src/core/services/ThemeService.cpp,
    config/themes/default/theme.yaml,
    config/themes/amoled/theme.yaml,
    config/themes/ocean/theme.yaml,
    config/themes/ember/theme.yaml,
    config/themes/connected-device/theme.yaml,
    tests/test_theme_service.cpp,
    tests/data/themes/default/theme.yaml
  </files>
  <action>
    **ThemeService.hpp:**
    1. Remove 6 Q_PROPERTY declarations: `textPrimary`, `textSecondary`, `red`, `onRed`, `yellow`, `onYellow`
    2. Remove 6 accessor methods: `textPrimary()`, `textSecondary()`, `red()`, `onRed()`, `yellow()`, `onYellow()`
    3. Remove the `pressed` Q_PROPERTY declaration and `pressed()` accessor method declaration
    4. Keep `barShadow`, `success`, `onSuccess` (still needed)

    **ThemeService.cpp:**
    1. Remove the `pressed()` method implementation (it computes onSurface at 10% alpha)
    2. Remove `"pressed"` from `recalcDerivedColors()` if it's stored there
    3. Remove deprecated keys from `knownAATokenKeys` static set: `"text-primary"`, `"text-secondary"`, `"red"`, `"on-red"`, `"yellow"`, `"on-yellow"`
    4. Remove same 6 keys from `knownM3Keys` static set (they were custom, not actually M3)
    5. Remove same 6 keys from `allThemeKeys` set construction

    **All 5 bundled theme YAMLs** (default, amoled, ocean, ember, connected-device):
    Remove these keys from BOTH day and night maps: `text-primary`, `text-secondary`, `red`, `on-red`, `yellow`, `on-yellow`

    **tests/test_theme_service.cpp:**
    1. In `dayModeColors()`: change `service.textPrimary()` to `service.onSurface()` (expect same color value)
    2. In `nightModeColors()`: change `service.textPrimary()` to `service.onSurface()` (expect same color value)
    3. In `allBaseTokensPresent()`: remove the 6 QVERIFY lines for textPrimary/textSecondary/red/onRed/yellow/onYellow
    4. In `derivedColors()`: remove the `pressed` QCOMPARE assertion entirely
    5. In `backwardCompatOldPropertyNames()`: remove the entire test or convert to verify the properties DON'T exist (QVERIFY that calling activeColor("text-primary") returns transparent/default since the key is gone from YAML)
    6. In test data theme YAML (`tests/data/themes/default/theme.yaml`): remove the 6 deprecated keys from day/night maps. Ensure `on-surface` has the value that `text-primary` used to have (white for day, #c0c0c0 for night) so existing test assertions still pass.

    **CRITICAL:** The QML rename in Task 2 MUST happen in the same build as this C++ removal. Both tasks are in the same plan and will be committed together. Do NOT build between tasks -- removing Q_PROPERTYs without updating QML would cause runtime binding failures.
  </action>
  <verify>
    <automated>MISSING -- Task 2 must complete first, then build + test together. Do not build after this task alone.</automated>
  </verify>
  <done>ThemeService.hpp has no textPrimary/textSecondary/red/onRed/yellow/onYellow/pressed Q_PROPERTYs. All 5 bundled theme YAMLs have no deprecated keys. Test file updated to use M3 names.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk QML rename -- all deprecated token references to M3 equivalents</name>
  <files>
    qml/controls/NormalText.qml,
    qml/controls/SectionHeader.qml,
    qml/controls/InfoBanner.qml,
    qml/controls/ReadOnlyField.qml,
    qml/controls/EqBandSlider.qml,
    qml/controls/FullScreenPicker.qml,
    qml/controls/SegmentedButton.qml,
    qml/controls/SettingsListItem.qml,
    qml/controls/SettingsSlider.qml,
    qml/controls/SettingsToggle.qml,
    qml/components/ExitDialog.qml,
    qml/components/FirstRunBanner.qml,
    qml/components/GestureOverlay.qml,
    qml/components/NavbarPopup.qml,
    qml/components/Navbar.qml,
    qml/components/NotificationArea.qml,
    qml/components/PairingDialog.qml,
    qml/applications/android_auto/AndroidAutoMenu.qml,
    qml/applications/bt_audio/BtAudioView.qml,
    qml/applications/phone/IncomingCallOverlay.qml,
    qml/applications/phone/PhoneView.qml,
    qml/applications/settings/AASettings.qml,
    qml/applications/settings/AboutSettings.qml,
    qml/applications/settings/CompanionSettings.qml,
    qml/applications/settings/ConnectionSettings.qml,
    qml/applications/settings/ConnectivitySettings.qml,
    qml/applications/settings/DisplaySettings.qml,
    qml/applications/settings/EqSettings.qml,
    qml/applications/settings/SystemSettings.qml,
    qml/applications/settings/VideoSettings.qml
  </files>
  <action>
    Perform the following find-replace operations across ALL QML files:

    **Token renames (mechanical):**
    1. `ThemeService.textPrimary` -> `ThemeService.onSurface` (all ~99 refs)
    2. `ThemeService.textSecondary` -> `ThemeService.onSurfaceVariant` (all ~58 refs)
    3. `ThemeService.red` -> `ThemeService.error` (all ~15 refs). Be careful: only match `ThemeService.red` not `ThemeService.onRed` or other substrings. Watch for `Qt.darker(ThemeService.red, ...)` -- change to `Qt.darker(ThemeService.error, ...)`.
    4. `ThemeService.onRed` -> `ThemeService.onError` (all ~3 refs)
    5. `ThemeService.yellow` -> `ThemeService.tertiary` (all ~3 refs)
    6. `ThemeService.onYellow` -> Do NOT replace (should be 0 remaining QML refs, but if any exist, replace with `ThemeService.onTertiary`)

    **NormalText.qml specifically:** The default `color` property should change from `ThemeService.textPrimary` to `ThemeService.onSurface`. This one change propagates to all NormalText instances that don't override color.

    **Text on colored backgrounds:** Where text sits on an `error` background, ensure it uses `ThemeService.onError` (not `onSurface`). Where text sits on a `success` background, ensure it uses `ThemeService.onSuccess`. The research doc has specific patterns for this.

    **Verification after all renames:**
    Run `grep -rn "ThemeService\.\(textPrimary\|textSecondary\|onRed\|onYellow\)" qml/` -- should return 0 results.
    Run `grep -rn "ThemeService\.red\b" qml/` -- should return 0 results (be careful with regex to avoid matching `ThemeService.error` etc.).
    Run `grep -rn "ThemeService\.yellow\b" qml/` -- should return 0 results.

    **Then build and test:**
    ```bash
    cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) && ctest --output-on-failure
    ```
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) && ctest --output-on-failure</automated>
  </verify>
  <done>Zero QML references to textPrimary, textSecondary, red (as ThemeService.red), onRed, yellow, onYellow. Project compiles cleanly. All tests pass. No deprecated token names remain anywhere in the codebase except historical docs/comments.</done>
</task>

</tasks>

<verification>
1. `grep -rn "ThemeService\.\(textPrimary\|textSecondary\|onRed\|onYellow\)" qml/` returns empty
2. `grep -rn "ThemeService\.red\b" qml/` returns empty
3. `grep -rn "ThemeService\.yellow\b" qml/` returns empty
4. `grep -rn "Q_PROPERTY.*textPrimary\|Q_PROPERTY.*textSecondary\|Q_PROPERTY.*\bred\b\|Q_PROPERTY.*onRed\|Q_PROPERTY.*yellow\|Q_PROPERTY.*onYellow\|Q_PROPERTY.*pressed" src/core/services/ThemeService.hpp` returns empty
5. `grep -rn "text-primary\|text-secondary\|on-red\|on-yellow" config/themes/*/theme.yaml` returns empty (only match should be in docs)
6. Build passes: `cmake --build build -j$(nproc)`
7. Tests pass: `cd build && ctest --output-on-failure`
</verification>

<success_criteria>
- ThemeService exposes only M3 standard roles + success/onSuccess/barShadow/scrim derived colors
- All ~170 QML token references use M3 names (onSurface, onSurfaceVariant, error, onError, tertiary)
- All 5 bundled theme YAMLs contain only M3 standard + success keys
- knownAATokenKeys and knownM3Keys contain only standard M3 keys
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-m3-color-role-mapping-fix/03.3-01-SUMMARY.md`
</output>
