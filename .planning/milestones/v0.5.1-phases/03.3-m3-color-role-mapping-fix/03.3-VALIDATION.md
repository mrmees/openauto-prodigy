---
phase: 03.3
slug: m3-color-role-mapping-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-09
---

# Phase 03.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | QTest (Qt 6) |
| **Config file** | tests/CMakeLists.txt |
| **Quick run command** | `cd build && ctest -R test_theme_service --output-on-failure` |
| **Full suite command** | `cd build && ctest --output-on-failure` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd build && ctest -R test_theme_service --output-on-failure`
- **After every plan wave:** Run `cd build && ctest --output-on-failure`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.3-01-01 | 01 | 1 | (implicit) | unit | `ctest -R test_theme_service --output-on-failure` | Needs update | ⬜ pending |
| 03.3-01-02 | 01 | 1 | (implicit) | unit | `ctest -R test_theme_service --output-on-failure` | Needs update | ⬜ pending |
| 03.3-01-03 | 01 | 1 | (implicit) | build | `cmake --build . -j$(nproc)` | N/A | ⬜ pending |
| 03.3-02-01 | 02 | 1 | (implicit) | build | `cmake --build . -j$(nproc)` | N/A | ⬜ pending |
| 03.3-02-02 | 02 | 1 | (implicit) | manual-only | Run app, switch themes | Manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_theme_service.cpp` — update: remove `backwardCompatOldPropertyNames` test, remove `textPrimary()`/`red()`/`yellow()` from `allBaseTokensPresent`, remove `pressed()` from `derivedColors`, update `dayModeColors`/`nightModeColors` to use `onSurface` instead of `textPrimary`

*Existing test infrastructure covers most phase requirements with updates above.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| QML files load without binding errors | (implicit) | Runtime QML binding failures not testable in unit tests | Run app, check console for "ReferenceError" or "is not a function" |
| Visual correctness across themes | (implicit) | Subjective visual assessment | Switch between all 5 bundled themes + companion theme, verify text visible, containers have depth |
| Automotive glanceability | (implicit) | Subjective assessment at distance | Verify pressed states and container hierarchy visible at arm's length |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
