---
phase: 03-theme-color-system
plan: 03
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - config/themes/connected-device/theme.yaml
  - src/core/services/ThemeService.hpp
  - src/core/services/ThemeService.cpp
  - src/core/aa/AndroidAutoOrchestrator.hpp
  - src/core/aa/AndroidAutoOrchestrator.cpp
  - tests/test_theme_service.cpp
autonomous: true
requirements: [THM-01, THM-02]

must_haves:
  truths:
    - "Connected Device theme appears in theme picker as 5th option"
    - "Connected Device theme loads default fallback colors on first launch"
    - "ThemeService has applyAATokens method that updates colors and persists to YAML"
    - "UiConfigRequest (0x8011) is sent to phone on AA session start with current theme colors"
    - "Received AA tokens overwrite Connected Device theme colors and emit colorsChanged"
  artifacts:
    - path: "config/themes/connected-device/theme.yaml"
      provides: "Connected Device theme with default fallback palette"
      contains: "id: connected-device"
    - path: "src/core/services/ThemeService.hpp"
      provides: "applyAATokens and persistConnectedDeviceTheme methods"
      contains: "void applyAATokens"
    - path: "src/core/aa/AndroidAutoOrchestrator.cpp"
      provides: "UiConfigRequest sending on session start"
      contains: "UiConfigRequest"
  key_links:
    - from: "src/core/aa/AndroidAutoOrchestrator.cpp"
      to: "src/core/services/ThemeService.hpp"
      via: "applyAATokens call"
      pattern: "applyAATokens"
    - from: "src/core/services/ThemeService.cpp"
      to: "config/themes/connected-device/theme.yaml"
      via: "YAML write for persistence"
      pattern: "persistConnectedDeviceTheme"
---

<objective>
Add Connected Device theme that receives live AA palette tokens from the phone, and implement UiConfigRequest sending on AA session start.

Purpose: When using the "Connected Device" theme, the HU chrome adopts colors from the phone's Material You palette, creating a unified visual experience. The theme file doubles as a persistent cache of last-received colors.
Output: 5th bundled theme, AA token ingestion method, UiConfigRequest protocol integration.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-theme-color-system/03-01-SUMMARY.md
@.planning/phases/03-theme-color-system/03-RESEARCH.md

<interfaces>
<!-- ThemeService after Plan 01 -->
From src/core/services/ThemeService.hpp:
```cpp
// 16 base properties: primary, onSurface, surface, etc.
// 5 derived: scrim, pressed, barShadow, success, onSuccess
QColor activeColor(const QString& key) const;
bool loadTheme(const QString& themeDirPath);
QString currentThemeId() const;
QMap<QString, QColor> dayColors_;
QMap<QString, QColor> nightColors_;
```

<!-- UiConfigRequest proto (read-only submodule) -->
From libs/prodigy-oaa-protocol/proto/oaa/video/UiConfigRequestMessage.proto:
```protobuf
message UiConfigRequest {
    optional UiConfigData config = 1;
}
message UiConfigData {
    repeated UiConfigEntry primary_configs = 1;
    repeated UiConfigEntry secondary_configs = 2;
}
message UiConfigEntry {
    optional string key = 1;
    oneof value {
        UiConfigValue config_value = 2;
    }
}
message UiConfigValue {
    optional uint32 value = 1;
}
```

<!-- AndroidAutoOrchestrator pattern -->
Orchestrator manages AA session lifecycle and has access to ThemeService via HostContext.
Video channel messages are sent via messenger/session infrastructure.
Wire message 0x8011 = UiConfigRequest, HU->Phone on video channel.
Response 0x8012 = UpdateHuUiConfigResponse (ThemingTokensStatus enum).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Connected Device theme + applyAATokens method</name>
  <files>config/themes/connected-device/theme.yaml, src/core/services/ThemeService.hpp, src/core/services/ThemeService.cpp, tests/test_theme_service.cpp</files>
  <behavior>
    - Test: Connected Device theme YAML loads with all 16 base tokens in day and night
    - Test: applyAATokens with {"primary": 0xFFFF0000} changes primary() to red when connected-device theme is active
    - Test: applyAATokens does NOT change colors when a different theme is active
    - Test: applyAATokens emits colorsChanged signal
    - Test: persistConnectedDeviceTheme writes updated colors to YAML (use temp dir)
  </behavior>
  <action>
1. Create `config/themes/connected-device/theme.yaml`:
   - id: connected-device
   - name: Connected Device
   - version: 2.0.0
   - font_family: "Lato"
   - day/night sections with all 16 AA token keys, using the default theme's colors as initial fallback palette
   - Add a comment explaining this theme is updated live when AA sends UiConfigRequest tokens

2. Add to `ThemeService.hpp`:
   - `void applyAATokens(const QMap<QString, uint32_t>& argbTokens)` -- public method
   - `void persistConnectedDeviceTheme()` -- private method
   - Add `#include <yaml-cpp/yaml.h>` if not already present in header (or forward declare and keep in .cpp)

3. Implement in `ThemeService.cpp`:
   - `applyAATokens()`:
     - Guard: if `currentThemeId() != "connected-device"` return early
     - For each token in argbTokens: convert uint32_t ARGB to QColor via `QColor::fromRgba(value)`, store in both dayColors_ and nightColors_ (AA sends one palette, apply to both modes)
     - Only process known AA token key strings (the 16 base tokens plus any of the 46 known keys per user decision on future-proofing)
     - Call `persistConnectedDeviceTheme()` to write to YAML
     - Emit `colorsChanged()`
   - `persistConnectedDeviceTheme()`:
     - Find the connected-device theme directory from `themeDirectories_` map
     - If not found, try `~/.openauto/themes/connected-device/` (user themes dir)
     - Create directory with `QDir::mkpath()` if needed
     - Write theme YAML with current dayColors_ and nightColors_ using yaml-cpp Emitter
     - Preserve id, name, version, font_family metadata

4. Add tests to `test_theme_service.cpp`:
   - `applyAATokensUpdatesColors()`: create connected-device theme in temp dir, load it, call applyAATokens with known ARGB values, verify primary() changed
   - `applyAATokensIgnoredForOtherThemes()`: load a different theme, call applyAATokens, verify colors unchanged
   - `applyAATokensEmitsSignal()`: verify colorsChanged emitted
   - `persistConnectedDeviceThemeWritesYaml()`: apply tokens, verify the YAML file was updated with new color values

5. Build and test: `cd build && cmake .. && make -j$(nproc) && ctest -R test_theme_service --output-on-failure`
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc) 2>&1 | tail -5 && ctest -R test_theme_service --output-on-failure</automated>
  </verify>
  <done>Connected Device theme exists as 5th bundled theme. applyAATokens updates colors when connected-device theme is active, persists to YAML, and emits colorsChanged. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire UiConfigRequest sending on AA session start</name>
  <files>src/core/aa/AndroidAutoOrchestrator.hpp, src/core/aa/AndroidAutoOrchestrator.cpp</files>
  <action>
Wire the UiConfigRequest (0x8011) sending on AA session start so the phone's projected UI can match the HU theme colors.

1. Find where AA session setup completes in `AndroidAutoOrchestrator.cpp` (after channel setup, before normal operation begins). This is the point to send UiConfigRequest.

2. Add a method `sendUiConfigRequest()` that:
   - Gets current theme colors from ThemeService (accessible via HostContext)
   - Constructs `oaa::proto::messages::UiConfigRequest` protobuf:
     - Create `UiConfigData` with `primary_configs` entries
     - For each of the 16 base token colors: create `UiConfigEntry` with key string and `UiConfigValue` with `QColor::rgba()` (returns ARGB uint32)
   - Sends the message as 0x8011 on the video channel
   - Logs the send at info level

3. Handle the response (0x8012 `UpdateHuUiConfigResponse`):
   - Parse `ThemingTokensStatus` enum from response
   - Log the status (accepted, rejected, error)
   - No action needed on the status -- it's informational

4. Call `sendUiConfigRequest()` after video channel setup completes.

5. Include the UiConfigRequest proto header. Check that the proto is compiled in the CMakeLists -- if not, ensure it's included in the prodigy-oaa-protocol build list.

6. Build: `cd build && cmake .. && make -j$(nproc)`
   Full test suite: `cd build && ctest --output-on-failure`

NOTE: This is HU->Phone only (we push our theme TO the phone). The Connected Device theme's "receive from phone" capability depends on whether the phone sends colors back in a message we haven't yet identified. For now, the Connected Device theme uses its cached/default colors, and applyAATokens is ready to receive tokens when/if we discover the phone->HU color message.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>UiConfigRequest (0x8011) sent on AA session start with current theme's 16 base token colors. Response (0x8012) handled and logged. Full build and test suite passes.</done>
</task>

</tasks>

<verification>
- `grep -r 'connected-device' config/themes/` confirms theme directory exists
- `grep 'applyAATokens' src/core/services/ThemeService.hpp` confirms method exists
- `grep 'UiConfigRequest' src/core/aa/AndroidAutoOrchestrator.cpp` confirms protocol integration
- `cd build && ctest --output-on-failure` -- all tests pass
</verification>

<success_criteria>
- Connected Device is 5th theme option, loadable and switchable
- applyAATokens method updates colors + persists to YAML when connected-device theme active
- UiConfigRequest sent on AA session start with theme colors
- UpdateHuUiConfigResponse handled and logged
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-theme-color-system/03-03-SUMMARY.md`
</output>
