---
phase: 03-theme-color-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/services/ThemeService.hpp
  - src/core/services/ThemeService.cpp
  - config/themes/default/theme.yaml
  - config/themes/amoled/theme.yaml
  - config/themes/ocean/theme.yaml
  - config/themes/ember/theme.yaml
  - tests/data/themes/default/theme.yaml
  - tests/test_theme_service.cpp
autonomous: true
requirements: [THM-01, THM-02]

must_haves:
  truths:
    - "ThemeService exposes 16 AA wire token properties (primary, onSurface, surface, surfaceVariant, surfaceContainerLow, inverseSurface, inverseOnSurface, outline, outlineVariant, background, textPrimary, textSecondary, red, onRed, yellow, onYellow)"
    - "ThemeService exposes derived color properties (scrim, pressed, barShadow, success, onSuccess)"
    - "All 4 bundled themes load with valid colors for all 16 base tokens in both day and night"
    - "Day/night mode switching works with new property names"
    - "Unit tests pass with updated accessor names and new derived color tests"
  artifacts:
    - path: "src/core/services/ThemeService.hpp"
      provides: "16 base Q_PROPERTYs with AA token names + derived color properties"
      contains: "Q_PROPERTY(QColor primary"
    - path: "src/core/services/ThemeService.cpp"
      provides: "Derived color implementations (scrim, pressed, success, barShadow)"
      contains: "QColor ThemeService::scrim()"
    - path: "config/themes/default/theme.yaml"
      provides: "Default theme with all 16 AA token keys"
      contains: "primary:"
    - path: "tests/test_theme_service.cpp"
      provides: "Updated tests for new property names + derived colors"
      contains: "service.primary()"
  key_links:
    - from: "src/core/services/ThemeService.hpp"
      to: "config/themes/*/theme.yaml"
      via: "activeColor(key) lookup"
      pattern: "activeColor\\(\"primary\"\\)"
    - from: "tests/test_theme_service.cpp"
      to: "src/core/services/ThemeService.hpp"
      via: "accessor method calls"
      pattern: "service\\.primary\\(\\)"
---

<objective>
Rename all 16 ThemeService Q_PROPERTYs from OAP-derived names to AA wire token names, add derived color properties, and migrate all bundled theme YAMLs.

Purpose: Establishes the semantic color foundation that all subsequent QML migration and Connected Device theme work builds on. This is a clean break -- old property names stop working.
Output: ThemeService with 16 base + 5 derived Q_PROPERTYs, 4 migrated theme YAMLs, passing unit tests.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-theme-color-system/03-RESEARCH.md

<interfaces>
<!-- ThemeService current interface (will be renamed) -->
From src/core/services/ThemeService.hpp:
```cpp
class ThemeService : public QObject, public IThemeService {
    Q_OBJECT
    // 16 old Q_PROPERTYs: backgroundColor, highlightColor, controlBackgroundColor, etc.
    // All use pattern: QColor accessorName() const { return activeColor("yaml_key"); }
    QColor activeColor(const QString& key) const;
    // dayColors_ and nightColors_ are QMap<QString, QColor> loaded from YAML
};
```

Property rename mapping (from RESEARCH.md):
| Old Property | Old YAML Key | New Property | New YAML Key |
|-------------|-------------|-------------|-------------|
| backgroundColor | background | background | background |
| highlightColor | highlight | primary | primary |
| controlBackgroundColor | control_background | surfaceVariant | surface_variant |
| controlForegroundColor | control_foreground | onSurface | on_surface |
| normalFontColor | normal_font | textPrimary | text_primary |
| specialFontColor | special_font | primary | primary (collapsed) |
| descriptionFontColor | description_font | textSecondary | text_secondary |
| barBackgroundColor | bar_background | surfaceContainerLow | surface_container_low |
| controlBoxBackgroundColor | control_box_background | surface | surface |
| gaugeIndicatorColor | gauge_indicator | primary | primary (collapsed) |
| iconColor | icon | onSurface | on_surface (collapsed) |
| sideWidgetBackgroundColor | side_widget_background | surface | surface (collapsed) |
| barShadowColor | bar_shadow | (derived) | -- |
| dividerColor | divider | outlineVariant | outline_variant |
| pressedColor | pressed | (derived) | -- |
| highlightFontColor | highlight_font | inverseSurface | inverse_surface |

New tokens not mapped from old: inverseOnSurface, outline, red, onRed, yellow, onYellow
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename ThemeService properties and add derived colors</name>
  <files>src/core/services/ThemeService.hpp, src/core/services/ThemeService.cpp, tests/test_theme_service.cpp, tests/data/themes/default/theme.yaml</files>
  <behavior>
    - Test: service.primary() returns the "primary" key color from theme YAML
    - Test: service.onSurface() returns the "on_surface" key color
    - Test: service.surface() returns the "surface" key color
    - Test: service.background() returns the "background" key color (same key, just accessor rename)
    - Test: service.textPrimary() returns the "text_primary" key color
    - Test: service.textSecondary() returns the "text_secondary" key color
    - Test: service.surfaceContainerLow() returns the "surface_container_low" key color
    - Test: service.inverseSurface() returns the "inverse_surface" key color
    - Test: service.outlineVariant() returns the "outline_variant" key color
    - Test: service.red() returns the "red" key color
    - Test: service.scrim() returns QColor(0,0,0,180) regardless of theme
    - Test: service.pressed() returns onSurface color at 10% alpha
    - Test: service.barShadow() returns QColor(0,0,0,128)
    - Test: service.success() returns QColor("#4CAF50")
    - Test: Day/night mode switching still works with new accessor names
    - Test: color("primary") IThemeService lookup works
  </behavior>
  <action>
1. Update `tests/data/themes/default/theme.yaml` to use new AA token key names (primary, on_surface, surface, surface_variant, surface_container_low, inverse_surface, inverse_on_surface, outline, outline_variant, background, text_primary, text_secondary, red, on_red, yellow, on_yellow). Map existing color values using the rename table from RESEARCH.md. Add new keys that didn't exist before (inverse_on_surface, outline, red, on_red, yellow, on_yellow) with sensible defaults.

2. Update `tests/test_theme_service.cpp`:
   - Rename all accessor calls: `backgroundColor()` -> `background()`, `highlightColor()` -> `primary()`, `normalFontColor()` -> `textPrimary()`, `barBackgroundColor()` -> `surfaceContainerLow()`
   - Update `colorByName()` test: `color("highlight")` -> `color("primary")`
   - Add new test `derivedColors()` verifying scrim, pressed, barShadow, success, onSuccess return expected values
   - Add new test `allBaseTokensPresent()` that loads the test theme and checks all 16 base accessors return non-transparent colors

3. Update `ThemeService.hpp`:
   - Replace all 16 Q_PROPERTY declarations with new AA token names. The 16 base properties are: primary, onSurface, surface, surfaceVariant, surfaceContainerLow, inverseSurface, inverseOnSurface, outline, outlineVariant, background, textPrimary, textSecondary, red, onRed, yellow, onYellow
   - Add derived Q_PROPERTYs: scrim, pressed, barShadow, success, onSuccess (all NOTIFY colorsChanged)
   - Replace accessor method declarations with new names mapping to new YAML keys
   - Remove old accessor declarations (backgroundColor, highlightColor, etc.)
   - Note: `specialFontColor` and `gaugeIndicatorColor` both collapse into `primary`. `iconColor` collapses into `onSurface`. `sideWidgetBackgroundColor` and `controlBoxBackgroundColor` collapse into `surface`. This is intentional per user decision.

4. Update `ThemeService.cpp`:
   - Update `dividerColor()` -> `outlineVariant()` with same fallback logic but using key "outline_variant"
   - Remove old `pressedColor()` and `highlightFontColor()` implementations
   - Add derived color implementations:
     - `scrim()`: return QColor(0, 0, 0, 180)
     - `pressed()`: QColor c = activeColor("on_surface"); c.setAlpha(26); return c
     - `barShadow()`: return QColor(0, 0, 0, 128)
     - `success()`: return QColor("#4CAF50")
     - `onSuccess()`: return QColor("#FFFFFF")

5. Build and run tests: `cd build && cmake .. && ctest -R test_theme_service --output-on-failure`
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc) 2>&1 | tail -5 && ctest -R test_theme_service --output-on-failure</automated>
  </verify>
  <done>ThemeService has 16 base + 5 derived Q_PROPERTYs with AA token names. Old property names removed. All theme tests pass including derived color verification.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 4 bundled theme YAMLs to new token keys</name>
  <files>config/themes/default/theme.yaml, config/themes/amoled/theme.yaml, config/themes/ocean/theme.yaml, config/themes/ember/theme.yaml</files>
  <action>
Migrate all 4 bundled theme YAML files from old key names to new AA wire token names. Each theme needs all 16 keys in both day and night sections.

Key mapping for YAML (old -> new):
- background -> background (same)
- highlight -> primary
- control_background -> surface_variant
- control_foreground -> on_surface
- normal_font -> text_primary
- special_font -> (remove, collapsed into primary)
- description_font -> text_secondary
- bar_background -> surface_container_low
- control_box_background -> surface
- gauge_indicator -> (remove, collapsed into primary)
- icon -> (remove, collapsed into on_surface)
- side_widget_background -> (remove, collapsed into surface)
- bar_shadow -> (remove, now derived)
- divider -> outline_variant
- pressed -> (remove, now derived)
- highlight_font -> inverse_surface

New keys to ADD (not in old themes):
- inverse_on_surface: text color for inverse_surface backgrounds (typically white or near-white)
- outline: visible border/outline color (use a mid-gray appropriate to theme)
- red: error/danger color (use existing hardcoded reds like "#cc4444" or "#F44336")
- on_red: text on red backgrounds (typically "#ffffff")
- yellow: warning color (use "#FF9800" or similar)
- on_yellow: text on yellow backgrounds (typically "#000000")

Update version to "2.0.0" in each theme file.

For each theme, preserve the existing color aesthetic while mapping to new keys. Read each theme file first to understand its color palette before migrating.

After migration, verify the app builds: `cd build && cmake .. && make -j$(nproc)`
Then run full test suite: `cd build && ctest --output-on-failure`
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure</automated>
  </verify>
  <done>All 4 bundled themes have 16 AA token keys in both day/night sections. Version updated to 2.0.0. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `cd build && ctest --output-on-failure` -- all tests pass
- `grep -c 'Q_PROPERTY.*QColor' src/core/services/ThemeService.hpp` returns 21 (16 base + 5 derived)
- `grep 'highlight\|control_background\|normal_font\|special_font\|gauge_indicator' config/themes/default/theme.yaml | wc -l` returns 0 (old keys gone)
- `grep 'primary:\|on_surface:\|text_primary:' config/themes/default/theme.yaml | wc -l` returns at least 3 (new keys present)
</verification>

<success_criteria>
- ThemeService has exactly 16 base AA token Q_PROPERTYs + 5 derived color Q_PROPERTYs
- All 4 bundled themes have all 16 keys in both day and night sections
- Test theme data migrated and all tests pass
- No references to old property names in ThemeService.hpp/cpp
</success_criteria>

<output>
After completion, create `.planning/phases/03-theme-color-system/03-01-SUMMARY.md`
</output>
