---
phase: 03-theme-color-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - qml/applications/phone/PhoneView.qml
  - qml/applications/phone/IncomingCallOverlay.qml
  - qml/applications/settings/CompanionSettings.qml
  - qml/applications/settings/ConnectionSettings.qml
  - qml/applications/settings/ConnectivitySettings.qml
  - qml/applications/settings/EqSettings.qml
  - qml/components/ExitDialog.qml
  - qml/components/GestureOverlay.qml
  - qml/components/NotificationArea.qml
  - qml/components/PairingDialog.qml
  - qml/controls/MaterialIcon.qml
  - qml/applications/bt_audio/BtAudioView.qml
  - qml/applications/home/HomeMenu.qml
  - qml/applications/settings/AASettings.qml
  - qml/applications/settings/AboutSettings.qml
  - qml/applications/settings/DisplaySettings.qml
  - qml/applications/settings/SystemSettings.qml
  - qml/applications/settings/VideoSettings.qml
  - qml/components/NavbarControl.qml
  - qml/components/NavbarPopup.qml
  - qml/components/Navbar.qml
  - qml/components/Wallpaper.qml
  - qml/components/FirstRunBanner.qml
  - qml/controls/EqBandSlider.qml
  - qml/controls/FullScreenPicker.qml
  - qml/controls/InfoBanner.qml
  - qml/controls/NormalText.qml
  - qml/controls/ReadOnlyField.qml
  - qml/controls/SectionHeader.qml
  - qml/controls/SegmentedButton.qml
  - qml/controls/SettingsListItem.qml
  - qml/controls/SettingsSlider.qml
  - qml/controls/SettingsToggle.qml
  - qml/controls/SpecialText.qml
  - qml/controls/Tile.qml
  - qml/main.qml
  - web-config/templates/themes.html
  - src/core/services/IpcServer.cpp
autonomous: true
requirements: [THM-03]

must_haves:
  truths:
    - "No hardcoded hex color values remain in QML files (except debug overlays in AndroidAutoMenu.qml)"
    - "All QML files reference ThemeService with new AA token property names"
    - "Overlays use ThemeService.scrim instead of hardcoded semi-transparent black"
    - "Status indicators use ThemeService.success/red/yellow instead of hardcoded greens/reds/oranges"
    - "Web config panel theme editor uses new AA token key names"
    - "IPC get_theme/set_theme uses new key names"
  artifacts:
    - path: "qml/components/GestureOverlay.qml"
      provides: "Fully themed overlay (was 17 hardcodes)"
      contains: "ThemeService.scrim"
    - path: "qml/applications/phone/PhoneView.qml"
      provides: "Phone view with themed status colors"
      contains: "ThemeService.success"
    - path: "web-config/templates/themes.html"
      provides: "Theme editor with AA token key names"
      contains: "primary"
  key_links:
    - from: "qml/**/*.qml"
      to: "src/core/services/ThemeService.hpp"
      via: "Q_PROPERTY binding"
      pattern: "ThemeService\\.(primary|onSurface|surface|scrim|success|red)"
    - from: "web-config/templates/themes.html"
      to: "src/core/services/IpcServer.cpp"
      via: "IPC JSON key names"
      pattern: "colorKeys.*primary"
---

<objective>
Eliminate all hardcoded hex colors from QML files and update all ThemeService property references to use new AA token names. Update web config panel and IPC to match.

Purpose: Completes THM-03 -- every color in the UI is now drawn from the semantic theme system. Themes become truly global.
Output: Zero hardcoded hex colors in QML (except debug), all 34 QML files using new property names, web config and IPC updated.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-theme-color-system/03-01-SUMMARY.md
@.planning/phases/03-theme-color-system/03-RESEARCH.md

<interfaces>
<!-- New ThemeService properties (from Plan 01) -->
Base properties: primary, onSurface, surface, surfaceVariant, surfaceContainerLow,
  inverseSurface, inverseOnSurface, outline, outlineVariant, background,
  textPrimary, textSecondary, red, onRed, yellow, onYellow

Derived properties: scrim, pressed, barShadow, success, onSuccess

<!-- QML property rename mapping for find-replace -->
ThemeService.backgroundColor -> ThemeService.background
ThemeService.highlightColor -> ThemeService.primary
ThemeService.controlBackgroundColor -> ThemeService.surfaceVariant
ThemeService.controlForegroundColor -> ThemeService.onSurface
ThemeService.normalFontColor -> ThemeService.textPrimary
ThemeService.specialFontColor -> ThemeService.primary
ThemeService.descriptionFontColor -> ThemeService.textSecondary
ThemeService.barBackgroundColor -> ThemeService.surfaceContainerLow
ThemeService.controlBoxBackgroundColor -> ThemeService.surface
ThemeService.gaugeIndicatorColor -> ThemeService.primary
ThemeService.iconColor -> ThemeService.onSurface
ThemeService.sideWidgetBackgroundColor -> ThemeService.surface
ThemeService.barShadowColor -> ThemeService.barShadow
ThemeService.dividerColor -> ThemeService.outlineVariant
ThemeService.pressedColor -> ThemeService.pressed
ThemeService.highlightFontColor -> ThemeService.inverseSurface

<!-- Hardcoded color replacement map (from RESEARCH.md inventory) -->
Scrim/overlay: "#CC000000", "#AA000000" -> ThemeService.scrim
Panel bg: "#DD1a1a2e", "#DD2a2a3e", "#0f3460" -> ThemeService.surfaceContainerLow or ThemeService.surface
Text: "#e0e0e0" -> ThemeService.textPrimary, "#a0a0c0"/"#808080"/"#AAAAAA" -> ThemeService.textSecondary
Success green: "#4CAF50", "#388E3C", "#44aa44" -> ThemeService.success
Error red: "#F44336", "#D32F2F", "#cc4444", "#d32f2f" -> ThemeService.red
Warning orange: "#FF9800" -> ThemeService.yellow
Disabled: "#666666" -> ThemeService.textSecondary
Pressed pairs: use ThemeService.primary (pressed) vs ThemeService.surfaceContainerLow (normal)
Debug colors in AndroidAutoMenu.qml: LEAVE AS-IS (intentional debug overlay)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all ThemeService property references in QML files</name>
  <files>All 34 QML files listed in files_modified that reference ThemeService</files>
  <action>
Systematic find-and-replace across ALL 34 QML files that reference ThemeService properties. Use the exact mapping:

```
ThemeService.backgroundColor -> ThemeService.background
ThemeService.highlightColor -> ThemeService.primary
ThemeService.controlBackgroundColor -> ThemeService.surfaceVariant
ThemeService.controlForegroundColor -> ThemeService.onSurface
ThemeService.normalFontColor -> ThemeService.textPrimary
ThemeService.specialFontColor -> ThemeService.primary
ThemeService.descriptionFontColor -> ThemeService.textSecondary
ThemeService.barBackgroundColor -> ThemeService.surfaceContainerLow
ThemeService.controlBoxBackgroundColor -> ThemeService.surface
ThemeService.gaugeIndicatorColor -> ThemeService.primary
ThemeService.iconColor -> ThemeService.onSurface
ThemeService.sideWidgetBackgroundColor -> ThemeService.surface
ThemeService.barShadowColor -> ThemeService.barShadow
ThemeService.dividerColor -> ThemeService.outlineVariant
ThemeService.pressedColor -> ThemeService.pressed
ThemeService.highlightFontColor -> ThemeService.inverseSurface
```

This is a mechanical rename -- do NOT change any hardcoded hex values in this task (that's Task 2). Only rename ThemeService.oldName references to ThemeService.newName.

After replacement, verify zero old property names remain:
`grep -rn 'ThemeService\.\(backgroundColor\|highlightColor\|controlBackgroundColor\|controlForegroundColor\|normalFontColor\|specialFontColor\|descriptionFontColor\|barBackgroundColor\|controlBoxBackgroundColor\|gaugeIndicatorColor\|iconColor\|sideWidgetBackgroundColor\|barShadowColor\|dividerColor\|pressedColor\|highlightFontColor\)' qml/`

Build to verify no QML binding errors: `cd build && cmake .. && make -j$(nproc)`
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -rn 'ThemeService\.\(backgroundColor\|highlightColor\|controlBackgroundColor\|controlForegroundColor\|normalFontColor\|specialFontColor\|descriptionFontColor\|barBackgroundColor\|controlBoxBackgroundColor\|gaugeIndicatorColor\|iconColor\|sideWidgetBackgroundColor\|barShadowColor\|dividerColor\|pressedColor\|highlightFontColor\)' qml/ | wc -l && echo "should be 0" && cd build && cmake .. && make -j$(nproc) 2>&1 | tail -5</automated>
  </verify>
  <done>All 254+ ThemeService references across 34 QML files use new AA token property names. Zero old property names remain in QML. App builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate hardcoded hex colors from QML + update IPC/web config</name>
  <files>qml/components/GestureOverlay.qml, qml/components/NotificationArea.qml, qml/components/PairingDialog.qml, qml/components/ExitDialog.qml, qml/applications/phone/PhoneView.qml, qml/applications/phone/IncomingCallOverlay.qml, qml/applications/settings/CompanionSettings.qml, qml/applications/settings/ConnectionSettings.qml, qml/applications/settings/ConnectivitySettings.qml, qml/applications/settings/EqSettings.qml, qml/controls/MaterialIcon.qml, web-config/templates/themes.html, src/core/services/IpcServer.cpp</files>
  <action>
Replace all hardcoded hex color values in QML files with ThemeService property references. Use the categorized inventory from RESEARCH.md:

**Scrim/overlay backgrounds** (4 files):
- `"#CC000000"` in PairingDialog, IncomingCallOverlay, CompanionSettings -> `ThemeService.scrim`
- `"#AA000000"` in GestureOverlay -> `ThemeService.scrim`

**Panel/card backgrounds** (2 files):
- `"#DD1a1a2e"`, `"#0f3460"` in GestureOverlay -> use `ThemeService.surfaceContainerLow` (the panel bg colors should now come from theme). For the alpha variants, use Qt.rgba() with ThemeService color: `Qt.rgba(ThemeService.surfaceContainerLow.r, ThemeService.surfaceContainerLow.g, ThemeService.surfaceContainerLow.b, 0.87)`
- `"#DD2a2a3e"`, `"#0f3460"` in NotificationArea -> same treatment

**Text colors** (3 files):
- `"#e0e0e0"` (10x in GestureOverlay, 2x in NotificationArea, 1x IncomingCallOverlay) -> `ThemeService.textPrimary`
- `"#a0a0c0"`, `"#808080"`, `"#AAAAAA"`, `"#CCCCCC"` -> `ThemeService.textSecondary`

**Success green** (4 files):
- `"#4CAF50"`, `"#388E3C"` in PhoneView -> `ThemeService.success` (for pressed variant, use `Qt.darker(ThemeService.success, 1.2)`)
- `"#44aa44"` in ConnectionSettings, ConnectivitySettings -> `ThemeService.success`
- `"#4CAF50"` in CompanionSettings -> `ThemeService.success`

**Error/danger red** (7 files):
- `"#F44336"`, `"#D32F2F"` in PhoneView, IncomingCallOverlay, ExitDialog, CompanionSettings -> `ThemeService.red`
- `"#cc4444"` in PairingDialog, ConnectionSettings, ConnectivitySettings -> `ThemeService.red`
- `"#d32f2f"` in EqSettings -> `ThemeService.red`
- For pressed red variants like "#D32F2F" paired with "#F44336", use `Qt.darker(ThemeService.red, 1.15)`

**Warning/status** (1 file):
- `"#FF9800"` in CompanionSettings -> `ThemeService.yellow`

**Pressed state ternaries** (2 files):
- GestureOverlay: `"#e94560"` (pressed) vs `"#0f3460"` (normal) -> `ThemeService.primary` vs `ThemeService.surfaceContainerLow`
- PhoneView/IncomingCallOverlay: pressed pairs -> `Qt.darker(ThemeService.success/red, 1.15)` vs `ThemeService.success/red`

**Disabled text** (1 file):
- `"#666666"` in PhoneView -> `ThemeService.textSecondary`

**Debug colors** -- LEAVE AS-IS:
- AndroidAutoMenu.qml: `"#00FF00"` (touch crosshair), `"#666666"`, `"#FFA500"`, `"#FFFF00"` (signal quality). These are intentional high-contrast debug visuals.

**MaterialIcon.qml**: Check what hex color it has and replace appropriately.

**Web config panel** (`web-config/templates/themes.html`):
- Update the `colorKeys` array to use the 16 new AA token names: `['primary', 'on_surface', 'surface', 'surface_variant', 'surface_container_low', 'inverse_surface', 'inverse_on_surface', 'outline', 'outline_variant', 'background', 'text_primary', 'text_secondary', 'red', 'on_red', 'yellow', 'on_yellow']`
- Leave base.html CSS as-is per RESEARCH.md recommendation.

**IPC** (`src/core/services/IpcServer.cpp`):
- `handleGetTheme()` reads `dayColors()` and `nightColors()` maps directly -- these already use the new YAML keys since ThemeService loads from YAML. Verify the exported key names are correct. No changes should be needed unless it hardcodes key names.
- `handleSetTheme()` -- same, verify it writes correct keys.

After all replacements, verify:
`grep -rn '#[0-9a-fA-F]\{6,8\}"' qml/ --include='*.qml' | grep -v AndroidAutoMenu | grep -v '// debug' | wc -l`
Should be 0 (or very close -- only debug colors remain).
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -rn '"#[0-9a-fA-F]\{6,8\}"' qml/ --include='*.qml' | grep -v AndroidAutoMenu | grep -v '// debug' | wc -l && echo "should be 0" && cd build && cmake .. && make -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Zero hardcoded hex colors in QML (except AndroidAutoMenu debug). Web config colorKeys updated to 16 AA tokens. IPC verified. Full build and test suite passes.</done>
</task>

</tasks>

<verification>
- `grep -rn '"#[0-9a-fA-F]\{6,8\}"' qml/ --include='*.qml' | grep -v AndroidAutoMenu | grep -v '// debug' | wc -l` returns 0
- `grep -rn 'ThemeService\.\(backgroundColor\|highlightColor\|normalFontColor\)' qml/ | wc -l` returns 0
- `cd build && ctest --output-on-failure` -- all tests pass
- Web config themes.html contains 'primary' in colorKeys array
</verification>

<success_criteria>
- No hardcoded hex color values in any QML file except AndroidAutoMenu.qml debug overlay
- All 34 QML files use new ThemeService property names
- Web config panel and IPC use new AA token key names
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-theme-color-system/03-02-SUMMARY.md`
</output>
