---
phase: 03.5-navbar-status-indicators
plan: 02
type: execute
wave: 2
depends_on: ["03.5-01"]
files_modified:
  - qml/components/Navbar.qml
autonomous: true
requirements:
  - NAV-05

must_haves:
  truths:
    - "Outer controls (volume/brightness) use 20% width, center clock uses 60%"
    - "Zone proportions apply to both horizontal and vertical navbar orientations"
  artifacts:
    - path: "qml/components/Navbar.qml"
      provides: "Updated zone proportions (20/60/20)"
      contains: "0.20"
---

<objective>
Update navbar zone proportions from 25/50/25 to 20/60/20, giving the clock more room and pushing volume/brightness controls closer to the screen edges.

Purpose: With the phone's clock hidden via SDP (Plan 01), the navbar clock is the only clock visible during AA. Wider center zone improves glanceability. Battery/signal indicators were planned here but the phone doesn't send that data — so this plan is just the proportion update.
Output: Updated Navbar.qml with new zone widths/heights.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@qml/components/Navbar.qml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navbar zone proportions to 20/60/20</name>
  <files>qml/components/Navbar.qml</files>
  <action>
    Update zone proportions in both horizontal and vertical layouts:

    **Horizontal layout (Row):**
    - hControl0: `width: parent.width * 0.20` (was `* 0.25`)
    - hControl1: `width: parent.width * 0.60` (was `* 0.50`)
    - hControl2: `width: parent.width * 0.20` (was `* 0.25`)

    **Vertical layout (Column):**
    - vControl0: `height: parent.height * 0.20` (was `* 0.25`)
    - vControl1: `height: parent.height * 0.60` (was `* 0.50`)
    - vControl2: `height: parent.height * 0.20` (was `* 0.25`)
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure -j$(nproc)</automated>
  </verify>
  <done>
    - All 6 zone proportions updated (3 horizontal, 3 vertical)
    - Compiles and all tests pass
  </done>
</task>

</tasks>

<verification>
- Build compiles on VM
- All tests pass
- Zone proportions are 20/60/20 in both orientations
</verification>

<success_criteria>
- Navbar zone proportions updated to 20/60/20
- Both horizontal and vertical layouts updated
- No regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-navbar-status-indicators/03.5-02-SUMMARY.md`
</output>
