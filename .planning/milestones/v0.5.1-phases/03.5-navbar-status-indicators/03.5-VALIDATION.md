---
phase: 03.5
slug: navbar-status-indicators
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-09
---

# Phase 03.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | CTest + Google Test |
| **Config file** | tests/CMakeLists.txt |
| **Quick run command** | `cd build && ctest --output-on-failure -R "test_name" -j$(nproc)` |
| **Full suite command** | `cd build && ctest --output-on-failure -j$(nproc)` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd build && ctest --output-on-failure -R "test_name" -j$(nproc)`
- **After every plan wave:** Run `cd build && ctest --output-on-failure -j$(nproc)`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Behavior | Test Type | Automated Command | File Exists? | Status |
|----------|-----------|-------------------|-------------|--------|
| Battery property updates from protocol data | unit | `ctest -R test_aa_orchestrator` | Exists (extend) | ⬜ pending |
| Signal property updates from protocol data | unit | `ctest -R test_aa_orchestrator` | Exists (extend) | ⬜ pending |
| Properties reset on disconnect | unit | `ctest -R test_aa_orchestrator` | Exists (extend) | ⬜ pending |
| hidden_ui_elements in SDP when navbar visible | unit | `ctest -R test_configuration` | Exists (extend) | ⬜ pending |
| hidden_ui_elements absent when navbar hidden | unit | `ctest -R test_configuration` | Exists (extend) | ⬜ pending |
| Battery icon threshold mapping | unit | `ctest -R test_indicator_mapping` | Wave 0 | ⬜ pending |
| Signal icon mapping | unit | `ctest -R test_indicator_mapping` | Wave 0 | ⬜ pending |
| Navbar zone proportions (15/70/15) | manual | Visual on Pi | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_indicator_mapping.cpp` — battery level-to-icon and signal strength-to-icon mapping tests
- [ ] Extend `tests/test_aa_orchestrator.cpp` — battery/signal property lifecycle (update on data, reset on disconnect)

*Existing test infrastructure covers SDP/config verification.*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Navbar zone proportions (15/70/15) | Visual layout on physical display | Connect Pi, verify volume/brightness icons at edges, indicators flanking clock in center |
| Icons render correctly | Font glyph availability | Visual check that battery/signal Material Icons display properly |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
