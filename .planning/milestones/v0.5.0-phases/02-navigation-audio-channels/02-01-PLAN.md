---
phase: 02-navigation-audio-channels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/open-androidauto/include/oaa/Channel/MessageIds.hpp
  - libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp
  - libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp
  - libs/open-androidauto/src/Messenger/ProtocolLogger.cpp
  - src/core/aa/AndroidAutoOrchestrator.cpp
  - tests/test_navigation_channel_handler.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "NavigationTurnEvent (0x8004) is parsed and all fields (road_name, maneuver_type, turn_direction, turn_icon, distance_meters, distance_unit) are emitted via Qt signal"
    - "NavigationNotification (0x8006) parsing extracts all steps (multi-step lookahead), lane guidance, and destination/ETA data"
    - "NavigationFocusIndication is handled when received -- parsed, state tracked, focus_data logged"
    - "Orchestrator connects nav handler signals and logs parsed data at debug level"
  artifacts:
    - path: "libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp"
      provides: "New signals and handler declarations for turn event, enhanced notification, focus indication"
      contains: "navigationTurnEvent"
    - path: "libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp"
      provides: "Switch cases and proto parsing for 0x8004, enhanced 0x8006, focus indication"
      contains: "handleTurnEvent"
    - path: "libs/open-androidauto/include/oaa/Channel/MessageIds.hpp"
      provides: "NAV_TURN_EVENT constant"
      contains: "NAV_TURN_EVENT"
    - path: "tests/test_navigation_channel_handler.cpp"
      provides: "Unit tests for turn event parsing, notification enhancement, focus indication"
      contains: "TestNavigationChannelHandler"
  key_links:
    - from: "NavigationChannelHandler.cpp"
      to: "NavigationTurnEventMessage.pb.h"
      via: "ParseFromArray in handleTurnEvent"
      pattern: "NavigationTurnEvent.*ParseFromArray"
    - from: "AndroidAutoOrchestrator.cpp"
      to: "NavigationChannelHandler signals"
      via: "connect() in onNewConnection"
      pattern: "connect.*navHandler_.*navigationTurnEvent"
---

<objective>
Parse inbound NavigationTurnEvent (0x8004), enhance NavigationNotification (0x8006) with lane guidance and multi-step data, and handle NavigationFocusIndication for focus negotiation completion.

Purpose: NAV-01 (turn event parsing) and NAV-02 (focus indication handling) are the primary navigation protocol compliance gaps. This makes navigation guidance data available via Qt signals for future turn-card UI.
Output: Extended NavigationChannelHandler with new switch cases, signals, and state tracking. Unit tests. Orchestrator wiring for debug logging.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-audio-channels/02-CONTEXT.md
@.planning/phases/02-navigation-audio-channels/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From libs/open-androidauto/include/oaa/Channel/MessageIds.hpp:
```cpp
namespace NavigationMessageId {
    constexpr uint16_t NAV_STATE    = 0x8003;
    constexpr uint16_t NAV_STEP     = 0x8006;
    constexpr uint16_t NAV_DISTANCE = 0x8007;
    // NAV_TURN_EVENT = 0x8004 needs adding
}
```

From libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp:
```cpp
class NavigationChannelHandler : public oaa::IChannelHandler {
    Q_OBJECT
public:
    explicit NavigationChannelHandler(QObject* parent = nullptr);
    uint8_t channelId() const override { return oaa::ChannelId::Navigation; }
    void onChannelOpened() override;
    void onChannelClosed() override;
    void onMessage(uint16_t messageId, const QByteArray& payload, int dataOffset = 0) override;
signals:
    void navigationStateChanged(bool active);
    void navigationStepChanged(const QString& instruction, const QString& destination, int maneuverType);
    void navigationDistanceChanged(const QString& distance, int unit);
private:
    void handleNavState(const QByteArray& payload);
    void handleNavStep(const QByteArray& payload);
    void handleNavDistance(const QByteArray& payload);
    bool navActive_ = false;
};
```

From libs/open-androidauto/include/oaa/Channel/IChannelHandler.hpp (base class):
```cpp
signals:
    void sendRequested(uint8_t channelId, uint16_t messageId, const QByteArray& data);
    void unknownMessage(uint16_t messageId, const QByteArray& data);
```

Proto definitions:
- NavigationTurnEvent: road_name(string), maneuver_type(ManeuverType.Enum), turn_direction(TurnSide.Enum), turn_icon(bytes), distance_meters(int32), distance_unit(int32)
- NavigationFocusIndication: focus_data(bytes, required)
- NavigationNotification: repeated NavigationStep (with lanes, road_info), repeated NavigationDestination (with address, charging_station)
- NavigationStep: maneuver, instruction, repeated NavigationLane (with repeated NavigationLaneDirection: shape + is_recommended), road_info (repeated road_names)

ProtocolLogger.cpp line 310: `case 0x8004: return "NAVIGATION_TURN_EVENT";` (already has nav 0x8004 named)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NavigationChannelHandler extension -- turn event, enhanced notification, focus indication</name>
  <files>
    libs/open-androidauto/include/oaa/Channel/MessageIds.hpp,
    libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp,
    libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp,
    libs/open-androidauto/src/Messenger/ProtocolLogger.cpp,
    tests/test_navigation_channel_handler.cpp,
    tests/CMakeLists.txt
  </files>
  <behavior>
    - Test: Sending serialized NavigationTurnEvent with road_name="Main St", maneuver_type=1, turn_direction=1, turn_icon="\x89PNG", distance_meters=200, distance_unit=1 to onMessage(0x8004) emits navigationTurnEvent signal with all matching fields
    - Test: Sending NavigationTurnEvent with only road_name set emits signal with empty/default values for missing fields
    - Test: Sending invalid payload to 0x8004 does NOT emit signal (parse failure handled gracefully)
    - Test: Sending NavigationNotification with 2 steps (each with lanes and road_info) and 1 destination emits navigationNotificationReceived with step count, lane data, and destination
    - Test: Sending serialized NavigationFocusIndication with focus_data bytes emits navigationFocusChanged signal
    - Test: After receiving focus indication, navFocusActive_ state is updated
  </behavior>
  <action>
    **MessageIds.hpp:** Add `NAV_TURN_EVENT = 0x8004` and `NAV_FOCUS_INDICATION = 0x8005` (try 0x8005 as a candidate -- the message ID is unknown per research, but 0x8005 is the gap between NAV_STATE=0x8003 and NAV_STEP=0x8006; if no messages arrive on 0x8005, handle NavigationFocusIndication in the default case with a proto parse attempt as fallback). Add a comment noting the ID is provisional.

    **NavigationChannelHandler.hpp:** Add new signals:
    - `navigationTurnEvent(const QString& roadName, int maneuverType, int turnDirection, const QByteArray& turnIcon, int distanceMeters, int distanceUnit)` -- per user decision, turn icon PNG as QByteArray
    - `navigationNotificationReceived(int stepCount, int laneCount, const QString& destination, const QString& eta)` -- enhanced notification signal
    - `navigationFocusChanged(bool hasFocus)` -- focus state change

    Add private handler methods: `handleTurnEvent()`, `handleFocusIndication()`. Enhance existing `handleNavStep()`.
    Add state member: `bool navFocusActive_ = false;`

    **NavigationChannelHandler.cpp:**
    - Add switch case for `NAV_TURN_EVENT` (0x8004) calling `handleTurnEvent()`
    - `handleTurnEvent()`: Parse NavigationTurnEvent proto, extract all 6 fields (with has_xxx guards for optional fields, defaults: empty string, 0, 0, empty QByteArray, -1, 0), log at qInfo with [NavChannel] prefix, emit signal
    - Enhance `handleNavStep()`: Iterate ALL steps (not just steps(0)), extract lane guidance (NavigationLaneDirection shape + is_recommended) per step, extract destination details from destinations list including ETA. Log lane count and step count. Emit enhanced notification signal.
    - Add switch case for NAV_FOCUS_INDICATION: Parse NavigationFocusIndication proto, log focus_data hex (capped at 64 bytes), update navFocusActive_, emit navigationFocusChanged. If the message ID is unknown, add a comment in the default case attempting to parse any unrecognized message as NavigationFocusIndication (try parse, if succeeds handle it, if fails fall through to unknown logging).

    **ProtocolLogger.cpp:** ProtocolLogger already has 0x8004 named "NAVIGATION_TURN_EVENT" at line 310. Add entry for NAV_FOCUS_INDICATION ID if using 0x8005.

    **Tests:** Create `tests/test_navigation_channel_handler.cpp` with QTest + QSignalSpy. Register in tests/CMakeLists.txt with `oap_add_test(test_navigation_channel SOURCES test_navigation_channel_handler.cpp)`.

    Follow established handler pattern: `QByteArray::fromRawData()` for zero-copy, `ShortDebugString()` for proto debug, `ParseFromArray()` for deserialization.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. -DBUILD_TESTING=ON && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest -R test_navigation_channel --output-on-failure</automated>
  </verify>
  <done>NavigationTurnEvent (0x8004) parsed with all fields emitted via signal. NavigationNotification enhanced with multi-step, lanes, destinations. NavigationFocusIndication handled (best-effort on unknown message ID). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator wiring for navigation signals</name>
  <files>src/core/aa/AndroidAutoOrchestrator.cpp</files>
  <action>
    In `AndroidAutoOrchestrator::onNewConnection()`, after the existing `navHandler_` signal connections (around line 388-404), add connections for the new signals:

    1. `connect(&navHandler_, &NavigationChannelHandler::navigationTurnEvent, ...)` -- lambda that logs via `qCDebug(lcAA)` with format: `[Nav] turn: {roadName} maneuver:{type} dir:{dir} dist:{meters} unit:{unit} icon:{bytes}bytes`
    2. `connect(&navHandler_, &NavigationChannelHandler::navigationNotificationReceived, ...)` -- lambda that logs: `[Nav] notification: {stepCount} steps, {laneCount} lanes, dest:{destination}`
    3. `connect(&navHandler_, &NavigationChannelHandler::navigationFocusChanged, ...)` -- lambda that logs: `[Nav] focus: {hasFocus}`

    In `disconnectHandlerSignals()` (around line 607), navHandler_ disconnect is already handled by `navHandler_.disconnect(this)` -- no change needed there.

    Per user decision: Qt signals only, no IEventBus events or Q_PROPERTYs. Orchestrator connects and logs, no app-layer parsing.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -3 && ctest --output-on-failure</automated>
  </verify>
  <done>Orchestrator connects all three new nav handler signals. Full test suite passes. No regressions.</done>
</task>

</tasks>

<verification>
- `cd build && ctest -R test_navigation_channel --output-on-failure` -- new nav handler tests pass
- `cd build && ctest --output-on-failure` -- full suite green (no regressions)
- grep confirms: `NavigationTurnEvent` parsed in handler, signal connected in orchestrator
</verification>

<success_criteria>
- NavigationTurnEvent (0x8004) parsed with all 6 fields and emitted via Qt signal
- NavigationNotification enhanced to extract all steps, lane guidance, and destination data
- NavigationFocusIndication handled (best-effort given unknown message ID)
- Orchestrator logs all navigation data at debug level
- New unit tests cover turn event parsing (valid, partial, invalid payloads)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-audio-channels/02-01-SUMMARY.md`
</output>
