---
phase: 01-proto-foundation-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/open-androidauto/proto
  - libs/open-androidauto/src/HU/Handlers/*.cpp
autonomous: true
requirements: [SUB-01]

must_haves:
  truths:
    - "Project compiles cleanly with v1.0 proto submodule on both Qt 6.4 (claude-dev) and Qt 6.8 (Pi)"
    - "All 47 existing tests pass without modification or with minimal fixes"
    - "No existing AA functionality regresses (video, touch, audio all still work)"
  artifacts:
    - path: "libs/open-androidauto/proto"
      provides: "v1.0 proto definitions submodule pointer"
      contains: "1cd8919"
  key_links:
    - from: "libs/open-androidauto/CMakeLists.txt"
      to: "libs/open-androidauto/proto/oaa/*.proto"
      via: "GLOB_RECURSE proto compilation"
      pattern: "file.*GLOB_RECURSE.*PROTO_FILES"
---

<objective>
Update the open-android-auto proto submodule from bdbb3f3 to v1.0 tag (1cd8919) and fix any compilation breaks.

Purpose: Align Prodigy with verified proto definitions so that later phases can reference correct message types and field names.
Output: Clean build against v1.0 protos, all tests passing.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-proto-foundation-observability/01-CONTEXT.md

@libs/open-androidauto/CMakeLists.txt
@src/core/aa/AndroidAutoOrchestrator.cpp
@src/core/aa/AndroidAutoOrchestrator.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review v1.0 proto delta and update submodule</name>
  <files>libs/open-androidauto/proto</files>
  <action>
1. Review the proto diff between current (bdbb3f3) and v1.0 (1cd8919) per user decision — understand what changed before fixing anything:
   ```bash
   cd libs/open-androidauto/proto
   git diff --stat bdbb3f3..v1.0 -- '*.proto'
   git diff bdbb3f3..v1.0 -- '*.proto' | head -500  # scan for renames, removed fields, changed enums
   ```

2. Update the proto submodule pointer:
   ```bash
   cd libs/open-androidauto/proto
   git checkout v1.0
   cd ../../..
   git add libs/open-androidauto/proto
   ```

3. The v1.0 delta is ~223 proto files changed (mostly docs, validation tests, schema corrections). CMake auto-globs all `*.proto` files, so new files compile automatically. The risk is renamed fields, changed enum values, or restructured messages breaking existing C++ handler code in `libs/open-androidauto/src/HU/Handlers/`.

4. Do NOT modify files inside the proto submodule — it is read-only per project policy.
  </action>
  <verify>
    <automated>cd libs/open-androidauto/proto && git rev-parse HEAD | grep -q "1cd8919" && echo "PASS: proto at v1.0" || echo "FAIL: proto not at v1.0"</automated>
  </verify>
  <done>Proto submodule pointer updated to v1.0 tag (1cd8919)</done>
</task>

<task type="auto">
  <name>Task 2: Fix compilation breaks and verify tests pass</name>
  <files>libs/open-androidauto/src/HU/Handlers/*.cpp, src/core/aa/*.cpp</files>
  <action>
1. Attempt a clean build:
   ```bash
   rm -rf build && mkdir build && cd build && cmake .. && make -j$(nproc)
   ```

2. If compilation fails due to proto renames or new fields:
   - Fix field/enum name references in the open-androidauto handler C++ code (these are NOT in the read-only proto submodule — they are in `libs/open-androidauto/src/`)
   - Fix any references in `src/core/aa/` Prodigy code that uses proto types directly
   - The v1.0 changes are described as "additive, no breaking changes" but schema corrections may have renamed fields for accuracy

3. Run all tests:
   ```bash
   cd build && ctest --output-on-failure
   ```

4. Fix any test failures caused by proto changes (field renames in test assertions, new required fields, etc.)

5. If any handler in `libs/open-androidauto/src/HU/Handlers/` references a proto field that was renamed in v1.0, update the C++ reference to match the new field name. Keep the handler behavior identical — only change field/enum names to match updated protos.
  </action>
  <verify>
    <automated>cd build && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Project compiles cleanly and all 47 tests pass with v1.0 proto submodule</done>
</task>

</tasks>

<verification>
- `git submodule status libs/open-androidauto/proto` shows v1.0 commit (1cd8919)
- `cd build && cmake --build . -j$(nproc)` succeeds with zero errors
- `cd build && ctest --output-on-failure` shows 47/47 tests pass
</verification>

<success_criteria>
- Proto submodule updated to v1.0 (1cd8919)
- Clean compilation on Qt 6.4 (claude-dev VM)
- All 47 existing tests pass
- No changes to proto files themselves (submodule is read-only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-proto-foundation-observability/01-01-SUMMARY.md`
</output>
