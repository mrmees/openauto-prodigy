---
phase: 04-oaa-proto-compliance-library-rename
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .gitmodules
  - CMakeLists.txt
  - src/CMakeLists.txt
  - libs/prodigy-oaa-protocol/CMakeLists.txt
  - libs/prodigy-oaa-protocol/tests/CMakeLists.txt
  - CLAUDE.md
  - README.md
  - docs/development.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REN-01, TST-01]

must_haves:
  truths:
    - "Library directory is libs/prodigy-oaa-protocol/ (not libs/open-androidauto/)"
    - "CMake target name is prodigy-oaa-protocol throughout build system"
    - "C++ namespace remains oaa:: (unchanged)"
    - "Proto submodule is at libs/prodigy-oaa-protocol/proto/ and git tracks it correctly"
    - "All 64 tests pass after rename (100% pass rate)"
    - "ROADMAP.md and REQUIREMENTS.md reflect v1.2 proto reality (retracted features annotated)"
  artifacts:
    - path: "libs/prodigy-oaa-protocol/CMakeLists.txt"
      provides: "Renamed library build target"
      contains: "project(prodigy-oaa-protocol"
    - path: ".gitmodules"
      provides: "Updated submodule path"
      contains: "libs/prodigy-oaa-protocol/proto"
    - path: "CMakeLists.txt"
      provides: "Top-level library reference"
      contains: "add_subdirectory(libs/prodigy-oaa-protocol)"
    - path: "src/CMakeLists.txt"
      provides: "App links to renamed target"
      contains: "prodigy-oaa-protocol"
  key_links:
    - from: "CMakeLists.txt"
      to: "libs/prodigy-oaa-protocol/CMakeLists.txt"
      via: "add_subdirectory"
      pattern: "add_subdirectory.*prodigy-oaa-protocol"
    - from: "src/CMakeLists.txt"
      to: "prodigy-oaa-protocol target"
      via: "target_link_libraries"
      pattern: "prodigy-oaa-protocol"
    - from: ".gitmodules"
      to: "open-android-auto remote"
      via: "submodule path mapping"
      pattern: "path = libs/prodigy-oaa-protocol/proto"
---

<objective>
Rename the in-tree protocol library from open-androidauto to prodigy-oaa-protocol and update all documentation to reflect v1.2 proto reality.

Purpose: The library has diverged from upstream -- it includes Prodigy-specific handlers, session management, and protocol logging. The rename makes ownership clear. ROADMAP and REQUIREMENTS need updating because several proto messages were retracted in v1.2 that were previously listed as shipped capabilities.

Output: Renamed library, accurate documentation, all tests still green.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-oaa-proto-compliance-library-rename/04-CONTEXT.md
@.planning/phases/04-oaa-proto-compliance-library-rename/04-01-SUMMARY.md
@CMakeLists.txt
@src/CMakeLists.txt
@libs/open-androidauto/CMakeLists.txt
@libs/open-androidauto/tests/CMakeLists.txt
@.gitmodules
@CLAUDE.md
@README.md
@docs/development.md

<interfaces>
From CMakeLists.txt line 29-30:
```cmake
# open-androidauto -- Qt-native AA protocol library
add_subdirectory(libs/open-androidauto)
```

From src/CMakeLists.txt lines 59-60, 74:
```cmake
target_include_directories(openauto-core PUBLIC
    ${CMAKE_SOURCE_DIR}/libs/open-androidauto/include
    ${CMAKE_BINARY_DIR}/libs/open-androidauto
)
target_link_libraries(openauto-core PUBLIC open-androidauto)
```

From libs/open-androidauto/CMakeLists.txt:
```cmake
project(open-androidauto VERSION 0.1.0 LANGUAGES CXX)
add_library(open-androidauto STATIC ...)
```

From libs/open-androidauto/tests/CMakeLists.txt line 5:
```cmake
target_link_libraries(${name} PRIVATE open-androidauto Qt6::Test)
```

From .gitmodules:
```
[submodule "libs/open-androidauto/proto"]
    path = libs/open-androidauto/proto
    url = https://github.com/mrmees/open-android-auto.git
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename library directory, update submodule path, and fix all build references</name>
  <files>
    .gitmodules
    CMakeLists.txt
    src/CMakeLists.txt
    libs/prodigy-oaa-protocol/CMakeLists.txt
    libs/prodigy-oaa-protocol/tests/CMakeLists.txt
  </files>
  <action>
    Rename the protocol library from open-androidauto to prodigy-oaa-protocol. Keep oaa:: C++ namespace unchanged per user decision.

    CRITICAL -- Submodule rename procedure (do this exactly):

    The submodule at libs/open-androidauto/proto must move with the directory. Git submodule moves are tricky -- follow this sequence:

    1. git submodule deinit libs/open-androidauto/proto (detach submodule)
    2. git rm --cached libs/open-androidauto/proto (remove from index, keep on disk)
    3. Edit .gitmodules: change path from libs/open-androidauto/proto to libs/prodigy-oaa-protocol/proto, change submodule section name to match
    4. git mv libs/open-androidauto libs/prodigy-oaa-protocol (rename parent directory -- proto/ moves with it)
    5. rm -rf .git/modules/libs/open-androidauto (clean old module cache)
    6. git submodule add https://github.com/mrmees/open-android-auto.git libs/prodigy-oaa-protocol/proto (re-register at new path)
    7. git submodule status to verify it tracks correctly

    If the above sequence fails at any step, alternative approach:
    1. Save the submodule commit hash: git submodule status | grep open-androidauto
    2. git submodule deinit -f libs/open-androidauto/proto
    3. git rm -f libs/open-androidauto/proto
    4. rm -rf .git/modules/libs/open-androidauto
    5. mv libs/open-androidauto libs/prodigy-oaa-protocol
    6. Update .gitmodules path
    7. git submodule add https://github.com/mrmees/open-android-auto.git libs/prodigy-oaa-protocol/proto
    8. cd libs/prodigy-oaa-protocol/proto and git checkout {saved_hash}

    CMake target rename (after directory move):
    - libs/prodigy-oaa-protocol/CMakeLists.txt: Replace all open-androidauto with prodigy-oaa-protocol in project(), add_library(), target_include_directories(), target_link_libraries(), and option() calls
    - libs/prodigy-oaa-protocol/tests/CMakeLists.txt: Replace open-androidauto with prodigy-oaa-protocol in target_link_libraries
    - CMakeLists.txt (root): Change add_subdirectory(libs/open-androidauto) to add_subdirectory(libs/prodigy-oaa-protocol), update comment
    - src/CMakeLists.txt: Update include dirs from libs/open-androidauto to libs/prodigy-oaa-protocol. Update target_link_libraries from open-androidauto to prodigy-oaa-protocol

    IMPORTANT: After rename, delete build directory CMake cache to avoid stale paths: rm -f build/CMakeCache.txt then reconfigure: cd build and cmake ..

    Do NOT modify files inside libs/prodigy-oaa-protocol/proto/ -- that is the read-only submodule.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && git submodule status && ls libs/prodigy-oaa-protocol/CMakeLists.txt && test ! -d libs/open-androidauto && rm -f build/CMakeCache.txt && cd build && cmake .. 2>&1 | tail -5 && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>Library lives at libs/prodigy-oaa-protocol/. libs/open-androidauto/ does not exist. CMake target is prodigy-oaa-protocol. Submodule tracked at libs/prodigy-oaa-protocol/proto/. Build succeeds. All 64 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation to reflect rename and v1.2 reality</name>
  <files>
    CLAUDE.md
    README.md
    docs/development.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Documentation updates for library rename:
    - CLAUDE.md: Replace libs/open-androidauto/ path references with libs/prodigy-oaa-protocol/ throughout (key files table, repository layout, submodule section, etc.). Keep "open-android-auto" when referring to the upstream git repo name (that has not changed). Update the "Hands-Off" submodule section path.
    - README.md: Update library references from open-androidauto to prodigy-oaa-protocol.
    - docs/development.md: Update build paths and library references.

    Broad sweep: Run grep -rn "open-androidauto" on *.md, *.cmake, *.txt, *.py, *.sh files and fix any remaining references. Exclude .planning-archive/ (historical), proto/ (read-only submodule), and .git/ (internal).

    ROADMAP.md updates for v1.2 reality:
    - Phase 2 description already has the retraction note -- verify it is accurate
    - Phase 3 description already has the retraction note -- verify it is accurate
    - Mark Phase 4 plans in the plan list
    - Ensure success criteria item 5 is satisfiable

    REQUIREMENTS.md updates for v1.2 reality:
    - Add Phase 4 requirements section with CLN-01, CLN-02, REN-01, TST-01 definitions
    - Update retracted requirements to show they were retracted:
      - NAV-02: Retracted in v1.2 -- NavigationFocusIndication was misidentified; nav focus is on Control channel
      - AUD-01: Retracted in v1.2 -- AudioFocusState was misidentified radio channel message
      - AUD-02: Retracted in v1.2 -- AudioStreamType was misidentified radio channel message
      - MED-01: Retracted in v1.2 -- MediaPlaybackCommand proto does not exist; keycode-based approach is v1.2-correct
    - Update traceability table with Phase 4 entries
    - Update coverage counts to reflect retracted items and new Phase 4 requirements
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -rn "open-androidauto" --include="*.md" --include="*.cmake" --include="*.txt" --include="*.py" --include="*.sh" | grep -v ".planning-archive/" | grep -v "proto/" | grep -v ".git/" | grep -v "open-android-auto" | head -5 && echo "---" && grep -c "CLN-01" .planning/REQUIREMENTS.md && grep -c "Retracted" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>No stale open-androidauto references remain in documentation (outside archive and proto submodule). REQUIREMENTS.md has CLN-01, CLN-02, REN-01, TST-01 defined. Retracted requirements (NAV-02, AUD-01, AUD-02, MED-01) are annotated. ROADMAP Phase 4 plans are listed.</done>
</task>

</tasks>

<verification>
- ls libs/prodigy-oaa-protocol/CMakeLists.txt exists
- ls libs/open-androidauto/ does NOT exist
- git submodule status shows libs/prodigy-oaa-protocol/proto
- grep -rn "open-androidauto" CMakeLists.txt src/CMakeLists.txt .gitmodules returns no hits
- ctest --test-dir build --output-on-failure shows 64/64 passed (0 failed)
- ROADMAP.md Phase 4 has goal and plan list
- REQUIREMENTS.md has CLN-01, CLN-02, REN-01, TST-01
- Retracted requirements annotated in REQUIREMENTS.md
</verification>

<success_criteria>
- Library fully renamed with no stale references in build system or documentation
- Git submodule path updated and functional
- 64/64 tests pass (0 failures)
- ROADMAP and REQUIREMENTS accurately reflect v1.2 proto reality
</success_criteria>

<output>
After completion, create `.planning/phases/04-oaa-proto-compliance-library-rename/04-02-SUMMARY.md`
</output>
