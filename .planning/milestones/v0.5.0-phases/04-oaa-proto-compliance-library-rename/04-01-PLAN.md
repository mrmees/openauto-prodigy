---
phase: 04-oaa-proto-compliance-library-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/open-androidauto/src/HU/Handlers/AudioChannelHandler.cpp
  - libs/open-androidauto/include/oaa/HU/Handlers/AudioChannelHandler.hpp
  - libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp
  - libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp
  - libs/open-androidauto/include/oaa/Channel/MessageIds.hpp
  - libs/open-androidauto/src/Messenger/ProtocolLogger.cpp
  - src/core/aa/AndroidAutoOrchestrator.cpp
  - src/core/aa/ServiceDiscoveryBuilder.cpp
  - src/core/aa/ServiceDiscoveryBuilder.hpp
  - tests/test_navbar_controller.cpp
autonomous: true
requirements: [CLN-01, CLN-02, TST-01]

must_haves:
  truths:
    - "No retracted handler stubs, RETRACTED comments, or dead signal declarations remain in source"
    - "MessageIds.hpp has no constants for retracted messages (0x8021, 0x8022 removed)"
    - "ServiceDiscoveryBuilder sends actual BSSID (wlan0 MAC address) in WiFi channel descriptor, not SSID string"
    - "Navbar controller hold progress test passes reliably"
    - "All 64 tests pass after cleanup"
  artifacts:
    - path: "libs/open-androidauto/src/HU/Handlers/AudioChannelHandler.cpp"
      provides: "Audio handler without retracted stubs"
      contains: "no handleAudioFocusState or handleAudioStreamType methods"
    - path: "libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp"
      provides: "Nav handler without retracted 0x8005 case"
      contains: "no 0x8005 case in onMessage switch"
    - path: "src/core/aa/ServiceDiscoveryBuilder.cpp"
      provides: "Correct WiFi BSSID in SDP response"
      contains: "set_bssid with MAC address from QNetworkInterface"
  key_links:
    - from: "AudioChannelHandler onMessage switch"
      to: "default unhandled logger"
      via: "0x8021/0x8022 fall through to default case (Phase 1 logger catches them)"
      pattern: "default:.*unknown"
    - from: "ServiceDiscoveryBuilder::buildWifiDescriptor"
      to: "wlan0 MAC address"
      via: "QNetworkInterface hardware address lookup"
      pattern: "hardwareAddress|set_bssid"
---

<objective>
Remove all retracted proto dead code, fix the WiFi bssid semantic bug, and fix the failing navbar test.

Purpose: Per user decision -- "Full removal, if it's not called, it's gone. No stubs, no RETRACTED comments." The Phase 1 unhandled-message logger catches any surprise traffic on retracted message IDs, so explicit stubs are redundant. The bssid bug sends the network name where the phone expects a MAC address. The navbar test is the only failing test blocking 100% pass rate.

Output: Clean handler code, correct WiFi SDP semantics, 64/64 tests green.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-oaa-proto-compliance-library-rename/04-CONTEXT.md
@libs/open-androidauto/src/HU/Handlers/AudioChannelHandler.cpp
@libs/open-androidauto/include/oaa/HU/Handlers/AudioChannelHandler.hpp
@libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp
@libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp
@libs/open-androidauto/include/oaa/Channel/MessageIds.hpp
@libs/open-androidauto/src/Messenger/ProtocolLogger.cpp
@src/core/aa/AndroidAutoOrchestrator.cpp
@src/core/aa/ServiceDiscoveryBuilder.cpp
@src/core/aa/ServiceDiscoveryBuilder.hpp
@tests/test_navbar_controller.cpp

<interfaces>
<!-- Retracted items to remove (verified by grep) -->

From libs/open-androidauto/include/oaa/Channel/MessageIds.hpp lines 32-33:
```cpp
constexpr uint16_t AUDIO_FOCUS_STATE = 0x8021;
constexpr uint16_t AUDIO_STREAM_TYPE = 0x8022;
```
Line 63 comment: "0x8005 was NAV_FOCUS_INDICATION -- retracted"

From AudioChannelHandler.cpp:
- Line 11: comment about removed .pb.h files
- Lines 54-59: switch cases for 0x8021 and 0x8022
- Lines 153-165: handleAudioFocusState() and handleAudioStreamType() method bodies

From NavigationChannelHandler.cpp:
- Line 8: `#include "NavigationFocusIndicationMessage.pb.h"` (dead include)
- Lines 46-48: switch case for 0x8005
- Lines 170-171: comment about removed handleFocusIndication

From ServiceDiscoveryBuilder.cpp line 401 (the bug):
```cpp
wifiChannel->set_bssid(wifiSsid_.toStdString());  // sends SSID, should send MAC
```

Fix pattern from BluetoothDiscoveryService.cpp:
```cpp
for (const auto& iface : QNetworkInterface::allInterfaces()) {
    if (iface.name() == "wlan0")
        bssid = iface.hardwareAddress();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove retracted handler stubs, dead code, and fix bssid bug</name>
  <files>
    libs/open-androidauto/src/HU/Handlers/AudioChannelHandler.cpp
    libs/open-androidauto/include/oaa/HU/Handlers/AudioChannelHandler.hpp
    libs/open-androidauto/src/HU/Handlers/NavigationChannelHandler.cpp
    libs/open-androidauto/include/oaa/HU/Handlers/NavigationChannelHandler.hpp
    libs/open-androidauto/include/oaa/Channel/MessageIds.hpp
    libs/open-androidauto/src/Messenger/ProtocolLogger.cpp
    src/core/aa/AndroidAutoOrchestrator.cpp
    src/core/aa/ServiceDiscoveryBuilder.cpp
    src/core/aa/ServiceDiscoveryBuilder.hpp
  </files>
  <action>
    Two concerns in one task because they share no file overlap and are both small mechanical changes.

    **A) Dead code removal** -- per user decision "Full removal, no stubs, no RETRACTED comments":

    AudioChannelHandler.cpp:
    - Delete line 11 comment about removed .pb.h includes
    - Remove `case AUDIO_FOCUS_STATE:` (0x8021) and `case AUDIO_STREAM_TYPE:` (0x8022) from onMessage() switch. These fall to default which emits unknownMessage -- the Phase 1 unhandled logger catches them.
    - Delete `handleAudioFocusState()` method body (~lines 153-158)
    - Delete `handleAudioStreamType()` method body (~lines 160-165)

    AudioChannelHandler.hpp:
    - Remove `handleAudioFocusState` and `handleAudioStreamType` private method declarations (lines 33-34)
    - Remove comment at line 27 about retracted signals

    NavigationChannelHandler.cpp:
    - Delete `#include "NavigationFocusIndicationMessage.pb.h"` at line 8 (dead include for retracted proto)
    - Remove `case 0x8005:` block (lines 46-48) -- falls to default unhandled logger
    - Delete comment at line ~170 about removed handleFocusIndication

    NavigationChannelHandler.hpp:
    - Remove comment at line 40 about retracted navigationFocusChanged

    MessageIds.hpp:
    - Delete `AUDIO_FOCUS_STATE = 0x8021` and `AUDIO_STREAM_TYPE = 0x8022` from AVMessageId namespace (lines 32-33)
    - Delete comment about retracted NAV_FOCUS_INDICATION at line 63

    ProtocolLogger.cpp:
    - Remove `case 0x8005: return "NAV_0x8005_RETRACTED";` (line 308) -- unknown IDs have hex fallback

    AndroidAutoOrchestrator.cpp:
    - Remove comment at line 347 about retracted audio signals
    - Remove comment at line 429 about retracted navigation signal

    ServiceDiscoveryBuilder.cpp:
    - Remove comment at line 25 about retracted PhoneStatusChannelData

    **B) WiFi BSSID fix** (CLN-02):

    ServiceDiscoveryBuilder.hpp:
    - Add `QString wifiBssid_` member alongside `wifiSsid_`
    - Add `wifiBssid` constructor parameter (with default `{}`)

    ServiceDiscoveryBuilder.cpp:
    - Accept `wifiBssid` in constructor, store in `wifiBssid_`
    - In `buildWifiDescriptor()` line 401: change `wifiChannel->set_bssid(wifiSsid_.toStdString())` to `wifiChannel->set_bssid(wifiBssid_.toStdString())`

    Caller (AndroidAutoOrchestrator.cpp or wherever ServiceDiscoveryBuilder is constructed):
    - Look up wlan0 MAC via `QNetworkInterface::allInterfaces()` -- find interface with name "wlan0", get `hardwareAddress()`. Use same pattern as BluetoothDiscoveryService.cpp (~line 382-393).
    - Pass the MAC string to ServiceDiscoveryBuilder constructor as `wifiBssid`.
    - Fallback: if no wlan0 found, pass empty string (the proto field stays empty which is better than a wrong value).

    Do NOT touch files inside `libs/open-androidauto/proto/` -- that's the read-only submodule.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -5 && echo "---GREP CHECK---" && grep -rn "retracted\|RETRACTED" ../libs/open-androidauto/src/ ../libs/open-androidauto/include/ ../src/core/aa/AndroidAutoOrchestrator.cpp ../src/core/aa/ServiceDiscoveryBuilder.cpp 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <done>Zero retracted handler stubs, dead methods, retracted comments, or dead MessageIds constants remain. ServiceDiscoveryBuilder sends wlan0 MAC address as BSSID. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix navbar controller hold progress test</name>
  <files>tests/test_navbar_controller.cpp</files>
  <action>
    Fix the failing `testHoldProgressEmittedDuringHold` test. The assertion `lastProgress > firstProgress` returns FALSE -- this is a timing issue where QTimer-based hold progress signals aren't firing reliably during `QTest::qWait()`.

    Read the test fully to understand the exact pattern. Likely fixes (try in order):
    1. Replace `QTest::qWait(N)` with `QSignalSpy::wait(timeout)` which processes events more reliably
    2. Add explicit `QCoreApplication::processEvents()` calls between wait steps
    3. Increase wait durations to give timers more room

    The fix should make the test deterministic, not just "usually pass with longer waits." Prefer approach 1 or 2 over 3.

    Run the test in isolation multiple times to confirm it passes reliably:
    `for i in {1..5}; do ctest --test-dir build -R test_navbar_controller --output-on-failure 2>&1 | tail -3; done`
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -3 && for i in {1..3}; do ctest -R test_navbar_controller --output-on-failure 2>&1 | tail -3; done</automated>
  </verify>
  <done>testHoldProgressEmittedDuringHold passes reliably (3 consecutive runs, 0 failures). Full suite: 64/64 pass.</done>
</task>

</tasks>

<verification>
- `grep -rn "retracted\|RETRACTED" libs/open-androidauto/src/ libs/open-androidauto/include/ src/core/aa/AndroidAutoOrchestrator.cpp src/core/aa/ServiceDiscoveryBuilder.cpp` returns no hits
- `grep -n "handleAudioFocusState\|handleAudioStreamType\|AUDIO_FOCUS_STATE\|AUDIO_STREAM_TYPE" libs/open-androidauto/include/ libs/open-androidauto/src/` returns no hits
- `grep -n "set_bssid" src/core/aa/ServiceDiscoveryBuilder.cpp` shows MAC address variable, not wifiSsid_
- `ctest --test-dir build --output-on-failure` shows 64/64 passed (0 failed)
</verification>

<success_criteria>
- Zero retracted handler stubs, dead method declarations, or RETRACTED comments in source (excluding proto/ submodule)
- WiFi SDP descriptor contains wlan0 MAC address as BSSID
- 64/64 tests pass with 0 failures
- Navbar hold progress test passes reliably across multiple runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-oaa-proto-compliance-library-rename/04-01-SUMMARY.md`
</output>
