---
phase: 03-touch-normalization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - qml/controls/UiMetrics.qml
  - qml/applications/android_auto/AndroidAutoMenu.qml
autonomous: false
requirements: [TCH-03]
gap_closure: true

must_haves:
  truths:
    - "Settings text is comfortably readable at arm's length on 1024x600 display"
    - "Font size increase applies uniformly across all settings pages via UiMetrics tokens"
    - "No hardcoded pixel font sizes remain in settings-visible QML"
  artifacts:
    - path: "qml/controls/UiMetrics.qml"
      provides: "Increased base font sizes for automotive readability (~3mm+ cap height)"
      contains: "fontBody.*28"
  key_links:
    - from: "qml/controls/UiMetrics.qml"
      to: "all settings QML pages"
      via: "UiMetrics.fontBody, UiMetrics.fontSmall, etc."
      pattern: "UiMetrics\\.font(Body|Small|Tiny|Title|Heading)"
---

<objective>
Close UAT gap: text too small on 1024x600 automotive display.

Purpose: Increase UiMetrics base font sizes ~1.4x so all settings text hits 3mm+ cap height at arm's length. The scaling system works correctly — only the base values need adjustment.

Output: Updated UiMetrics.qml with larger base font sizes, updated font floors, fixed hardcoded font size in AndroidAutoMenu.qml.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/settings-text-too-small.md

@qml/controls/UiMetrics.qml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase UiMetrics base font sizes for automotive readability</name>
  <files>qml/controls/UiMetrics.qml, qml/applications/android_auto/AndroidAutoMenu.qml</files>
  <action>
In `qml/controls/UiMetrics.qml`, increase the base font size values (the integer multiplied by `_fontScaleTotal`) to achieve ~3mm+ cap height on a 7-inch 1024x600 display (169.5 DPI, scale factor ~1.06x):

Current -> New base sizes:
- fontTiny:    14 -> 20 (will render ~21px, ~3.1mm height)
- fontSmall:   16 -> 22 (will render ~23px, ~3.4mm height)
- fontBody:    20 -> 28 (will render ~30px, ~4.4mm height)
- fontTitle:   22 -> 30 (will render ~32px, ~4.7mm height)
- fontHeading: 28 -> 36 (will render ~38px, ~5.6mm height)

Update the corresponding font floor defaults to maintain proportional minimums:
- fontTiny floor:    10 -> 14
- fontSmall floor:   12 -> 16
- fontBody floor:    14 -> 20
- fontTitle floor:   16 -> 22
- fontHeading floor: 18 -> 26

Update the startup logging block (lines 157-162) to match the new base values in the `fonts` array.

Also fix the one hardcoded font size in `qml/applications/android_auto/AndroidAutoMenu.qml` line 59: change `font.pixelSize: 10` to `font.pixelSize: UiMetrics.fontTiny` (this is a debug touch coordinate overlay, fontTiny is appropriate).

Do NOT change:
- The scaling math (_fontScaleTotal, _autoFontScale, scaleH/scaleV)
- The token override system (_tok function)
- The floor system logic (_floor function)
- Any layout tokens (rowH, touchMin, spacing, margins, etc.)
- Any other files — all settings pages already use UiMetrics font tokens correctly
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -E "fontBody.*28 \*" qml/controls/UiMetrics.qml && grep -E "fontSmall.*22 \*" qml/controls/UiMetrics.qml && grep -E "fontTiny.*20 \*" qml/controls/UiMetrics.qml && grep "UiMetrics.fontTiny" qml/applications/android_auto/AndroidAutoMenu.qml && echo "PASS: all font sizes updated"</automated>
  </verify>
  <done>All five font tokens use increased base values. Font floors updated proportionally. No hardcoded font.pixelSize values remain outside UiMetrics. Build compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify text readability on Pi</name>
  <files>n/a</files>
  <action>
User verifies text readability on the physical 1024x600 display at arm's length.
  </action>
  <verify>User confirms text is readable</verify>
  <done>User approves text sizing across all settings pages</done>
  <what-built>Increased all UiMetrics font base sizes ~1.4x for automotive readability. fontBody now renders at ~30px (4.4mm height) instead of 21px (2.2mm). All settings pages inherit the change automatically via UiMetrics tokens.</what-built>
  <how-to-verify>
    1. Build and deploy to Pi (git push, git pull on Pi, rebuild)
    2. Open any settings sub-page (e.g., System, Display, Audio)
    3. Verify text is comfortably readable at arm's length
    4. Check that no text is clipped or overflowing row containers
    5. Check that section headers, control labels, and picker values all look proportional
    6. Navigate through all 9 settings pages to confirm consistency
  </how-to-verify>
  <resume-signal>Type "approved" if text sizing is good, or describe any issues (too big, clipping, specific pages with problems)</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "font.pixelSize:" qml/controls/UiMetrics.qml` should return 0 (no hardcoded sizes in UiMetrics itself)
- `grep -rn "font.pixelSize:.*[0-9]" qml/applications/settings/ | grep -v UiMetrics` should return 0 (no hardcoded sizes in settings pages)
- Build succeeds: `cd build && cmake --build . -j$(nproc)` exits 0
</verification>

<success_criteria>
- All UiMetrics font tokens use increased base values (~1.4x)
- fontBody renders at ~30px on 1024x600 @ 7" (was 21px)
- User confirms text is readable at arm's length on Pi
- No layout breakage (clipping, overflow) from larger text
</success_criteria>

<output>
After completion, create `.planning/phases/03-touch-normalization/03-03-SUMMARY.md`
</output>
