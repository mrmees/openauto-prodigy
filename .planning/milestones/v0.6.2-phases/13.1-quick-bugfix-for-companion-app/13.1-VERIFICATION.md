---
phase: 13.1-quick-bugfix-for-companion-app
verified: 2026-03-15T18:30:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
---

# Phase 13.1: Companion Reconnect Hardening — Verification Report

**Phase Goal:** Fix stale CompanionListenerService client blocking all future companion reconnects. Always-replace on new connection, errorOccurred handler, 30s inactivity timeout, one shared idempotent clearClientSession() cleanup path.
**Verified:** 2026-03-15T18:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A companion app reconnection succeeds immediately instead of being permanently rejected | VERIFIED | `onNewConnection()` L195-201: if `client_` non-null, calls `clearClientSession()` then accepts new client. `alwaysReplaceStaleClient` test passes (86/86). |
| 2 | A socket error on the companion connection triggers full cleanup, not a zombie session | VERIFIED | `errorOccurred` connected at L208-213 via lambda calling `clearClientSession()`. `errorOccurredTriggersCleanup` test uses SO_LINGER RST and asserts `isConnected() == false`. |
| 3 | 30 seconds of no valid status messages from the companion triggers session teardown | VERIFIED | `inactivityTimer_` created in constructor (L23-33, `setSingleShot(true)`, 30000ms), started on new connection (L215), reset on MAC-verified status (L319). `inactivityTimeout` test at 100ms passes. |
| 4 | Calling clearClientSession() twice in succession does not crash or double-emit signals | VERIFIED | L668 null-guard `if (!client_) return;` is first line. `cleanupIdempotent` test asserts `spy.count() == 1` after double-cleanup path. |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/core/services/CompanionListenerService.hpp` | clearClientSession() declaration, QTimer* inactivityTimer_ member | VERIFIED | L92 `void clearClientSession();`, L135 `QTimer* inactivityTimer_ = nullptr;`, L136 `int inactivityTimeoutMs_ = 30000;`, L5 `#include <QTimer>`, L43 `void setInactivityTimeout(int ms);` |
| `src/core/services/CompanionListenerService.cpp` | Idempotent cleanup method, always-replace logic, errorOccurred handler, inactivity timer | VERIFIED | 706 lines, all four behaviors fully implemented (see key links below). Not a stub. |
| `tests/test_companion_listener.cpp` | 4 new tests: alwaysReplaceStaleClient, cleanupIdempotent, inactivityTimeout, errorOccurredTriggersCleanup | VERIFIED | All 4 tests present at L555-716. Test suite passes: 86/86 total, 15 companion tests including the 4 new ones. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `CompanionListenerService::onNewConnection()` | `clearClientSession()` | always-replace when client_ is non-null | WIRED | L195-201: `if (client_) { ... clearClientSession(); }` before accepting new connection |
| `QAbstractSocket::errorOccurred` | `clearClientSession()` | signal/slot lambda in onNewConnection | WIRED | L208-213: `connect(client_, &QAbstractSocket::errorOccurred, this, [this](...) { clearClientSession(); })` |
| `QTimer::timeout (inactivityTimer_)` | `clearClientSession()` | timer lambda in constructor | WIRED | L26-33: `connect(inactivityTimer_, &QTimer::timeout, this, [this]() { ... clearClientSession(); })` |
| `handleStatus() after MAC verify` | `inactivityTimer_->start()` | timer reset on valid status | WIRED | L319: `inactivityTimer_->start();` immediately after MAC-verified status block |

All 4 key links confirmed wired against actual source lines.

---

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| BF-01 | New companion connection replaces stale client instead of rejecting, with diagnostic logging | SATISFIED | `onNewConnection()` L196-200: warning log with old/new peer addresses + reason=replaced_by_new_connection |
| BF-02 | Socket errorOccurred triggers full session cleanup via shared idempotent path | SATISFIED | `errorOccurred` lambda at L208-213 calls `clearClientSession()` — same path used by all other teardown triggers |
| BF-03 | 30-second inactivity timeout (from last valid status message) triggers session teardown | SATISFIED | Timer created with 30000ms interval, reset at L319 only after `verifyMac()` returns true (authenticated status only) |
| BF-04 | clearClientSession() is idempotent — double-call safe, no double-emit, no crash | SATISFIED | Null-guard at L668, `client_->disconnect(this)` at L671 before abort prevents re-entrant signals |

No orphaned requirements — REQUIREMENTS.md traceability table maps BF-01 through BF-04 exclusively to Phase 13.1.

---

### Anti-Patterns Found

None. No TODOs, FIXMEs, placeholders, empty implementations, or stub returns found in the modified files.

Notable quality observation: `stop()` was refactored to delegate to `clearClientSession()` (deviation from plan, correctly auto-fixed), eliminating a fifth divergent cleanup path that would have been a latent bug.

---

### Human Verification Required

One item needs manual Pi verification (documented in plan success criteria and session handoff):

**Manual Pi test — companion reconnect after force-stop**
- **Test:** Connect companion app to Pi. Force-stop the companion app (Android "Force Stop"). Relaunch companion app. Observe reconnection behavior.
- **Expected:** Reconnection succeeds immediately — no "Connection problem" error, no need to restart the Pi-side service.
- **Why human:** Requires real BT+WiFi handshake and physical Android device. Cannot replicate the full wireless AA + companion TCP stack in a unit test environment.

**Manual Pi test — inactivity timeout resets state**
- **Test:** Connect companion, let it sit idle for 30+ seconds with no status messages. Verify GPS/battery/internet indicators reset to defaults in the UI.
- **Expected:** UI shows no GPS, no phone battery, no internet — indicators go dark/default after 30s of companion silence.
- **Why human:** Requires observing live UI state on the 1024x600 display.

These are verification items, not gaps. The automated implementation is complete and correct.

---

### Commit Verification

Both documented commits exist and have correct diffs:
- `87ebaa5` — `test(13.1-01): add failing tests for companion reconnect hardening` (+3 files, 173 insertions)
- `8741a4e` — `fix(13.1-01): harden companion reconnect with idempotent cleanup` (+3 files, 104 insertions/27 deletions)

---

### Test Results

```
86/86 tests passed (0 failures)
test_companion_listener: 15 tests, Passed 5.34 sec
Full suite: 16.27 sec
```

No regressions introduced.

---

_Verified: 2026-03-15T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
