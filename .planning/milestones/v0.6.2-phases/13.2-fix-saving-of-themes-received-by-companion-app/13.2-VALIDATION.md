---
phase: 13.2
slug: fix-saving-of-themes-received-by-companion-app
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-15
---

# Phase 13.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Qt Test (QTest) + CTest |
| **Config file** | tests/CMakeLists.txt |
| **Quick run command** | `cd build && ctest --output-on-failure -R "theme\|config"` |
| **Full suite command** | `cd build && ctest --output-on-failure` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd build && ctest --output-on-failure -R "theme\|config"`
- **After every plan wave:** Run `cd build && ctest --output-on-failure`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 13.2-01-01 | 01 | 1 | TP-01 | unit | `ctest -R theme` | ❌ W0 | ⬜ pending |
| 13.2-01-02 | 01 | 1 | WP-UX-01 | visual | manual | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New test case in `tests/test_theme_service.cpp` for setTheme persistence via IConfigService
- [ ] Mock or real ConfigService with temp file for persistence verification

*Existing test infrastructure (ThemeService test helpers, temp dir fixture) covers the base.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Wallpaper toggle shows/hides picker | WP-UX-01 | QML visual layout | Open settings → theme → verify toggle controls picker visibility |
| Delete button at bottom of list | WP-UX-01 | QML layout order | Open settings → theme → verify delete button is last row |
| Theme persists across restart | TP-01 | Full app lifecycle | Set companion theme → restart service → verify theme loads |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
