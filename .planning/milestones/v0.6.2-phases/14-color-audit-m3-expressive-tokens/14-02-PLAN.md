---
phase: 14-color-audit-m3-expressive-tokens
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - qml/components/NavbarControl.qml
  - qml/components/Navbar.qml
  - qml/components/PairingDialog.qml
  - qml/components/NavbarPopup.qml
  - qml/components/ExitDialog.qml
  - qml/components/GestureOverlay.qml
  - qml/widgets/AAStatusWidget.qml
  - qml/controls/Tile.qml
  - qml/controls/SettingsToggle.qml
  - qml/applications/settings/CompanionSettings.qml
  - qml/applications/settings/ThemeSettings.qml
  - qml/applications/settings/ConnectionSettings.qml
  - qml/applications/settings/DebugSettings.qml
  - docs/state-matrix.md
autonomous: false
requirements: [CA-01, CA-02, CA-03]

must_haves:
  truths:
    - "NavbarControl quick-tap shows a distinctly colored pressed fill (primaryContainer) not just rest-state colors"
    - "NavbarControl hold-progress fill uses primaryContainer (not tertiary)"
    - "NavbarControl icon color changes to onPrimaryContainer during press and hold progress"
    - "AA-active mode still forces black/white navbar colors"
    - "AAStatusWidget connected icon uses ThemeService.success (semantic green) not primary"
    - "All 4 popup/dialog QML files use ThemeService.surfaceTintHigh or surfaceTintHighest instead of inline math"
    - "Power menu pressed buttons have onPrimaryContainer text on primaryContainer background"
    - "Tile content uses onPrimary (rest) / onPrimaryContainer (pressed) instead of inverseOnSurface"
    - "Every checked Switch in settings uses primaryContainer active styling — whether through SettingsToggle or declared inline"
    - "CompanionSettings degraded status uses ThemeService.warning (not tertiary)"
    - "State matrix document maps all control types to their token assignments per state"
  artifacts:
    - path: "qml/components/NavbarControl.qml"
      provides: "Pressed state + hold-progress primaryContainer fill + onPrimaryContainer icon color"
    - path: "qml/components/PairingDialog.qml"
      provides: "Centralized surfaceTintHigh usage"
    - path: "qml/components/NavbarPopup.qml"
      provides: "Centralized surfaceTintHigh usage (with aaActive guard)"
    - path: "qml/components/ExitDialog.qml"
      provides: "Centralized surfaceTintHigh usage"
    - path: "qml/components/GestureOverlay.qml"
      provides: "Centralized surfaceTintHighest usage (alpha stays in QML)"
    - path: "qml/widgets/AAStatusWidget.qml"
      provides: "Semantic green for connected state"
    - path: "qml/controls/Tile.qml"
      provides: "Correct on-* pairing for primary/primaryContainer backgrounds"
    - path: "qml/controls/SettingsToggle.qml"
      provides: "Active Switch styled with primaryContainer track"
    - path: "qml/applications/settings/ThemeSettings.qml"
      provides: "Inline Switch uses primaryContainer active styling"
    - path: "qml/applications/settings/ConnectionSettings.qml"
      provides: "Inline Switch uses primaryContainer active styling"
    - path: "qml/applications/settings/DebugSettings.qml"
      provides: "Inline Switch uses primaryContainer active styling"
    - path: "qml/applications/settings/CompanionSettings.qml"
      provides: "Degraded status uses warning token instead of tertiary"
    - path: "docs/state-matrix.md"
      provides: "Capability-based token map for all control types"
  key_links:
    - from: "qml/components/NavbarControl.qml"
      to: "ThemeService.primaryContainer"
      via: "QML property binding for pressed AND hold-progress states"
      pattern: "ThemeService\\.primaryContainer"
    - from: "qml/components/PairingDialog.qml"
      to: "ThemeService.surfaceTintHigh"
      via: "QML property binding replacing inline math"
      pattern: "ThemeService\\.surfaceTintHigh"
    - from: "qml/widgets/AAStatusWidget.qml"
      to: "ThemeService.success"
      via: "QML property binding for semantic status"
      pattern: "ThemeService\\.success"
    - from: "qml/controls/SettingsToggle.qml"
      to: "ThemeService.primaryContainer"
      via: "Material.accent on Switch for active track color"
      pattern: "ThemeService\\.primaryContainer"
    - from: "qml/applications/settings/CompanionSettings.qml"
      to: "ThemeService.warning"
      via: "QML property binding replacing tertiary for degraded status"
      pattern: "ThemeService\\.warning"
---

<objective>
Apply QML color audit fixes across all affected files: fix NavbarControl pressed + hold-progress state model, style SettingsToggle active state with primaryContainer, replace tertiary-as-warning in CompanionSettings with proper warning token, centralize popup tint math, apply semantic status colors, fix token pairings, and produce the capability-based state matrix document.

Purpose: This is the visible payoff -- users see correctly paired tokens, bolder accent colors on interactive surfaces, consistent popup tinting, and proper semantic status colors. The state matrix documents the system for future maintenance.

Output: All QML files updated with correct M3 token usage + state matrix document.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-color-audit-m3-expressive-tokens/14-CONTEXT.md
@.planning/phases/14-color-audit-m3-expressive-tokens/14-RESEARCH.md
@.planning/phases/14-color-audit-m3-expressive-tokens/14-01-SUMMARY.md

@qml/components/NavbarControl.qml
@qml/components/Navbar.qml
@qml/components/PairingDialog.qml
@qml/components/NavbarPopup.qml
@qml/components/ExitDialog.qml
@qml/components/GestureOverlay.qml
@qml/widgets/AAStatusWidget.qml
@qml/controls/Tile.qml
@qml/controls/SettingsToggle.qml
@qml/applications/settings/CompanionSettings.qml
@qml/applications/settings/ThemeSettings.qml
@qml/applications/settings/ConnectionSettings.qml
@qml/applications/settings/DebugSettings.qml

<interfaces>
<!-- New ThemeService properties from Plan 01 (available after wave 1) -->
From src/core/services/ThemeService.hpp (added by Plan 01):
```cpp
Q_PROPERTY(QColor surfaceTintHigh READ surfaceTintHigh NOTIFY colorsChanged)
Q_PROPERTY(QColor surfaceTintHighest READ surfaceTintHighest NOTIFY colorsChanged)
Q_PROPERTY(QColor warning READ warning NOTIFY colorsChanged)
Q_PROPERTY(QColor onWarning READ onWarning NOTIFY colorsChanged)
```

<!-- Existing properties used in this plan -->
From src/core/services/ThemeService.hpp (existing):
```cpp
Q_PROPERTY(QColor primaryContainer READ primaryContainer NOTIFY colorsChanged)
Q_PROPERTY(QColor onPrimaryContainer READ onPrimaryContainer NOTIFY colorsChanged)
Q_PROPERTY(QColor onPrimary READ onPrimary NOTIFY colorsChanged)
Q_PROPERTY(QColor success READ success NOTIFY colorsChanged)  // #4CAF50
```

<!-- Navbar context needed for AA-active guard -->
From qml/components/Navbar.qml (existing):
```qml
// Lines 15-16: navbar aliases available to NavbarControl children
property color barBg: aaActive ? "#000000" : ThemeService.surfaceContainer
property color barFg: aaActive ? "#FFFFFF" : ThemeService.onSurface
property bool aaActive: ...  // true during AA projection
```

<!-- NavbarControl touch handling (existing) -->
From qml/components/NavbarControl.qml (existing):
```qml
// Lines 168-173: MouseArea for launcher mode touch
MouseArea {
    anchors.fill: parent
    onPressed: NavbarController.handlePress(root.controlIndex)
    onReleased: NavbarController.handleRelease(root.controlIndex)
    onExited: NavbarController.handleCancel(root.controlIndex)
}
```

<!-- SettingsToggle structure (existing) -->
From qml/controls/SettingsToggle.qml (existing):
```qml
// Line 44-51: bare Switch control, no Material styling
Switch {
    id: toggle
    onToggled: {
        if (root.configPath === "") return
        ConfigService.setValue(root.configPath, checked)
        ConfigService.save()
    }
}
```

<!-- CompanionSettings degraded status (existing) -->
From qml/applications/settings/CompanionSettings.qml (existing):
```qml
// Line 201: degraded route status indicator dot
if (s === "degraded") return ThemeService.tertiary  // WRONG: tertiary as warning
// Line 218: degraded route status text color
if (s === "degraded") return ThemeService.tertiary  // WRONG: tertiary as warning
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NavbarControl pressed + hold state, SettingsToggle active style, CompanionSettings warning token, popup tint centralization, and token pairings</name>
  <files>qml/components/NavbarControl.qml, qml/components/Navbar.qml, qml/components/PairingDialog.qml, qml/components/NavbarPopup.qml, qml/components/ExitDialog.qml, qml/components/GestureOverlay.qml, qml/widgets/AAStatusWidget.qml, qml/controls/Tile.qml, qml/controls/SettingsToggle.qml, qml/applications/settings/CompanionSettings.qml, qml/applications/settings/ThemeSettings.qml, qml/applications/settings/ConnectionSettings.qml, qml/applications/settings/DebugSettings.qml</files>
  <action>
    **NavbarControl.qml (CA-02 -- pressed state + hold-progress):**

    The current NavbarControl has NO visual feedback on quick taps (press-down). It only shows hold-progress fill. The requirement says "active/pressed navbar control shows a distinctly colored fill (primaryContainer)". Must add pressed-state feedback for quick taps in addition to hold-progress.

    1. Add a `_pressed` property tracking the MouseArea pressed state:
       `property bool _pressed: touchArea.pressed` (rename the existing MouseArea to `touchArea` at line 168)

    2. Line 40 (background Rectangle): Change `color: navbar.barBg` to include pressed state feedback:
       ```qml
       color: navbar.aaActive ? navbar.barBg
            : (root._pressed || root._holdProgress > 0) ? ThemeService.primaryContainer
            : navbar.barBg
       ```
       Remove the separate progress overlay Rectangle (lines 44-52 entirely) -- the background itself now becomes primaryContainer on press/hold, which is cleaner than a partial-fill overlay.

       WAIT -- the hold-progress overlay fills from bottom to top as a visual progress indicator. This is different from a full pressed state. Keep BOTH:
       - The background Rectangle stays `navbar.barBg` (unchanged).
       - Add a NEW pressed-state overlay Rectangle (full fill, NOT bottom-up) BEFORE the existing progress overlay:
         ```qml
         // Pressed-state feedback (instant, full fill)
         Rectangle {
             anchors.fill: parent
             color: ThemeService.primaryContainer
             opacity: navbar.aaActive ? 0.0 : (root._pressed && root._holdProgress === 0 ? 0.3 : 0.0)
             Behavior on opacity { NumberAnimation { duration: 100 } }
         }
         ```
         This shows on quick tap-down but fades out once hold-progress takes over.

    3. Line 49 (existing progress overlay): Change `color` from `ThemeService.tertiary` to `ThemeService.primaryContainer`. Keep the bottom-up fill behavior and `opacity: 0.3`.

    4. Line 60 (icon color): Change from `navbar.barFg` to:
       `color: navbar.aaActive ? navbar.barFg : (root._pressed || root._holdProgress > 0 ? ThemeService.onPrimaryContainer : navbar.barFg)`

    5. Clock text colors (lines 110, 152 -- horizontal and vertical clock Text elements): Same conditional:
       `color: navbar.aaActive ? navbar.barFg : (root._pressed || root._holdProgress > 0 ? ThemeService.onPrimaryContainer : navbar.barFg)`

    6. Rename the MouseArea `id` at line 168 to `touchArea` so `_pressed` can bind to it.

    **Navbar.qml -- Power menu pressedTextColor (CA-01, Pitfall 5):**
    Line 317: Change `pressedTextColor: navbar.barFg` to:
    `pressedTextColor: navbar.aaActive ? "#FFFFFF" : ThemeService.onPrimaryContainer`
    (The pressed background is already `ThemeService.primaryContainer` on line 315, so onPrimaryContainer is the correct pairing.)

    **SettingsToggle.qml (CA-03 -- active toggle uses primaryContainer):**
    The locked context says active settings toggles should use primaryContainer. The bare `Switch` control at line 44 has no Material styling. Add Material accent color to make the active (checked) Switch track use primaryContainer:

    1. Add import at top of file: `import QtQuick.Controls.Material`
    2. On the Switch element (line 44), add Material accent binding:
       ```qml
       Switch {
           id: toggle
           Material.accent: ThemeService.primaryContainer
           onToggled: { ... }
       }
       ```
       This makes the checked Switch track color use `primaryContainer` instead of the default Material accent, which is the M3-correct token for an active/selected toggle control surface.

    **Inline Switches in settings pages (CA-03 — every checked Switch must use the same primaryContainer styling):**

    Every Switch declared inline in settings pages (not just those using SettingsToggle) needs the same Material.accent treatment. Add `import QtQuick.Controls.Material` if not already present, and add `Material.accent: ThemeService.primaryContainer` to each inline Switch:

    - **ThemeSettings.qml** line 52: inline Switch — add `Material.accent: ThemeService.primaryContainer`
    - **ConnectionSettings.qml** line 43: `pairableSwitch` — add `Material.accent: ThemeService.primaryContainer`
    - **DebugSettings.qml** line 53: `codecSwitch` — add `Material.accent: ThemeService.primaryContainer`

    (DebugSettings.qml lines 224, 233, 249 are SettingsToggle instances — they inherit the fix from SettingsToggle.qml above.)

    **CompanionSettings.qml (CA-03 -- degraded status uses warning, not tertiary):**
    The locked context says "Do NOT let tertiary masquerade as warning -- define real semantic token paths for warning/degraded". Lines 201 and 218 use `ThemeService.tertiary` for degraded route status. Replace with `ThemeService.warning` (the new Q_PROPERTY from Plan 01):

    1. Line 201: Change `if (s === "degraded") return ThemeService.tertiary` to:
       `if (s === "degraded") return ThemeService.warning`

    2. Line 218: Change `if (s === "degraded") return ThemeService.tertiary` to:
       `if (s === "degraded") return ThemeService.warning`

    **PairingDialog.qml (CA-01 tint centralization):**
    Lines 30-34: Replace the inline Qt.rgba(...) color with:
    `color: ThemeService.surfaceTintHigh`
    Keep the radius and border unchanged.

    **ExitDialog.qml (CA-01 tint centralization):**
    Lines 20-24: Replace the inline Qt.rgba(...) color with:
    `color: ThemeService.surfaceTintHigh`

    **NavbarPopup.qml (CA-01 tint centralization):**
    Lines 82-86: Replace the ternary's else branch. Keep the aaActive guard:
    `color: navbar.aaActive ? "#1A1A1A" : ThemeService.surfaceTintHigh`

    **GestureOverlay.qml (CA-01 tint centralization):**
    Lines 75-79: Replace inline Qt.rgba(...) with surfaceTintHighest, keeping alpha in QML:
    `color: Qt.rgba(ThemeService.surfaceTintHighest.r, ThemeService.surfaceTintHighest.g, ThemeService.surfaceTintHighest.b, 0.87)`

    **AAStatusWidget.qml (CA-01 semantic color):**
    Line 28: Change connected icon color from `ThemeService.primary` to `ThemeService.success`:
    `color: connected ? ThemeService.success : ThemeService.onSurfaceVariant`

    **Tile.qml (CA-01 token pairing):**
    Line 68 (icon color): Change from `ThemeService.inverseOnSurface` to:
    `color: tile._isPressed ? ThemeService.onPrimaryContainer : ThemeService.onPrimary`
    Line 75 (text color): Same change:
    `color: tile._isPressed ? ThemeService.onPrimaryContainer : ThemeService.onPrimary`
    (The background is `primary` at rest -> `onPrimary` for content. Pressed is `primaryContainer` -> `onPrimaryContainer`.)
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake -DCMAKE_PREFIX_PATH=/opt/qt/6.8.2/gcc_64 .. && make -j$(nproc) && ctest --output-on-failure</automated>
  </verify>
  <done>All 13 QML files updated with correct M3 token pairings. NavbarControl shows primaryContainer fill on both quick-tap press AND hold-progress with onPrimaryContainer content. SettingsToggle active Switch track uses primaryContainer. CompanionSettings degraded status uses ThemeService.warning instead of tertiary. 4 popup/dialog files use centralized surfaceTintHigh/Highest. AAStatusWidget uses semantic success. Tile uses correct on-* pairings. Full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Create capability-based state matrix document</name>
  <files>docs/state-matrix.md</files>
  <action>
    Create `docs/state-matrix.md` documenting the token assignments for every control type and state.

    Structure:
    1. **Header:** Purpose statement -- this is the single source of truth for which M3 tokens each control type uses in each state.
    2. **Control Categories:** action, selection, gesture, passive (per locked decision).
    3. **For each control, document a table:**

    | State | Container | Content | Border | Notes |

    Controls to document:
    - **Action controls:** ElevatedButton, FilledButton, OutlinedButton, SettingsRow (tappable)
    - **Selection controls:** SettingsToggle (Switch), Tile, theme selector, ComboBox/picker
    - **Gesture controls:** NavbarControl (rest, pressed, hold-progress, active states)
    - **Passive content:** NormalText, SpecialText, SettingsRow (display-only), dividers, page dots

    Specific token assignments per control (matching what's implemented):
    - **NavbarControl rest:** surfaceContainer bg (via barBg), onSurface icon (via barFg)
    - **NavbarControl pressed (quick tap):** primaryContainer fill overlay at 0.3 opacity, onPrimaryContainer icon
    - **NavbarControl hold-progress:** primaryContainer fill overlay (bottom-up) at 0.3 opacity, onPrimaryContainer icon
    - **NavbarControl (AA active):** #000000 bg, #FFFFFF icon (hardcoded, AA owns display)
    - **ElevatedButton rest:** surfaceContainerLow bg, barFg text
    - **ElevatedButton pressed:** primaryContainer bg, onPrimaryContainer text
    - **Tile rest:** primary bg, onPrimary content
    - **Tile pressed:** primaryContainer bg, onPrimaryContainer content
    - **SettingsToggle active (checked):** Switch track uses primaryContainer (via Material.accent)
    - **SettingsToggle inactive:** default Material Switch styling
    - **Settings rows:** surfaceContainer bg (neutral), onSurface text -- NO primaryContainer fill per "don't Skittle the settings" decision
    - **Popup/dialog surfaces:** surfaceTintHigh (opaque) or surfaceTintHighest with 0.87 alpha
    - **Widget cards:** surfaceContainer glass (from WidgetHost), accent via text/icons only (primary for values/icons, onSurface/onSurfaceVariant for labels)
    - **AAStatusWidget connected:** success (semantic green), NOT primary
    - **Page dots:** onSurfaceVariant at 0.4 opacity

    4. **Semantic status colors section:** success (green #4CAF50), warning (amber #FF9800), error (error token), onSuccess/onWarning/onError all white -- "semantic always wins over theme accent for status indicators". Note: CompanionSettings degraded route status uses `warning` token, NOT tertiary.

    5. **Expression principle:** "Colored islands on calm surfaces" -- interactive surfaces users touch get container fills, structural backgrounds stay surfaceContainer family.

    6. **Widget accent rules recap:** one accent family per widget, primary for default, primaryContainer reserved for state changes.

    7. **Deferred items:** Shared QML StateStyle component (future phase if controls drift), widget state-change container fills.
  </action>
  <verify>
    <automated>test -f /home/matt/claude/personal/openautopro/openauto-prodigy/docs/state-matrix.md && grep -c "NavbarControl\|ElevatedButton\|Tile\|SettingsToggle\|semantic\|warning" /home/matt/claude/personal/openautopro/openauto-prodigy/docs/state-matrix.md</automated>
  </verify>
  <done>docs/state-matrix.md exists with token assignments for all control categories (action, selection, gesture, passive), including NavbarControl pressed state, SettingsToggle active styling, semantic status color rules with warning token, and the "colored islands" expression principle documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pi visual verification of color audit in day and night modes</name>
  <what-built>Complete QML color audit: NavbarControl pressed + hold states, SettingsToggle active styling, CompanionSettings warning token, popup tint centralization, semantic status colors, and token pairings across 10 QML files.</what-built>
  <how-to-verify>
    Deploy to Pi: cross-build binary, rsync to Pi, push QML via git pull on Pi.

    **Day mode checks:**
    1. Open launcher -- tiles should show bold primary-colored backgrounds with legible text (onPrimary, not inverseOnSurface)
    2. **Quick-tap a navbar control** (volume/brightness) -- should show brief primaryContainer fill flash with onPrimaryContainer icon, visually distinct from rest state. This is the pressed state, NOT hold progress.
    3. **Hold a navbar control** -- fill should animate from bottom to top in primaryContainer with onPrimaryContainer icon, NOT tertiary
    4. Open power menu (long-hold clock) -- press a button, text should remain readable on primaryContainer background
    5. Check AAStatusWidget on home screen -- if AA is connected, icon should be green (semantic), not theme accent
    6. Open any dialog (pairing, exit) -- background should have subtle primary tint (same as before but centralized)
    7. **Open Settings > Companion** -- toggle the companion enabled switch. When checked/active, the Switch track should show primaryContainer color, not the default blue/green Material accent.
    8. **Check Companion Settings route status** -- if degraded state is visible, the status dot and text should be amber (warning), NOT the theme's tertiary color.
    8a. **Open Settings > Debug** -- toggle the codec enable switch (inline Switch, not SettingsToggle). When checked, the track should show the same primaryContainer color as the SettingsToggle switches below it. All switches in all settings pages must look consistent.
    8b. **Open Settings > Connection** -- toggle the pairable switch. Same primaryContainer track color when checked.

    **Night mode checks (in a dark environment):**
    9. Toggle to night mode via settings or companion
    10. Accent colors (primary, primaryContainer on tiles/navbar) should be noticeably less saturated than day mode
    11. Colors should still be recognizably the same hue -- not washed out or shifted
    12. Primary-colored surfaces should be comfortable to look at, not a glare source
    13. Success green on AAStatusWidget should NOT be clamped (semantic, not accent)

    **AA mode check:**
    14. Connect to AA -- navbar should still show black bg + white icons regardless of theme
    15. 3-finger gesture overlay tint should look consistent with other dialogs
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && ctest --output-on-failure
```
All tests pass. Visual verification on Pi confirms correct token pairings in both day and night modes.
</verification>

<success_criteria>
- NavbarControl quick-tap shows primaryContainer pressed fill (distinct from rest state)
- NavbarControl hold-progress shows primaryContainer fill with onPrimaryContainer icons
- Every checked Switch in settings (SettingsToggle and inline) uses primaryContainer track color
- CompanionSettings degraded route status uses ThemeService.warning (not tertiary)
- All 4 popup/dialog files use centralized ThemeService.surfaceTintHigh/surfaceTintHighest (no inline math)
- AAStatusWidget connected state uses ThemeService.success (semantic green)
- Tile content uses onPrimary/onPrimaryContainer (correct M3 pairing)
- Power menu pressed text uses onPrimaryContainer
- Night mode accent colors are visually comfortable in dark environment
- State matrix document covers all control types with token assignments including pressed state and active toggle
- Pi visual verification confirms legibility in both day and night modes
</success_criteria>

<output>
After completion, create `.planning/phases/14-color-audit-m3-expressive-tokens/14-02-SUMMARY.md`
</output>
