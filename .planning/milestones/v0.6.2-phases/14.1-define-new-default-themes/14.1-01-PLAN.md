---
phase: 14.1-define-new-default-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/themes/colors/theme.yaml
  - config/themes/colors/wallpaper.jpg
  - config/themes/squares/theme.yaml
  - config/themes/squares/wallpaper.jpg
  - config/themes/black/theme.yaml
  - config/themes/black/wallpaper.jpg
  - config/themes/leather/theme.yaml
  - config/themes/leather/wallpaper.jpg
  - config/themes/ferns/theme.yaml
  - config/themes/ferns/wallpaper.jpg
  - config/themes/default/theme.yaml
  - config/themes/default/wallpaper.jpg
  - config/themes/retro/theme.yaml
  - config/themes/retro/wallpaper.jpg
  - config/themes/vaporwave/theme.yaml
  - config/themes/vaporwave/wallpaper.jpg
  - config/themes/waves/theme.yaml
  - config/themes/waves/wallpaper.jpg
autonomous: false
requirements: [TM-01, TM-02]

must_haves:
  truths:
    - "All 9 promoted bundled themes are present in the theme picker and no retired bundled themes (ember, ocean, amoled) appear"
    - "First-install loads the Prodigy palette as the default theme"
    - "Each bundled theme has a wallpaper visible in the theme picker"
    - "connected-device internal theme still exists and is hidden from picker"
    - "Old themes (ember, ocean, amoled) no longer appear in theme picker"
  artifacts:
    - path: "config/themes/default/theme.yaml"
      provides: "Prodigy theme renamed as default"
      contains: "id: default"
    - path: "config/themes/colors/theme.yaml"
      provides: "Colors theme"
      contains: "id: colors"
    - path: "config/themes/squares/theme.yaml"
      provides: "Squares theme"
      contains: "id: squares"
    - path: "config/themes/black/theme.yaml"
      provides: "Black theme"
      contains: "id: black"
    - path: "config/themes/leather/theme.yaml"
      provides: "Leather theme"
      contains: "id: leather"
    - path: "config/themes/ferns/theme.yaml"
      provides: "Ferns theme"
      contains: "id: ferns"
    - path: "config/themes/retro/theme.yaml"
      provides: "Retro theme"
      contains: "id: retro"
    - path: "config/themes/vaporwave/theme.yaml"
      provides: "Vaporwave theme"
      contains: "id: vaporwave"
    - path: "config/themes/waves/theme.yaml"
      provides: "Waves theme"
      contains: "id: waves"
  key_links:
    - from: "config/themes/default/theme.yaml"
      to: "ThemeService fallback"
      via: "id: default matches ThemeService default theme lookup"
      pattern: "id: default"
    - from: "config/themes/connected-device/theme.yaml"
      to: "ThemeService AA token cache"
      via: "Preserved unchanged from before migration"
      pattern: "id: connected-device"
---

<objective>
Promote 9 visually-approved companion-created themes from the Pi into bundled `config/themes/`, replacing the 4 old defaults. Rename prodigy to default for first-install identity.

Purpose: Ship polished, M3-generated themes as the out-of-box experience instead of the hand-crafted placeholder themes.
Output: 9 theme directories in `config/themes/` with YAML + wallpaper, old themes removed, tests green.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-define-new-default-themes/14.1-CONTEXT.md
@.planning/phases/14.1-define-new-default-themes/14.1-RESEARCH.md

@config/themes/connected-device/theme.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull themes from Pi and replace bundled defaults</name>
  <files>
    config/themes/colors/theme.yaml, config/themes/colors/wallpaper.jpg,
    config/themes/squares/theme.yaml, config/themes/squares/wallpaper.jpg,
    config/themes/black/theme.yaml, config/themes/black/wallpaper.jpg,
    config/themes/leather/theme.yaml, config/themes/leather/wallpaper.jpg,
    config/themes/ferns/theme.yaml, config/themes/ferns/wallpaper.jpg,
    config/themes/default/theme.yaml, config/themes/default/wallpaper.jpg,
    config/themes/retro/theme.yaml, config/themes/retro/wallpaper.jpg,
    config/themes/vaporwave/theme.yaml, config/themes/vaporwave/wallpaper.jpg,
    config/themes/waves/theme.yaml, config/themes/waves/wallpaper.jpg
  </files>
  <action>
    1. SCP 9 theme directories from Pi (`matt@192.168.1.152:~/.openauto/themes/`) to a temp staging area.
       Themes to pull: colors, squares, black, leather, ferns, prodigy, retro, vaporwave, waves.
       Each has `theme.yaml` + `wallpaper.jpg`.

    2. Delete old bundled themes: `config/themes/default/`, `config/themes/ember/`, `config/themes/ocean/`, `config/themes/amoled/`.
       DO NOT delete `config/themes/connected-device/` -- it is the internal AA token cache.

    3. Copy pulled themes into `config/themes/`:
       - `prodigy/` -> `config/themes/default/` (rename directory)
       - All others keep their directory name as-is

    4. Edit `config/themes/default/theme.yaml`:
       - Change `id: prodigy` to `id: default`
       - Change `name: prodigy` to `name: Prodigy`

    5. Title-case the `name:` field in all other theme YAMLs:
       - colors -> Colors, squares -> Squares, black -> Black, leather -> Leather,
         ferns -> Ferns, retro -> Retro, vaporwave -> Vaporwave, waves -> Waves

    6. Resize oversized wallpapers using ImageMagick (`convert input.jpg -resize 1440x1440 -quality 85 output.jpg`):
       - squares (4320x4320) -> 1440x1440
       - leather (3712x3712) -> 1440x1440
       - ferns (3000x3000) -> 1440x1440
       - retro (2667x2667) -> 1440x1440
       Leave as-is: colors (1200x1200), black (2160x2160 -- small file), prodigy/default (1440x1440), vaporwave (1803x1803 -- moderate), waves (2000x2000 -- moderate).
       Actually, also resize black (2160x2160), vaporwave (1803x1803), and waves (2000x2000) if they're over 1440 -- all should be capped at 1440x1440 for consistency.

    7. Verify: `ls config/themes/` shows 10 directories (9 new + connected-device), no old themes (ember, ocean, amoled).
       Verify: Each new theme directory has theme.yaml + wallpaper.jpg.
       Verify: `grep "id:" config/themes/*/theme.yaml` shows correct IDs.

    DO NOT modify `tests/data/themes/default/theme.yaml` -- that test fixture validates ThemeService loading behavior with known color values. Changing it would require updating dozens of test assertions for zero benefit.
  </action>
  <verify>
    <automated>ls config/themes/ | sort && echo "---" && grep "^id:" config/themes/*/theme.yaml && echo "---" && grep "^name:" config/themes/*/theme.yaml</automated>
  </verify>
  <done>
    - 9 new theme directories exist in config/themes/ with theme.yaml + wallpaper.jpg each
    - Old themes (default/ember/ocean/amoled with hand-crafted colors) are gone
    - connected-device directory is untouched
    - default/theme.yaml has id: default, name: Prodigy
    - All theme names are title-cased
    - No wallpaper exceeds 1440x1440 pixels
  </done>
</task>

<task type="auto">
  <name>Task 2: Build, verify locally, and clean Pi user themes</name>
  <files>build/</files>
  <action>
    1. Rebuild the project: `cd build && cmake -DCMAKE_PREFIX_PATH=/opt/qt/6.8.2/gcc_64 .. && make -j$(nproc)`

    2. Run the full test suite: `cd build && ctest --output-on-failure`
       Tests use `tests/data/themes/default/theme.yaml` (unchanged test fixture), NOT production `config/themes/`.
       All tests should pass unchanged since we did not modify test data.

    3. Spot-check that each new theme YAML is parseable:
       - Each has `id:`, `name:`, `version:`, `font_family:`, `day:`, `night:` sections
       - Color values are `#ffRRGGBB` format (companion ARGB) -- QColor handles it

    4. Delete the duplicate user theme copies on the Pi that were just promoted to bundled:
       ```
       ssh matt@192.168.1.152 'rm -rf ~/.openauto/themes/colors ~/.openauto/themes/squares ~/.openauto/themes/black ~/.openauto/themes/leather ~/.openauto/themes/ferns ~/.openauto/themes/prodigy ~/.openauto/themes/retro ~/.openauto/themes/vaporwave ~/.openauto/themes/waves'
       ```
       This ensures the bundled themes are the active ones (user themes override bundled per ThemeService scan order).
       DO NOT delete other user themes that may exist (e.g. miata, purple, default).

    5. Cross-build for Pi: `./cross-build.sh`

    6. Deploy to Pi (git push requires user approval per workspace rules):
       ```
       rsync -av build-pi/src/openauto-prodigy matt@192.168.1.152:~/openauto-prodigy/build/src/
       git push  # user approval required
       ssh matt@192.168.1.152 'cd ~/openauto-prodigy && git pull && sudo systemctl restart openauto-prodigy.service'
       ```

    If any test or build fails, diagnose whether it's related to the theme migration or a pre-existing issue.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake -DCMAKE_PREFIX_PATH=/opt/qt/6.8.2/gcc_64 .. -DCMAKE_BUILD_TYPE=Debug 2>&1 | tail -5 && make -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Full test suite passes (86+ tests)
    - Build completes without errors
    - Duplicate user themes removed from Pi
    - Cross-build succeeds
    - Binary + themes deployed to Pi
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Pi visual verification of bundled themes</name>
  <files></files>
  <action>
    Present checkpoint for human verification on Pi hardware.

    **What to verify on the Pi:**

    1. Settings > Theme picker includes all 9 promoted themes with title-cased names (additional user themes like miata/purple may also appear — that's expected)
    2. No retired bundled themes visible (ember, ocean, amoled should be gone)
    3. Cycle through all 9 themes — each loads wallpaper and colors correctly
    4. Restart the app (`sudo systemctl restart openauto-prodigy.service`) — default theme (Prodigy) loads on startup
    5. Delete config to simulate fresh install (`rm ~/.openauto/config.yaml && sudo systemctl restart openauto-prodigy.service`) — verify Prodigy colors appear as default

    **Deploy command (if not already deployed):**
    ```
    ./cross-build.sh
    rsync -av build-pi/src/openauto-prodigy matt@192.168.1.152:~/openauto-prodigy/build/src/
    git push  # user approval required
    ssh matt@192.168.1.152 'cd ~/openauto-prodigy && git pull && sudo systemctl restart openauto-prodigy.service'
    ```
  </action>
  <verify>
    Human confirms themes load correctly on Pi hardware.
  </verify>
  <done>
    - All 9 bundled themes verified on Pi
    - Default/Prodigy loads as first-install theme
    - No old themes visible in picker
  </done>
</task>

</tasks>

<verification>
- `ls config/themes/` shows: black, colors, connected-device, default, ferns, leather, retro, squares, vaporwave, waves (10 dirs)
- `grep "^id:" config/themes/*/theme.yaml` shows 10 unique IDs matching directory names
- `grep "^name:" config/themes/*/theme.yaml` shows title-cased names for all 9 user-facing themes
- `ctest --output-on-failure` all green
- No wallpaper.jpg exceeds 1440x1440 pixels
</verification>

<success_criteria>
- 9 companion-created themes are bundled in config/themes/ replacing the 4 old defaults
- Prodigy theme is the new default (id: default, name: Prodigy)
- connected-device internal theme is preserved
- All wallpapers optimized to max 1440x1440
- Full test suite passes
- Duplicate user themes cleaned from Pi (~/.openauto/themes/)
- Cross-build succeeds and binary deployed to Pi
- Pi visual verification confirms all 9 themes load correctly from bundled path
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-define-new-default-themes/14.1-01-SUMMARY.md`
</output>
