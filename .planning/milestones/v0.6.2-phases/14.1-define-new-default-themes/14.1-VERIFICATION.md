---
phase: 14.1-define-new-default-themes
verified: 2026-03-15T22:55:00Z
status: human_needed
score: 5/5 must-haves verified
re_verification: false
human_verification:
  - test: "Open Settings > Theme picker on Pi hardware, cycle through all 9 themes"
    expected: "All 9 bundled themes appear with title-cased names (Black, Colors, Ferns, Leather, Prodigy, Retro, Squares, Vaporwave, Waves). No ember, ocean, or amoled entries. Each theme loads its wallpaper and color palette correctly."
    why_human: "Requires Pi display and touch to navigate the settings UI. Theme picker rendering and wallpaper loading cannot be verified programmatically."
  - test: "Delete ~/.openauto/config.yaml on Pi, restart openauto-prodigy.service, observe startup theme"
    expected: "App starts with Prodigy (indigo/violet) colors — the default/Prodigy theme — not a grey or neutral fallback."
    why_human: "Fresh-install default theme identity requires actually deleting state and restarting on Pi hardware."
  - test: "Open theme picker, scroll through all 9 items"
    expected: "ListView scrolls smoothly without GPU freeze or stutter. No delegate shadows cause lag on Pi."
    why_human: "GPU freeze from MultiEffect shadows was the reason shadows were removed (commit e3594f9). Must verify the fix holds on Pi's VideoCore GPU."
---

# Phase 14.1: Define New Default Themes — Verification Report

**Phase Goal:** Promote 9 visually-approved companion-created themes from the Pi into bundled `config/themes/`, replacing the 4 old defaults. Prodigy theme becomes the new default.
**Verified:** 2026-03-15T22:55:00Z
**Status:** HUMAN_NEEDED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 9 promoted bundled themes present, no retired themes (ember, ocean, amoled) | VERIFIED | `ls config/themes/` = black, colors, connected-device, default, ferns, leather, retro, squares, vaporwave, waves. grep for ember/ocean/amoled returns nothing. |
| 2 | First-install loads Prodigy palette as default theme | VERIFIED | `main.cpp:215` — `if (savedTheme.isEmpty()) savedTheme = "default"`. `config/themes/default/theme.yaml` has `id: default`, `name: Prodigy`, full indigo M3 palette. |
| 3 | Each bundled theme has a wallpaper visible in the theme picker | VERIFIED | All 9 user-facing theme directories contain `wallpaper.jpg`. Dimensions confirmed via PIL: 8 at 1440x1440, colors at 1200x1200 (explicitly approved in plan as "leave as-is"). |
| 4 | connected-device internal theme exists and is hidden from picker | VERIFIED | `config/themes/connected-device/theme.yaml` present (103 lines). `ThemeService.cpp:161` — `if (id == "connected-device") continue;` excludes it from `availableThemes_` list shown in picker. |
| 5 | Old themes (ember, ocean, amoled) no longer appear in theme picker | VERIFIED | Directories do not exist in `config/themes/`. No references to these IDs in codebase. |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `config/themes/default/theme.yaml` | Prodigy theme, id: default | VERIFIED | `id: default`, `name: Prodigy`, `seed: "#5050F8"`, 75 lines full M3 palette, day+night color maps |
| `config/themes/colors/theme.yaml` | Colors theme, id: colors | VERIFIED | `id: colors`, `name: Colors`, 75 lines |
| `config/themes/squares/theme.yaml` | Squares theme, id: squares | VERIFIED | `id: squares`, `name: Squares`, 75 lines |
| `config/themes/black/theme.yaml` | Black theme, id: black | VERIFIED | `id: black`, `name: Black`, 75 lines |
| `config/themes/leather/theme.yaml` | Leather theme, id: leather | VERIFIED | `id: leather`, `name: Leather`, 75 lines |
| `config/themes/ferns/theme.yaml` | Ferns theme, id: ferns | VERIFIED | `id: ferns`, `name: Ferns`, 75 lines |
| `config/themes/retro/theme.yaml` | Retro theme, id: retro | VERIFIED | `id: retro`, `name: Retro`, 75 lines |
| `config/themes/vaporwave/theme.yaml` | Vaporwave theme, id: vaporwave | VERIFIED | `id: vaporwave`, `name: Vaporwave`, 75 lines |
| `config/themes/waves/theme.yaml` | Waves theme, id: waves | VERIFIED | `id: waves`, `name: Waves`, 75 lines |
| `config/themes/connected-device/theme.yaml` | Preserved unchanged | VERIFIED | `id: connected-device`, 103 lines (AA token cache fields) |
| `config/themes/default/wallpaper.jpg` | Prodigy wallpaper, max 1440x1440 | VERIFIED | 1440x1440 confirmed via PIL |
| `qml/controls/FullScreenPicker.qml` | No delegate shadows, full-screen dialog, scrollable list | VERIFIED | Line 245: `// Background (no shadow — too many delegates for Pi GPU)`. Dialog x:0 y:0 width:parent.width height:parent.height. ListView with clip:true, boundsBehavior:StopAtBounds. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `config/themes/default/theme.yaml` | ThemeService fallback | `id: default` matches `main.cpp:215` hardcoded fallback | WIRED | `main.cpp:215` — `if (savedTheme.isEmpty()) savedTheme = "default"`. ThemeService `setTheme("default")` looks up `themeDirectories_["default"]` which resolves to `config/themes/default/`. |
| `config/themes/connected-device/theme.yaml` | ThemeService AA token cache | `id: connected-device` excluded from picker, registered in `themeDirectories_` | WIRED | `ThemeService.cpp:160-161` — directory registered before `continue` skip. Lines 361-441 use `themeDirectories_["connected-device"]` for AA token persistence. |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TM-01 | 14.1-01-PLAN.md | 9 companion-created themes promoted from Pi user directory to bundled `config/themes/`, replacing old defaults | SATISFIED | 9 theme directories verified. Ember/ocean/amoled absent. All have full M3 YAML + wallpaper. |
| TM-02 | 14.1-01-PLAN.md | Prodigy theme renamed to `default` (first-install theme identity) | SATISFIED | `config/themes/default/theme.yaml`: `id: default`, `name: Prodigy`. `main.cpp:215` loads `"default"` on empty config. |

No orphaned requirements. REQUIREMENTS.md traceability table maps TM-01 and TM-02 exclusively to Phase 14.1 — both accounted for.

---

### Anti-Patterns Found

No anti-patterns found in modified files. Scanned:
- `config/themes/*/theme.yaml` — substantive M3 palettes, no placeholder values
- `qml/controls/FullScreenPicker.qml` — no TODO/FIXME, no empty handlers, shadow removal documented with comment

---

### Human Verification Required

#### 1. Theme picker completeness on Pi

**Test:** On the Pi, open Settings and navigate to the theme picker. Count and name all visible themes.
**Expected:** Exactly 9 user-facing themes appear: Black, Colors, Ferns, Leather, Prodigy, Retro, Squares, Vaporwave, Waves (plus any user themes that survive in ~/.openauto/themes/ — those are expected). Ember, Ocean, Amoled must not appear.
**Why human:** QML picker rendering requires the Pi display. The ThemeService scan and model-to-view binding cannot be confirmed by grep alone.

#### 2. Fresh-install default theme identity

**Test:** `ssh matt@192.168.1.152 'rm ~/.openauto/config.yaml && sudo systemctl restart openauto-prodigy.service'`, then observe the UI color scheme.
**Expected:** App starts with Prodigy's indigo/violet color palette (primary ~`#3531e0` day / `#c1c1ff` night), not a grey or neutral fallback.
**Why human:** Requires deleting live config on hardware and observing the visual result.

#### 3. FullScreenPicker scroll performance with 9 items

**Test:** Open the theme picker on Pi, scroll through all 9 entries.
**Expected:** Smooth scrolling, no GPU stutter or freeze. The shadow removal fix (commit e3594f9) should hold.
**Why human:** GPU freeze from MultiEffect delegate shadows is a Pi-specific hardware constraint (VideoCore). Cannot be reproduced on the dev VM with VMware SVGA.

---

### Summary

All 5 observable truths are fully verified in the codebase. The 10 theme directories (9 user-facing + connected-device) exist with substantive YAML files (75 lines each, complete M3 day+night palettes) and wallpapers capped at 1440x1440. The ThemeService fallback to `"default"` is hardcoded in `main.cpp:215` and the Prodigy theme is correctly installed at that ID. connected-device is registered but excluded from the picker via `ThemeService.cpp:161`. The FullScreenPicker has shadows removed, is full-screen, and has a scrollable delegate list. All 5 commits from the SUMMARY (`4180b13`, `4aa207b`, `e3594f9`, `f0d63d5`, `508adfe`) exist in git history. 87/87 tests pass.

The only outstanding items are Pi hardware checks that cannot be done programmatically: visual theme picker completeness, fresh-install default identity, and GPU scroll performance. These are not blocking defects — the code is correct — but they confirm the end-to-end user experience.

---

_Verified: 2026-03-15T22:55:00Z_
_Verifier: Claude (gsd-verifier)_
