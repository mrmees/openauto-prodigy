---
phase: 02-settings-restructuring
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - qml/applications/settings/AASettings.qml
  - qml/applications/settings/ConnectivitySettings.qml
  - qml/applications/settings/AudioSettings.qml
  - qml/applications/settings/SystemSettings.qml
  - qml/applications/settings/SettingsMenu.qml
autonomous: false
requirements: [SET-03, SET-04, SET-05, SET-06, SET-07, SET-08]

must_haves:
  truths:
    - "Android Auto tile opens a page with resolution, FPS, DPI, codecs, sidebar, auto-connect, and protocol capture controls"
    - "Connectivity tile opens a page with WiFi AP and Bluetooth controls (no AA controls)"
    - "Audio tile opens a page with volume, output device, mic, input device, and an EQ section entry"
    - "System tile opens a page with identity, hardware info, version, and close app button (merged About)"
    - "Every existing setting is reachable in the new hierarchy — no setting lost"
    - "All config paths are unchanged — existing config.yaml files work without migration"
  artifacts:
    - path: "qml/applications/settings/AASettings.qml"
      provides: "Android Auto category page composing controls from VideoSettings + ConnectionSettings"
      contains: "saveCodecConfig"
    - path: "qml/applications/settings/ConnectivitySettings.qml"
      provides: "Connectivity page with WiFi AP + BT only (AA controls removed)"
      contains: "WiFi Access Point"
    - path: "qml/applications/settings/SystemSettings.qml"
      provides: "System page with identity + hardware + About content merged"
      contains: "Close App"
    - path: "qml/applications/settings/SettingsMenu.qml"
      provides: "Updated page Component map pointing to new category pages"
      contains: "aaPage"
  key_links:
    - from: "qml/applications/settings/AASettings.qml"
      to: "CodecCapabilityModel"
      via: "Repeater model + saveCodecConfig/saveDecoderConfig functions"
      pattern: "CodecCapabilityModel"
    - from: "qml/applications/settings/SettingsMenu.qml"
      to: "qml/applications/settings/AASettings.qml"
      via: "Component { id: aaPage; AASettings {} }"
      pattern: "AASettings"
---

<objective>
Create the 6 category pages by reorganizing controls from existing settings files.

Purpose: Every setting is reachable through its logical category tile. Controls move between pages but keep their config keys and behavior. No setting is lost, no config migration needed.
Output: AASettings.qml (new), modified ConnectivitySettings, AudioSettings, SystemSettings. SettingsMenu updated to reference new pages.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-restructuring/02-RESEARCH.md
@.planning/phases/02-settings-restructuring/02-01-SUMMARY.md

@qml/applications/settings/VideoSettings.qml
@qml/applications/settings/ConnectionSettings.qml
@qml/applications/settings/AudioSettings.qml
@qml/applications/settings/SystemSettings.qml
@qml/applications/settings/AboutSettings.qml
@qml/applications/settings/SettingsMenu.qml
@docs/settings-tree.md

<interfaces>
<!-- Control migration map — controls move between pages, config keys unchanged -->

VideoSettings.qml → AASettings.qml:
  - SectionHeader "Playback" + FPS SegmentedButton + Resolution FullScreenPicker + DPI SettingsSlider
  - SectionHeader "Video Decoding" + Codec Repeater (entire block)
  - SectionHeader "Sidebar" + sidebar toggle + position SegmentedButton + width SettingsSlider
  - decoderPickerDialog Dialog (entire block)
  - codecDisplayName() function
  - saveCodecConfig() function
  - saveDecoderConfig() function
  - Component.onCompleted codec config loading

ConnectionSettings.qml → AASettings.qml:
  - SectionHeader "Android Auto" → rename to "Connection"
  - Auto-connect SettingsToggle (configPath: "connection.auto_connect_aa")
  - TCP Port ReadOnlyField (configPath: "connection.tcp_port")
  - SectionHeader "Protocol Capture" + 4 controls

ConnectionSettings.qml → ConnectivitySettings.qml:
  - SectionHeader "WiFi Access Point" + Channel FullScreenPicker + Band SegmentedButton
  - SectionHeader "Bluetooth" + BT name ReadOnlyField + pairable toggle + paired devices Repeater

AboutSettings.qml → SystemSettings.qml:
  - "OpenAuto Prodigy" heading Text
  - Version Text
  - Close App Button + ExitDialog

DisplaySettings.qml: UNCHANGED (already correct grouping for SET-04)
CompanionSettings.qml: UNCHANGED (already correct grouping for SET-07)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AASettings.qml and trim ConnectivitySettings.qml</name>
  <files>qml/applications/settings/AASettings.qml, qml/applications/settings/ConnectivitySettings.qml</files>
  <action>
**AASettings.qml — New file, composing controls from VideoSettings + ConnectionSettings:**

Create a new Flickable-based settings page following the same pattern as existing pages:
```
import QtQuick
import QtQuick.Layouts
import QtQuick.Controls
```

Structure (Flickable > ColumnLayout):

1. **SectionHeader "Playback"**
   - FPS SegmentedButton (from VideoSettings, configPath: "video.fps", options: ["30","60"], values: [30,60])
   - Resolution FullScreenPicker (configPath: "video.resolution", options/values: 480p/720p/1080p)
   - DPI SettingsSlider (configPath: "video.dpi", from:80 to:400 stepSize:10, restartRequired:true)

2. **SectionHeader "Video Decoding"**
   - Copy the ENTIRE codec Repeater block from VideoSettings.qml verbatim (lines 43-227)
   - This includes the codec toggle, hw/sw button, decoder name selector, and separators

3. **SectionHeader "Sidebar"**
   - Sidebar toggle (configPath: "video.sidebar.enabled", restartRequired: true)
   - Position SegmentedButton (configPath: "video.sidebar.position", Left/Right)
   - Width SettingsSlider (configPath: "video.sidebar.width", from:80 to:300 stepSize:10, restartRequired:true)

4. **SectionHeader "Connection"** (was "Android Auto" in ConnectionSettings)
   - Auto-connect SettingsToggle (configPath: "connection.auto_connect_aa")
   - TCP Port ReadOnlyField (configPath: "connection.tcp_port", placeholder: "5277")

5. **SectionHeader "Debug"** (was "Protocol Capture")
   - Enable Capture SettingsToggle (configPath: "connection.protocol_capture.enabled")
   - Format SegmentedButton (configPath: "connection.protocol_capture.format", JSONL/TSV)
   - Include Media Frames SettingsToggle (configPath: "connection.protocol_capture.include_media")
   - Capture Path ReadOnlyField (configPath: "connection.protocol_capture.path")

6. **The decoderPickerDialog** — copy the entire Dialog block from VideoSettings.qml

7. **Helper functions** — copy `codecDisplayName()`, `saveCodecConfig()`, `saveDecoderConfig()` from VideoSettings

8. **Component.onCompleted** — copy the codec config loading block from VideoSettings

CRITICAL: Every configPath must match the original exactly. Do not change any config keys.

**ConnectivitySettings.qml — Remove AA controls, keep WiFi + BT only:**

Remove from the top of the file:
- SectionHeader "Android Auto"
- Auto-connect SettingsToggle
- TCP Port ReadOnlyField
- SectionHeader "Protocol Capture" and all 4 protocol capture controls

Keep:
- SectionHeader "WiFi Access Point" + Channel picker + Band segmented button
- SectionHeader "Bluetooth" + BT name ReadOnlyField + pairable toggle + paired devices Repeater

The WiFi section becomes the first section in the page. Ensure the Flickable/ColumnLayout wrapper stays intact.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && test -f qml/applications/settings/AASettings.qml && grep -q "saveCodecConfig" qml/applications/settings/AASettings.qml && grep -q "connection.auto_connect_aa" qml/applications/settings/AASettings.qml && ! grep -q "auto_connect_aa" qml/applications/settings/ConnectivitySettings.qml && grep -q "WiFi Access Point" qml/applications/settings/ConnectivitySettings.qml && echo "PASS"</automated>
  </verify>
  <done>AASettings.qml contains all AA-related controls (video playback, codecs, sidebar, connection, debug). ConnectivitySettings.qml contains only WiFi AP and Bluetooth controls. All config paths preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Merge About into SystemSettings + AudioSettings EQ stub + SettingsMenu wiring</name>
  <files>qml/applications/settings/SystemSettings.qml, qml/applications/settings/AudioSettings.qml, qml/applications/settings/SettingsMenu.qml</files>
  <action>
**SystemSettings.qml — Merge About content:**

After the existing "Hardware" section and its controls, add:

1. **SectionHeader "About"**

2. App name and version (from AboutSettings.qml):
   ```qml
   ColumnLayout {
       Layout.fillWidth: true
       Layout.topMargin: UiMetrics.marginRow
       spacing: UiMetrics.marginRow

       Text {
           text: "OpenAuto Prodigy"
           font.pixelSize: UiMetrics.fontHeading
           font.bold: true
           color: ThemeService.normalFontColor
       }
       Text {
           text: "Version " + (ConfigService.value("identity.sw_version") || "0.0.0")
           font.pixelSize: UiMetrics.fontBody
           color: ThemeService.descriptionFontColor
       }
   }
   ```

3. Close App button (from AboutSettings.qml):
   - Copy the Button with its contentItem and background Rectangle
   - Include a spacer Item before it for padding

4. ExitDialog — copy the ExitDialog component from AboutSettings.qml:
   ```qml
   ExitDialog {
       id: exitDialog
       parent: Overlay.overlay
       anchors.centerIn: parent
   }
   ```

**AudioSettings.qml — Add EQ section entry:**

After the existing Microphone section (after the inputPicker and restartBanner), add:

1. **SectionHeader "Equalizer"**

2. A tappable row that opens EqSettings via the SettingsMenu StackView:
   ```qml
   SettingsListItem {
       icon: "\ue429"
       label: {
           if (typeof EqualizerService === "undefined") return "Equalizer"
           var preset = EqualizerService.activePresetName(0)
           return preset !== "" ? "Equalizer — " + preset : "Equalizer"
       }
       onClicked: {
           // Push EqSettings onto the SettingsMenu's StackView
           // Access via the StackView.view attached property
           var sv = StackView.view
           if (sv) {
               sv.push(eqPageComponent)
               ApplicationController.setTitle("Settings > Audio > Equalizer")
           }
       }
   }

   Component {
       id: eqPageComponent
       EqSettings {}
   }
   ```

Note: This uses the same depth-3 navigation pattern established in Phase 03-head-unit-eq-ui (milestone v0.4.1). The SettingsListItem control already has press feedback from Phase 1. If SettingsListItem isn't available in AudioSettings context, use a custom tappable Item with MaterialIcon + Text + chevron icon, matching the style from the old settings list.

**SettingsMenu.qml — Update page Components and openPage function:**

1. Replace `Component { id: videoPage; VideoSettings {} }` with `Component { id: aaPage; AASettings {} }`
2. Keep `Component { id: connectionPage; ConnectionSettings {} }` — same component, ConnectivitySettings is the same file just trimmed
3. Replace `Component { id: aboutPage; AboutSettings {} }` — remove this entirely (About is merged into System)

4. Update the `openPage()` function's `titles` map:
   - Change `"video"` key to `"aa"` with title `"Android Auto"`
   - Change `"connection"` title to `"Connectivity"`
   - Remove `"about"` entry
   - Keep display, audio, system, companion unchanged

5. Update the `pages` map:
   - Change `"video": videoPage` to `"aa": aaPage`
   - Remove `"about": aboutPage`
   - Keep all other entries

6. Update the tile grid from 02-01:
   - Android Auto tile: change `openPage("video")` to `openPage("aa")`
   - Connectivity tile: keep `openPage("connection")` (the page Component ref is unchanged)

7. Update the `onBackRequested` handler — when popping, set title based on depth:
   - If `settingsStack.depth > 1` after pop, check if we're back to a category page and set appropriate title
   - If `settingsStack.depth === 1`, set title to "Settings" (the tile grid)
   - This handles depth-3 navigation (Settings > Audio > Equalizer) correctly

**VideoSettings.qml and AboutSettings.qml:** These files are now unused. Do NOT delete them — leave them in place. They can be cleaned up in a future housekeeping pass. The SettingsMenu no longer references them.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -q "aaPage" qml/applications/settings/SettingsMenu.qml && grep -q "About" qml/applications/settings/SystemSettings.qml && grep -q "ExitDialog" qml/applications/settings/SystemSettings.qml && grep -q "Equalizer" qml/applications/settings/AudioSettings.qml && ! grep -q "aboutPage" qml/applications/settings/SettingsMenu.qml && echo "PASS"</automated>
  </verify>
  <done>SystemSettings contains About content (version + close app). AudioSettings has EQ section entry. SettingsMenu wired to AASettings, no longer references VideoSettings or AboutSettings. All 6 category tiles navigate to correct restructured pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify settings restructuring on Pi</name>
  <files></files>
  <action>
Complete settings restructuring: 6-tile grid landing page with live subtitles, 6 reorganized category pages (Android Auto, Display, Audio, Connectivity, Companion, System), read-only field UX improvement. Every existing setting reachable in the new hierarchy.

Build and run on Pi (or dev VM):
1. Open Settings — should see 3x2 tile grid with icons and status subtitles
2. Tap "Android Auto" — verify resolution, FPS, DPI, codecs, sidebar, auto-connect, protocol capture are all present
3. Tap back, tap "Display" — verify brightness, theme, wallpaper, orientation, day/night controls
4. Tap back, tap "Audio" — verify volume, output device, mic gain, input device, EQ entry
5. Tap back, tap "Connectivity" — verify WiFi AP channel/band and Bluetooth controls (NO AA controls)
6. Tap back, tap "Companion" — verify status, pairing, config controls
7. Tap back, tap "System" — verify identity, hardware, About heading, version, Close App button
8. Verify tile subtitles show live data (theme name, volume level, BT device, version)
9. Verify read-only fields show muted values without "Edit via web panel" text
10. Verify back navigation returns to tile grid from any category page
  </action>
  <verify>
    <automated>echo "Human verification required"</automated>
  </verify>
  <done>All 6 category pages verified with correct controls, live subtitles, and proper navigation</done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd build && cmake --build . -j$(nproc)`
2. AASettings.qml exists and contains all video+connection controls
3. ConnectivitySettings.qml has WiFi+BT only (no AA controls)
4. SystemSettings.qml has About content (version, close app, ExitDialog)
5. AudioSettings.qml has EQ section entry
6. SettingsMenu.qml references aaPage, not videoPage/aboutPage
7. Cross-reference with docs/settings-tree.md: every control listed in the tree is present in exactly one category page
</verification>

<success_criteria>
- All 6 category pages contain the correct controls per SET-03 through SET-08
- No setting is lost or orphaned (verified against settings-tree.md)
- All config paths unchanged (SET-09)
- Build succeeds on dev VM
- Human verification on Pi confirms navigation and controls work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-restructuring/02-02-SUMMARY.md`
</output>
