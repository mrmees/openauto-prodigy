---
phase: 04-visual-depth
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - qml/controls/Tile.qml
  - qml/controls/SettingsListItem.qml
  - qml/controls/SegmentedButton.qml
  - qml/controls/FullScreenPicker.qml
  - qml/components/ExitDialog.qml
  - qml/components/GestureOverlay.qml
  - qml/components/PairingDialog.qml
  - qml/components/NavbarPopup.qml
  - qml/applications/settings/SystemSettings.qml
  - qml/applications/settings/AboutSettings.qml
  - qml/applications/settings/ConnectionSettings.qml
  - qml/applications/bt_audio/BtAudioView.qml
  - qml/applications/phone/PhoneView.qml
  - qml/applications/phone/IncomingCallOverlay.qml
autonomous: true
requirements: ["STY-01"]
must_haves:
  truths:
    - "All tappable elements use M3 button components with MultiEffect shadows"
    - "No opacity: 0.85 press feedback remains anywhere in QML"
    - "Popup panels and dialogs use Level 2 elevation with surface tint"
    - "Press state shows scale + shadow reduction (no opacity dim)"
  artifacts:
    - path: "qml/controls/Tile.qml"
      provides: "Launcher tile delegating to FilledButton pattern with shadow"
    - path: "qml/controls/SettingsListItem.qml"
      provides: "Settings row with shadow, no opacity dim"
    - path: "qml/components/ExitDialog.qml"
      provides: "Exit dialog with M3 button components and Level 2 elevation"
    - path: "qml/components/GestureOverlay.qml"
      provides: "Gesture overlay panel with Level 2 elevation + surface tint"
  key_links:
    - from: "qml/controls/Tile.qml"
      to: "qml/controls/ElevatedButton.qml"
      via: "component usage or pattern adoption"
      pattern: "MultiEffect|ElevatedButton|FilledButton"
    - from: "qml/components/ExitDialog.qml"
      to: "qml/controls/ElevatedButton.qml"
      via: "button components for dialog actions"
      pattern: "ElevatedButton|FilledButton|OutlinedButton"
---

<objective>
Migrate all existing button/interactive patterns across the codebase to use the M3 button components or adopt their shadow pattern directly. Remove all opacity-dim press feedback. Apply Level 2 elevation to popup panels and dialogs.

Purpose: Consistent visual depth across the entire interactive surface -- every tappable element has shadow, every press shows depth change.
Output: 14 QML files updated with M3 depth treatment, zero opacity-dim patterns remaining.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-depth/04-RESEARCH.md
@.planning/phases/04-visual-depth/04-02-SUMMARY.md

<interfaces>
<!-- Button components created in Plan 02 -->

ElevatedButton.qml (general purpose raised button):
- Properties: text, iconCode, buttonColor, pressedColor, textColor, pressedTextColor, buttonScale, buttonEnabled, buttonRadius, elevation
- Signals: clicked(), pressAndHold()
- Default: surfaceContainerLow bg, primary text, elevation 1

FilledButton.qml (primary action button):
- Same properties as ElevatedButton
- Default: primary bg, onPrimary text, elevation 1

OutlinedButton.qml (secondary/cancel button):
- Same properties as ElevatedButton
- Default: transparent bg, primary text, outline border, elevation 0

<!-- Migration targets from RESEARCH.md -->

Opacity dim removal targets (6 locations):
- Tile.qml:17 — opacity: pressed ? 0.85 : (enabled ? 1.0 : 0.5)
- SettingsListItem.qml:16 — opacity: pressed ? 0.85 : 1.0
- SegmentedButton.qml:69 — opacity: pressed ? 0.85 : 1.0
- FullScreenPicker.qml:67,195 — opacity: pressed ? 0.85 : 1.0
- ConnectionSettings.qml:144 — opacity: pressed ? 0.85 : 1.0

Dialog/popup button migrations (26+ button instances across 8 files):
- ExitDialog.qml (3 buttons), GestureOverlay.qml (3 buttons)
- SystemSettings.qml (7 buttons), AboutSettings.qml (1 button)
- PhoneView.qml (5 buttons), IncomingCallOverlay.qml (2 buttons)
- BtAudioView.qml (3 buttons), NavbarPopup.qml (1 button)

Elevation Level 2 targets (panels/dialogs):
- NavbarPopup panel, GestureOverlay panel
- ExitDialog, PairingDialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate control components (Tile, SettingsListItem, SegmentedButton, FullScreenPicker)</name>
  <files>qml/controls/Tile.qml, qml/controls/SettingsListItem.qml, qml/controls/SegmentedButton.qml, qml/controls/FullScreenPicker.qml, qml/applications/settings/ConnectionSettings.qml</files>
  <action>
For each control, add `import QtQuick.Effects` and integrate MultiEffect shadow. Do NOT wrap these in ElevatedButton/FilledButton -- they have complex internal layouts that don't fit a simple button wrapper. Instead, adopt the shadow pattern directly:

**Tile.qml:**
1. Add `import QtQuick.Effects`
2. Remove `opacity: mouseArea.pressed && tileEnabled ? 0.85 : (tileEnabled ? 1.0 : 0.5)` from the root Item
3. Keep disabled state: set `bg.opacity: tileEnabled ? 1.0 : 0.5` on the background Rectangle
4. Add MultiEffect as sibling of bg Rectangle with Level 1 shadow values:
   - Resting: `shadowBlur: 0.35, shadowVerticalOffset: 2, shadowOpacity: 0.25`
   - Pressed: `shadowBlur: 0.15, shadowVerticalOffset: 1, shadowOpacity: 0.10`
   - `shadowColor: ThemeService.shadow`
   - Animate shadow properties with `Behavior { NumberAnimation { duration: UiMetrics.animDurationFast } }`
5. Add state layer Rectangle inside bg (anchors.fill, same radius, `color: ThemeService.onSurface`, `opacity: mouseArea.pressed ? 0.10 : 0.0`)
6. Keep existing scale animation (0.95)
7. Keep existing primaryContainer color swap on press

**SettingsListItem.qml:**
1. Add `import QtQuick.Effects`
2. Remove `opacity: mouseArea.pressed ? 0.85 : 1.0`
3. Add MultiEffect shadow (Level 1, same values as Tile)
4. Add state layer
5. Keep existing scale (0.97)

**SegmentedButton.qml:**
1. Add `import QtQuick.Effects`
2. Remove `opacity: segMouseArea.pressed ? 0.85 : 1.0` from each segment delegate
3. Add MultiEffect shadow on the overall button container (NOT per-segment -- one shadow for the whole segmented control)
4. Add state layer per segment
5. Keep existing secondaryContainer selected state

**FullScreenPicker.qml:**
1. Add `import QtQuick.Effects`
2. Remove both `opacity: pressed ? 0.85 : 1.0` patterns (lines 67 and 195)
3. Add shadow to the main picker row (Level 1)
4. Add shadow to each delegate item (Level 1)
5. Add state layers
6. Keep existing scale (0.97)

**ConnectionSettings.qml:**
1. Remove `opacity: forgetArea.pressed ? 0.85 : 1.0` (line 144)
2. Add MultiEffect shadow + state layer on the forget button
3. Add `import QtQuick.Effects`

After all changes, verify no `0.85` opacity values remain: `grep -rn "0\.85" qml/`
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -rn "0\.85" qml/ | grep -v "//"; cd build && cmake --build . -j$(nproc) 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 control files have MultiEffect shadows, zero opacity-dim press patterns remain, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dialog/popup buttons and apply panel elevation</name>
  <files>qml/components/ExitDialog.qml, qml/components/GestureOverlay.qml, qml/components/PairingDialog.qml, qml/components/NavbarPopup.qml, qml/applications/settings/SystemSettings.qml, qml/applications/settings/AboutSettings.qml, qml/applications/bt_audio/BtAudioView.qml, qml/applications/phone/PhoneView.qml, qml/applications/phone/IncomingCallOverlay.qml</files>
  <action>
**Dialog/popup buttons:** Replace Qt Controls Button instances that use custom `background: Rectangle { ... pressed ? color1 : color2 }` with the new M3 button components where possible. For buttons that need specific sizing or layout, adopt the shadow pattern directly.

Migration mapping:
- Primary actions (Save, Confirm, Exit, Yes): use **FilledButton**
- Cancel/No/secondary actions: use **OutlinedButton**
- General interactive buttons: use **ElevatedButton**

**ExitDialog.qml (3 buttons):**
- Replace custom Button backgrounds with FilledButton (exit/restart) and OutlinedButton (cancel)
- Dialog panel: add MultiEffect shadow at Level 2 (`shadowBlur: 0.50, shadowVerticalOffset: 4, shadowOpacity: 0.30`)
- Add surface tint to dialog bg: `Qt.rgba(ThemeService.surfaceContainerHigh.r * 0.95 + ThemeService.primary.r * 0.05, ...g..., ...b..., 1.0)`

**GestureOverlay.qml (3 buttons):**
- Replace button backgrounds with ElevatedButton components
- Panel: Level 2 shadow + surface tint on the overlay panel itself

**PairingDialog.qml:**
- Panel: Level 2 shadow + surface tint
- Migrate any buttons to M3 components

**NavbarPopup.qml (power menu button):**
- Panel: Level 2 shadow + surface tint
- Replace 1px border with shadow (remove `border.width: 1; border.color: ThemeService.outlineVariant`)
- Power menu button: use ElevatedButton

**SystemSettings.qml (7 buttons):**
- Replace all Button custom backgrounds with appropriate M3 button components
- Restart/shutdown = FilledButton, other actions = ElevatedButton

**AboutSettings.qml (1 button):**
- Replace with ElevatedButton

**BtAudioView.qml (3 buttons):**
- Playback controls: ElevatedButton with iconCode
- Keep existing layout, replace backgrounds

**PhoneView.qml (5 buttons):**
- Dial/call = FilledButton, keypad digits = ElevatedButton, hang up = FilledButton with error color

**IncomingCallOverlay.qml (2 buttons):**
- Accept = FilledButton (success color), Decline = FilledButton (error color)

**Surface tint pattern** for Level 2 panels:
```qml
readonly property color tintedSurface: Qt.rgba(
    ThemeService.surfaceContainerHigh.r * 0.95 + ThemeService.primary.r * 0.05,
    ThemeService.surfaceContainerHigh.g * 0.95 + ThemeService.primary.g * 0.05,
    ThemeService.surfaceContainerHigh.b * 0.95 + ThemeService.primary.b * 0.05,
    1.0
)
```

After all changes, verify no `parent.pressed ? ` color swap patterns remain in the migrated files.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>All dialog/popup buttons migrated to M3 components, panels have Level 2 elevation with surface tint, build succeeds, tests pass</done>
</task>

</tasks>

<verification>
- `grep -rn "0\.85" qml/` returns no press-feedback opacity patterns
- `grep -rn "parent.pressed" qml/components/ExitDialog.qml qml/components/GestureOverlay.qml` returns no matches
- Build succeeds, all tests pass
</verification>

<success_criteria>
Every tappable element has MultiEffect shadow with press-responsive depth. All opacity-dim patterns removed. Popup panels and dialogs at Level 2 elevation with surface tint.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-depth/04-03-SUMMARY.md`
</output>
