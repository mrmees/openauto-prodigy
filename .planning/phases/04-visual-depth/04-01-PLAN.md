---
phase: 04-visual-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CMakeLists.txt
  - src/core/aa/DmaBufVideoBuffer.cpp
  - src/core/aa/DmaBufVideoBuffer.hpp
  - src/core/aa/VideoDecoder.cpp
  - src/core/aa/VideoFramePool.hpp
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dev VM builds against Qt 6.8 (not system Qt 6.4)"
    - "All QT_VERSION guards are removed from source"
    - "CMake enforces Qt 6.8 minimum version"
    - "All 65+ existing tests pass on Qt 6.8"
  artifacts:
    - path: "src/CMakeLists.txt"
      provides: "Qt 6.8 minimum version enforcement + CMAKE_PREFIX_PATH guidance"
      contains: "find_package(Qt6 6.8"
    - path: "src/core/aa/VideoDecoder.cpp"
      provides: "Clean video decoder without version guards"
    - path: "src/core/aa/VideoFramePool.hpp"
      provides: "Clean video frame pool without version guards"
    - path: "src/core/aa/DmaBufVideoBuffer.cpp"
      provides: "DmaBuf buffer without version guards"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "~/Qt/6.8.2/gcc_64/"
      via: "CMAKE_PREFIX_PATH"
      pattern: "find_package.*Qt6 6\\.8"
---

<objective>
Upgrade dev VM (claude-dev) from Qt 6.4 to Qt 6.8.2 using aqtinstall, remove all QT_VERSION guards from the codebase, and enforce Qt 6.8 as minimum version in CMake.

Purpose: Qt 6.8 is required for MultiEffect (QtQuick.Effects) which powers the shadow/depth system in subsequent plans. The Pi already runs Qt 6.8.2.
Output: Dev VM building against Qt 6.8, clean codebase with no version guards, all tests passing.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-depth/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Qt 6.8.2 on dev VM via aqtinstall</name>
  <files>src/CMakeLists.txt</files>
  <action>
1. Install aqtinstall: `pip install aqtinstall`
2. Install Qt 6.8.2: `aqt install-qt linux desktop 6.8.2 gcc_64 --outputdir ~/Qt`
3. Verify installation: `~/Qt/6.8.2/gcc_64/bin/qmake6 --version`
4. Verify QtQuick.Effects module is available (check for `~/Qt/6.8.2/gcc_64/qml/QtQuick/Effects/` directory). If missing, install the module with `aqt install-qt linux desktop 6.8.2 gcc_64 -m qtquick3d` or similar module flag.
5. Update `src/CMakeLists.txt`: change `find_package(Qt6 REQUIRED` to `find_package(Qt6 6.8 REQUIRED` to enforce minimum version.
6. Add a comment near the top of CMakeLists.txt noting the CMAKE_PREFIX_PATH requirement for dev VM: `# Dev VM: cmake -DCMAKE_PREFIX_PATH=~/Qt/6.8.2/gcc_64 ..`
7. Rebuild from clean: `rm -rf build && mkdir build && cd build && cmake -DCMAKE_PREFIX_PATH=~/Qt/6.8.2/gcc_64 .. && make -j$(nproc)`
8. Run full test suite: `cd build && ctest --output-on-failure`

If aqtinstall base install does not include QtQuick.Effects, try: `aqt install-qt linux desktop 6.8.2 gcc_64 -m qtquick3d qtshadertools --outputdir ~/Qt` (Effects may be bundled with quick3d or shadertools).
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Qt 6.8.2 installed at ~/Qt/6.8.2/gcc_64/, project builds cleanly against it, all tests pass, CMake enforces Qt 6.8 minimum</done>
</task>

<task type="auto">
  <name>Task 2: Remove all QT_VERSION guards from C++ source</name>
  <files>src/core/aa/DmaBufVideoBuffer.cpp, src/core/aa/DmaBufVideoBuffer.hpp, src/core/aa/VideoDecoder.cpp, src/core/aa/VideoFramePool.hpp</files>
  <action>
Remove all `#if QT_VERSION >= QT_VERSION_CHECK(6,8,0)` / `#if QT_VERSION < QT_VERSION_CHECK(6,8,0)` preprocessor guards from:

1. **DmaBufVideoBuffer.cpp** (lines 3, 79): Remove the `#if`/`#endif` wrapping the entire file. The code inside the guard IS the file content -- keep it, remove only the guard lines.
2. **DmaBufVideoBuffer.hpp** (lines 4, 51): Same -- remove guards, keep content.
3. **VideoDecoder.cpp** (lines 4, 391, 419, 460): Remove all 4 guard blocks. For the `< 6.8` blocks, DELETE the old Qt 6.4 code paths entirely (they are no longer needed). For the `>= 6.8` blocks, keep the Qt 6.8 code, remove the guard lines.
4. **VideoFramePool.hpp** (lines 9, 46, 108): Remove guards, keep Qt 6.8 code paths, delete Qt 6.4 fallback code.

After removal, verify no `QT_VERSION_CHECK` references remain anywhere in `src/`:
`grep -r "QT_VERSION_CHECK\|QT_VERSION" src/`

Rebuild and run tests to confirm nothing broke.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -r "QT_VERSION_CHECK" src/ && echo "FAIL: guards remain" || echo "PASS: no guards" && cd build && cmake --build . -j$(nproc) 2>&1 | tail -3 && ctest --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>Zero QT_VERSION guards in src/, project builds and all tests pass on Qt 6.8</done>
</task>

</tasks>

<verification>
- `grep -r "QT_VERSION_CHECK" src/` returns no matches
- `cd build && ctest --output-on-failure` all tests pass
- `cmake --build build/ -j$(nproc)` compiles without warnings related to version guards
</verification>

<success_criteria>
Dev VM builds against Qt 6.8.2, all QT_VERSION guards removed, Qt 6.8 enforced as minimum in CMake, all existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-depth/04-01-SUMMARY.md`
</output>
