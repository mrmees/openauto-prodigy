---
phase: 04-visual-depth
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - qml/components/ExitDialog.qml
  - qml/components/GestureOverlay.qml
  - qml/components/PairingDialog.qml
  - qml/components/NavbarPopup.qml
  - qml/applications/settings/SystemSettings.qml
  - qml/applications/settings/AboutSettings.qml
  - qml/applications/bt_audio/BtAudioView.qml
  - qml/applications/phone/PhoneView.qml
  - qml/applications/phone/IncomingCallOverlay.qml
autonomous: true
requirements: ["STY-01"]
must_haves:
  truths:
    - "All dialog/popup buttons use M3 button components (FilledButton, OutlinedButton, ElevatedButton)"
    - "Popup panels and dialogs use Level 3 elevation with surface tint"
    - "No ad-hoc pressed color swap patterns remain in migrated files"
  artifacts:
    - path: "qml/components/ExitDialog.qml"
      provides: "Exit dialog with M3 button components and Level 3 elevation"
    - path: "qml/components/GestureOverlay.qml"
      provides: "Gesture overlay panel with Level 3 elevation + surface tint"
    - path: "qml/components/PairingDialog.qml"
      provides: "Pairing dialog with Level 3 elevation + surface tint"
    - path: "qml/components/NavbarPopup.qml"
      provides: "Navbar popup panel with Level 3 elevation"
    - path: "qml/applications/settings/SystemSettings.qml"
      provides: "System settings with M3 button components"
  key_links:
    - from: "qml/components/ExitDialog.qml"
      to: "qml/controls/FilledButton.qml"
      via: "button components for dialog actions"
      pattern: "FilledButton|OutlinedButton"
    - from: "qml/components/GestureOverlay.qml"
      to: "qml/controls/ElevatedButton.qml"
      via: "button components for overlay actions"
      pattern: "ElevatedButton"
---

<objective>
Migrate all dialog/popup buttons to M3 button components and apply Level 3 elevation with surface tint to popup panels and dialogs.

Purpose: Completes the depth treatment across the remaining interactive surfaces -- dialogs and popups sit above content and need higher elevation to establish visual hierarchy.
Output: 9 QML files with M3 button components and Level 3 panel elevation.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-depth/04-RESEARCH.md
@.planning/phases/04-visual-depth/04-02-SUMMARY.md

<interfaces>
<!-- Button components created in Plan 02 -->

ElevatedButton.qml (general purpose raised button):
- Properties: text, iconCode, buttonColor, pressedColor, textColor, pressedTextColor, buttonScale, buttonEnabled, buttonRadius, elevation
- Signals: clicked(), pressAndHold()
- Default: surfaceContainerLow bg, primary text, elevation 2

FilledButton.qml (primary action button):
- Same properties as ElevatedButton
- Default: primary bg, onPrimary text, elevation 2

OutlinedButton.qml (secondary/cancel button):
- Same properties as ElevatedButton
- Default: transparent bg, primary text, outline border, elevation 0

<!-- Elevation levels -->

- Level 2 (Resting): blur 0.50, offset 4px, opacity 0.30 (buttons at rest)
- Level 3 (Raised): blur 0.70, offset 6px, opacity 0.35, tint 5-8% primary (popups/dialogs)

<!-- Dialog/popup button migration targets (26+ button instances across 9 files) -->

- ExitDialog.qml (3 buttons), GestureOverlay.qml (3 buttons)
- SystemSettings.qml (7 buttons), AboutSettings.qml (1 button)
- PhoneView.qml (5 buttons), IncomingCallOverlay.qml (2 buttons)
- BtAudioView.qml (3 buttons), NavbarPopup.qml (1 button)
- PairingDialog.qml (buttons)

Elevation Level 3 targets (panels/dialogs):
- NavbarPopup panel, GestureOverlay panel
- ExitDialog, PairingDialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dialog and overlay buttons to M3 components</name>
  <files>qml/components/ExitDialog.qml, qml/components/GestureOverlay.qml, qml/components/PairingDialog.qml, qml/components/NavbarPopup.qml</files>
  <action>
Replace Qt Controls Button instances that use custom `background: Rectangle { ... pressed ? color1 : color2 }` with the new M3 button components.

Migration mapping:
- Primary actions (Exit, Restart, Yes, Confirm): use **FilledButton**
- Cancel/No/secondary actions: use **OutlinedButton**
- General interactive buttons: use **ElevatedButton**

**ExitDialog.qml (3 buttons):**
- Replace custom Button backgrounds with FilledButton (exit/restart) and OutlinedButton (cancel)
- Dialog panel: add MultiEffect shadow at Level 3 (`shadowBlur: 0.70, shadowVerticalOffset: 6, shadowOpacity: 0.35`)
- Add surface tint to dialog bg: blend 5-8% ThemeService.primary into existing surface color:
  `Qt.rgba(ThemeService.surfaceContainerHigh.r * 0.93 + ThemeService.primary.r * 0.07, ...g..., ...b..., 1.0)`

**GestureOverlay.qml (3 buttons):**
- Replace button backgrounds with ElevatedButton components
- Panel: Level 3 shadow + surface tint on the overlay panel itself

**PairingDialog.qml:**
- Panel: Level 3 shadow + surface tint
- Migrate any buttons to M3 components

**NavbarPopup.qml:**
- Panel: Level 3 shadow + surface tint
- Replace 1px border with shadow (remove `border.width: 1; border.color: ThemeService.outlineVariant`)
- Popup button: use ElevatedButton
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 dialog/overlay files use M3 button components and Level 3 panel elevation with surface tint, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Migrate settings and phone/bt_audio buttons to M3 components</name>
  <files>qml/applications/settings/SystemSettings.qml, qml/applications/settings/AboutSettings.qml, qml/applications/bt_audio/BtAudioView.qml, qml/applications/phone/PhoneView.qml, qml/applications/phone/IncomingCallOverlay.qml</files>
  <action>
**SystemSettings.qml (7 buttons):**
- Replace all Button custom backgrounds with appropriate M3 button components
- Restart/shutdown = FilledButton, other actions = ElevatedButton

**AboutSettings.qml (1 button):**
- Replace with ElevatedButton

**BtAudioView.qml (3 buttons):**
- Playback controls: ElevatedButton with iconCode
- Keep existing layout, replace backgrounds

**PhoneView.qml (5 buttons):**
- Dial/call = FilledButton, keypad digits = ElevatedButton, hang up = FilledButton with error color

**IncomingCallOverlay.qml (2 buttons):**
- Accept = FilledButton (success color), Decline = FilledButton (error color)

After all changes, verify no `parent.pressed ? ` color swap patterns remain in the migrated files.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 settings/phone/bt_audio files use M3 button components, build succeeds, tests pass</done>
</task>

</tasks>

<verification>
- `grep -rn "parent.pressed" qml/components/ExitDialog.qml qml/components/GestureOverlay.qml` returns no matches
- Build succeeds, all tests pass
- All 9 files use M3 button components
</verification>

<success_criteria>
Every dialog/popup button uses M3 button components. Popup panels and dialogs at Level 3 elevation with surface tint. No ad-hoc pressed color swap patterns remain in migrated files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-depth/04-05-SUMMARY.md`
</output>
