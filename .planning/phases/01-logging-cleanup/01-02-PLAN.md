---
phase: 01-logging-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/aa/AndroidAutoOrchestrator.cpp
  - src/core/aa/BluetoothDiscoveryService.cpp
  - src/core/aa/VideoDecoder.cpp
  - src/core/aa/EvdevTouchReader.cpp
  - src/core/aa/ServiceDiscoveryBuilder.cpp
  - src/core/aa/ThemeNightMode.cpp
  - src/core/aa/TimedNightMode.cpp
  - src/core/aa/GpioNightMode.cpp
  - src/core/aa/CodecCapability.cpp
  - src/core/aa/DmaBufVideoBuffer.cpp
  - src/core/aa/TouchHandler.hpp
  - src/core/services/BluetoothManager.cpp
  - src/core/services/AudioService.cpp
  - src/core/services/CompanionListenerService.cpp
  - src/core/services/SystemServiceClient.cpp
  - src/core/services/IpcServer.cpp
  - src/core/services/ActionRegistry.cpp
  - src/core/plugin/PluginManager.cpp
  - src/core/plugin/PluginLoader.cpp
  - src/core/plugin/PluginDiscovery.cpp
  - src/core/plugin/PluginManifest.cpp
  - src/core/plugin/HostContext.cpp
  - src/plugins/android_auto/AndroidAutoPlugin.cpp
  - src/plugins/bt_audio/BtAudioPlugin.cpp
  - src/plugins/phone/PhonePlugin.cpp
  - src/ui/PluginRuntimeContext.cpp
  - src/ui/PluginViewHost.cpp
  - src/core/services/IpcServer.hpp
  - web-config/server.py
  - web-config/templates/settings.html
autonomous: true
requirements: [LOG-01, LOG-02]

must_haves:
  truths:
    - "Zero raw qDebug()/qInfo() calls remain in app source (all use qCDebug/qCInfo/qCWarning with categories)"
    - "Default run produces only lifecycle info + warnings in journalctl"
    - "Runtime verbose toggle from web panel changes log output immediately without restart"
    - "Per-category debug_categories persists in config.yaml"
  artifacts:
    - path: "src/core/aa/AndroidAutoOrchestrator.cpp"
      provides: "Migrated AA logging"
      contains: "qCInfo(lcAA)"
    - path: "src/core/services/BluetoothManager.cpp"
      provides: "Migrated BT logging"
      contains: "qCInfo(lcBT)"
    - path: "src/core/services/IpcServer.cpp"
      provides: "set_logging/get_logging IPC commands"
      contains: "set_logging"
    - path: "web-config/server.py"
      provides: "Logging toggle endpoint"
      contains: "set_logging"
  key_links:
    - from: "web-config/server.py"
      to: "src/core/services/IpcServer.cpp"
      via: "IPC set_logging command"
      pattern: "set_logging"
    - from: "src/core/services/IpcServer.cpp"
      to: "src/core/Logging.hpp"
      via: "calls oap::setVerbose() and oap::setDebugCategories()"
      pattern: "oap::setVerbose\\|oap::setDebugCategories"
---

<objective>
Migrate all 250 remaining raw log calls across 25 source files to use QLoggingCategory macros, add IPC commands for runtime verbose toggle, and add the web panel logging controls. Completes the phase.

Purpose: With infrastructure from Plan 01 in place, this is a systematic migration + wiring the runtime toggle.
Output: All source files migrated, IPC commands added, web panel has logging controls.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-logging-cleanup/01-CONTEXT.md
@.planning/phases/01-logging-cleanup/01-RESEARCH.md
@.planning/phases/01-logging-cleanup/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Logging infrastructure) -->

From src/core/Logging.hpp:
```cpp
#include <QLoggingCategory>

Q_DECLARE_LOGGING_CATEGORY(lcAA)
Q_DECLARE_LOGGING_CATEGORY(lcBT)
Q_DECLARE_LOGGING_CATEGORY(lcAudio)
Q_DECLARE_LOGGING_CATEGORY(lcPlugin)
Q_DECLARE_LOGGING_CATEGORY(lcUI)
Q_DECLARE_LOGGING_CATEGORY(lcCore)

namespace oap {
void installLogHandler();
void setVerbose(bool enabled);
void setDebugCategories(const QStringList& categories);
bool isVerbose();
}
```

Tag-to-Category Mapping (from 01-RESEARCH.md):
| Existing Tag | Category Variable |
|---|---|
| [AAOrchestrator], [AAPlugin], [VideoDecoder], [EvdevTouch], [ServiceDiscoveryBuilder], [ThemeNightMode] | lcAA |
| [BTDiscovery], [BtManager], [BtAgent] | lcBT |
| [Audio] | lcAudio |
| [PluginViewHost] | lcUI |
| [GpioNightMode], [TimedNightMode], [system-service], main.cpp, CompanionListener | lcCore |
| PluginManager, PluginDiscovery, PluginLoader, PluginManifest, HostContext | lcPlugin |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 25 source files to QLoggingCategory macros</name>
  <files>src/core/aa/AndroidAutoOrchestrator.cpp, src/core/aa/BluetoothDiscoveryService.cpp, src/core/aa/VideoDecoder.cpp, src/core/aa/EvdevTouchReader.cpp, src/core/aa/ServiceDiscoveryBuilder.cpp, src/core/aa/ThemeNightMode.cpp, src/core/aa/TimedNightMode.cpp, src/core/aa/GpioNightMode.cpp, src/core/aa/CodecCapability.cpp, src/core/aa/DmaBufVideoBuffer.cpp, src/core/aa/TouchHandler.hpp, src/core/services/BluetoothManager.cpp, src/core/services/AudioService.cpp, src/core/services/CompanionListenerService.cpp, src/core/services/SystemServiceClient.cpp, src/core/services/IpcServer.cpp, src/core/services/ActionRegistry.cpp, src/core/plugin/PluginManager.cpp, src/core/plugin/PluginLoader.cpp, src/core/plugin/PluginDiscovery.cpp, src/core/plugin/PluginManifest.cpp, src/core/plugin/HostContext.cpp, src/plugins/android_auto/AndroidAutoPlugin.cpp, src/plugins/bt_audio/BtAudioPlugin.cpp, src/plugins/phone/PhonePlugin.cpp, src/ui/PluginRuntimeContext.cpp, src/ui/PluginViewHost.cpp</files>
  <action>
For each source file:
1. Add `#include "core/Logging.hpp"` (adjust relative path as needed — files in `src/core/aa/` use `#include "../Logging.hpp"`, files in `src/core/services/` use `#include "../Logging.hpp"`, files in `src/plugins/*/` use `#include "../../core/Logging.hpp"`, files in `src/ui/` use `#include "../core/Logging.hpp"`)
2. Replace every `qDebug() <<` with `qCDebug(lcXX) <<` using the tag-to-category mapping from the interfaces block above
3. Replace every `qInfo() <<` with `qCInfo(lcXX) <<`
4. Replace every `qWarning() <<` with `qCWarning(lcXX) <<`
5. Replace every `qCritical() <<` with `qCCritical(lcXX) <<`
6. Remove the manual bracket prefix from the message string (e.g., `"[BtManager] Pairable:"` becomes `"Pairable:"` since the handler adds the tag)
7. Remove any `#include <QDebug>` that is no longer needed (the Logging.hpp includes QLoggingCategory which provides the macros)

Do NOT modify files in `libs/open-androidauto/` (submodule — hands off per CLAUDE.md).

The BT plugin files (`BtAudioPlugin.cpp`, `PhonePlugin.cpp`, `BluetoothManager.cpp`) have `#ifdef HAS_BLUETOOTH` guards — the logging migration applies inside these guards as normal.

**Verification after migration:** Run `grep -rn 'qDebug()\|qInfo()\|qWarning()\|qCritical()' src/ --include='*.cpp' --include='*.hpp'` and confirm zero matches (all should now be qCDebug/qCInfo/qCWarning/qCCritical with category argument).
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && grep -rn 'qDebug()\|qInfo()\|qWarning()\|qCritical()' src/ --include='*.cpp' --include='*.hpp' | wc -l && cd build && cmake --build . -j$(nproc) 2>&1 | tail -5 && ctest --output-on-failure</automated>
  </verify>
  <done>Zero raw qDebug()/qInfo()/qWarning()/qCritical() calls remain in src/. All 250 calls migrated to categorized macros. Build succeeds. All 47+ existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add IPC logging commands and web panel toggle</name>
  <files>src/core/services/IpcServer.hpp, src/core/services/IpcServer.cpp, web-config/server.py, web-config/templates/settings.html</files>
  <action>
**IpcServer (C++ side):**

1. In `IpcServer.hpp`, add private method declarations:
   - `QByteArray handleGetLogging();`
   - `QByteArray handleSetLogging(const QVariantMap& data);`

2. In `IpcServer.cpp`:
   - Add `#include "../Logging.hpp"`
   - In `handleRequest()` dispatch, add:
     - `if (command == QLatin1String("get_logging")) return handleGetLogging();`
     - `if (command == QLatin1String("set_logging")) return handleSetLogging(data);`
   - Implement `handleGetLogging()`:
     - Return JSON: `{"verbose": bool, "debug_categories": [...]}`
     - Read from `oap::isVerbose()` and config's `logging.debug_categories`
   - Implement `handleSetLogging(data)`:
     - If `data` contains "verbose": call `oap::setVerbose(data["verbose"].toBool())`
     - If `data` contains "categories": call `oap::setDebugCategories(data["categories"].toStringList())`
     - Persist to config via `config_->setValueByPath("logging.verbose", ...)` and `config_->save(configPath_)`
     - Return `{"ok": true}`

**Web panel (Python side):**

3. In `web-config/server.py`, add two routes:
   - `GET /api/logging` — calls `ipc_request("get_logging")`, returns JSON
   - `POST /api/logging` — calls `ipc_request("set_logging", request.json)`, returns JSON

4. In `web-config/templates/settings.html`, add a "Logging" section (or create if settings.html doesn't exist):
   - Toggle switch for "Verbose logging" (calls POST /api/logging with {"verbose": true/false})
   - Checkbox group for per-category debug (aa, bt, audio, plugin, ui, core)
   - JavaScript: fetch GET /api/logging on page load to populate current state
   - JavaScript: POST /api/logging on toggle change, no page reload needed
   - Keep it simple — this is a debug tool, not a polished UI. Functional > pretty.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) 2>&1 | tail -3 && echo "--- IPC dispatch check ---" && grep -c "set_logging\|get_logging" ../src/core/services/IpcServer.cpp && echo "--- Web routes check ---" && grep -c "set_logging\|get_logging" ../web-config/server.py</automated>
  </verify>
  <done>IPC server handles get_logging and set_logging commands. Web panel has logging toggle UI. Runtime verbose changes take effect immediately without restart. Settings persist to config.yaml.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'qDebug()\|qInfo()\|qWarning()\|qCritical()' src/ --include='*.cpp' --include='*.hpp'` returns zero matches
2. `cmake --build . -j$(nproc)` succeeds
3. `ctest --output-on-failure` passes all tests (47+ existing + new logging tests)
4. IpcServer.cpp contains set_logging/get_logging handlers
5. web-config/server.py contains logging API routes
</verification>

<success_criteria>
- All 256 log calls migrated from raw macros to QLoggingCategory-aware macros
- No raw qDebug()/qInfo() calls remain in src/
- IPC set_logging/get_logging commands work
- Web panel has functional verbose toggle and per-category checkboxes
- Config persists logging settings to config.yaml
- All tests pass
- Phase 1 requirements LOG-01, LOG-02, LOG-03 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-logging-cleanup/01-02-SUMMARY.md`
</output>
