---
phase: 19-widget-instance-config
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-03-16T21:07:54.646Z
---

<current_state>
v0.6.4 Widget Work milestone initialized. Roadmap created with 3 phases (19-21), 18 requirements, 6 planned plans. No plans created yet — next step is discuss or plan phase 19.
</current_state>

<completed_work>

This session (v0.6.3 + v0.6.4 setup):
- Executed phases 17 (status reporting hardening) and 18 (hardware validation) of v0.6.3
- Hardware validation found and fixed 3 bugs: eth0 skip_interfaces bypass, redsocks config permissions, IPv6-mapped address prefix
- Clean post-fix revalidation on current HEAD — PASS through normal companion flow
- Fixed stale test assertions, added IPv6 prefix regression tests (140 Python + 87 C++ all green)
- Completed v0.6.3 milestone — archived, tagged, pushed
- Initialized v0.6.4 Widget Work — requirements (18), roadmap (3 phases), all committed and pushed
</completed_work>

<remaining_work>

- Phase 19: Widget Instance Config (WC-01, WC-02, WC-03) — needs discuss + plan + execute
- Phase 20: Simple Widgets (TC, BW, CS, AF) — can run parallel with 19
- Phase 21: Clock Styles & Weather (CK, WX) — depends on Phase 19
</remaining_work>

<decisions_made>

- v0.6.4 scope: all 6 widgets in one milestone (theme cycle, weather, battery, clock styles, companion status, AA focus toggle)
- Widget contract extension (per-instance config) before config-dependent widgets
- Weather data fetched by Pi via companion proxy (not companion-side)
- AA "disable" is actually VideoFocusRequest(NATIVE/PROJECTED) — keep AA connected, just pause projection
- Clock styles: 2-3 styles, per-widget instance selection
- Theme cycle: forward-only, 1x1
- Battery + companion status: display-only, no new plumbing needed
- Research skipped for this milestone
</decisions_made>

<blockers>
None
</blockers>

<context>
The widget system is mature (v0.5.2-v0.6.1 built the full framework). This milestone adds content widgets on top of the existing contract, plus one contract extension (per-instance config). The main unknowns are weather API selection and analog clock rendering — both deferred to phase planning/research.

Companion SOCKS proxy is working on hardware (validated in v0.6.3). Weather widget can use it for API calls once GPS location is available from companion status messages.
</context>

<next_action>
Start with: `/gsd:discuss-phase 19` to clarify widget config storage design, then `/gsd:plan-phase 19`
</next_action>
