---
phase: 13.1
slug: quick-bugfix-for-companion-app
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-15
---

# Phase 13.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Qt Test (QTest) + CTest |
| **Config file** | tests/CMakeLists.txt |
| **Quick run command** | `cd build && ctest --output-on-failure -R companion` |
| **Full suite command** | `cd build && ctest --output-on-failure` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd build && ctest --output-on-failure -R companion`
- **After every plan wave:** Run `cd build && ctest --output-on-failure`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 13.1-01-01 | 01 | 1 | CLS-cleanup | unit | `ctest -R companion` | ✅ | ⬜ pending |
| 13.1-01-02 | 01 | 1 | CLS-replace | unit | `ctest -R companion` | ❌ W0 | ⬜ pending |
| 13.1-01-03 | 01 | 1 | CLS-error | unit | `ctest -R companion` | ❌ W0 | ⬜ pending |
| 13.1-01-04 | 01 | 1 | CLS-timeout | unit | `ctest -R companion` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New test cases in `tests/test_companion_listener.cpp` for always-replace, errorOccurred, and inactivity timeout scenarios
- [ ] Test helper for simulating stale connections (connect, authenticate, then close without disconnect signal)

*Existing test infrastructure (QTcpServer helpers, authenticateClient, sendMacMessage) covers the base.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Companion reconnect after force-stop | CLS-replace | Requires real phone + Pi | 1. Connect companion, 2. Force-stop app, 3. Relaunch, 4. Verify reconnect succeeds |
| Stale state cleared from UI | CLS-cleanup | Requires Pi display | After timeout/disconnect, GPS/battery/internet indicators should reset |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
