---
phase: 02-settings-restructure
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - qml/applications/settings/AASettings.qml
  - qml/applications/settings/DisplaySettings.qml
  - qml/applications/settings/SystemSettings.qml
  - qml/applications/settings/ConnectionSettings.qml
  - qml/applications/settings/SettingsMenu.qml
  - src/CMakeLists.txt
autonomous: true
requirements: [SET-02, SET-03, SET-05, SET-08, SET-09, SET-10]

must_haves:
  truths:
    - "AA page shows only Resolution, DPI, and Auto-connect — no FPS, no codec section, no protocol capture, no TCP port"
    - "Display page shows screen info, UI scale, brightness, and navbar settings — no theme, no wallpaper, no clock, no night mode"
    - "System page shows left-hand drive, clock 24h, force dark mode, night mode config, software version, and Close App"
    - "Bluetooth page shows BT device name, pairing toggle, paired devices — no WiFi AP, no protocol capture, no AA duplicates"
    - "About section and FPS selector are gone from the entire UI"
    - "Debug AA protocol buttons are gone from System page"
    - "Dead files (AboutSettings, ConnectivitySettings, VideoSettings) are deleted and removed from CMakeLists"
  artifacts:
    - path: "qml/applications/settings/AASettings.qml"
      provides: "Slim AA page — resolution, DPI, auto-connect only"
      min_lines: 15
    - path: "qml/applications/settings/DisplaySettings.qml"
      provides: "Display page — screen info, UI scale, brightness, navbar"
      min_lines: 30
    - path: "qml/applications/settings/SystemSettings.qml"
      provides: "System page — LHD, clock 24h, dark mode, night mode, version, close app"
      min_lines: 40
    - path: "qml/applications/settings/ConnectionSettings.qml"
      provides: "Bluetooth page — BT name, pairing, paired devices"
      min_lines: 20
  key_links:
    - from: "qml/applications/settings/AASettings.qml"
      to: "ConfigService"
      via: "configPath bindings"
      pattern: "video\\.resolution|video\\.dpi|auto_connect"
    - from: "qml/applications/settings/SystemSettings.qml"
      to: "ConfigService"
      via: "configPath bindings for dark mode and night mode"
      pattern: "force_dark_mode|night_mode"
---

<objective>
Strip relocated controls from existing settings pages, restructure System/Bluetooth, and remove dead files.

Purpose: Complete the settings reorganization by cleaning up old pages now that new pages (from plan 01) hold the relocated controls.
Output: Slimmed AASettings, restructured DisplaySettings/SystemSettings/ConnectionSettings, deleted dead files
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-restructure/02-CONTEXT.md
@.planning/phases/02-settings-restructure/02-01-SUMMARY.md

@qml/applications/settings/AASettings.qml
@qml/applications/settings/DisplaySettings.qml
@qml/applications/settings/SystemSettings.qml
@qml/applications/settings/ConnectionSettings.qml
@qml/applications/settings/SettingsMenu.qml
@src/CMakeLists.txt

<interfaces>
<!-- Same control library as plan 01 — see 02-01-PLAN.md interfaces block -->
- SettingsToggle { label; configPath; restartRequired? }
- SettingsSlider { label; configPath; from; to; stepSize }
- ReadOnlyField { label; configPath?; value?; placeholder? }
- SectionHeader { text }
- FullScreenPicker { label; configPath; options; values; onActivated? }
- ElevatedButton { text; iconCode?; buttonEnabled?; onClicked }
- ExitDialog {}
- UiMetrics.*, ThemeService.*, ConfigService.*, DisplayInfo.*, DisplayService.*, NavbarController.*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim down AASettings and restructure DisplaySettings and SystemSettings</name>
  <files>
    qml/applications/settings/AASettings.qml
    qml/applications/settings/DisplaySettings.qml
    qml/applications/settings/SystemSettings.qml
  </files>
  <action>
**AASettings.qml** (SET-02, SET-08):
Rewrite to contain ONLY:
- Resolution picker: `FullScreenPicker` with `configPath: "video.resolution"`, options ["480p", "720p", "1080p"]
- DPI slider: `SettingsSlider` with `configPath: "video.dpi"`, from 80 to 400, stepSize 10, restartRequired true
- Auto-connect toggle: `SettingsToggle` with `configPath: "connection.auto_connect_aa"`
Remove entirely: FPS selector (SET-08), Video Decoding section (moved to Debug), Connection section (TCP port moved to Debug), Debug section (protocol capture moved to Debug), decoder picker dialog, all helper functions.
No section headers needed — only 3 controls.

**DisplaySettings.qml** (SET-03):
Keep:
- Screen info ReadOnlyField (size/PPI) — move to very top as info bar context
- UI Scale stepper (the `_currentScale` property, `_applyScale` function, and the [-] value [+] Item)
- SectionHeader "Display"
- Brightness/Screen Dimming slider
- SectionHeader "Navbar"
- Navbar Position picker
- Show Navbar during AA toggle

Remove entirely:
- SectionHeader "General" (rename flow — brightness goes under "Display")
- Theme picker + delete theme row (moved to ThemeSettings)
- Wallpaper picker (moved to ThemeSettings)
- SectionHeader "Clock" + 24h toggle (moved to System)
- SectionHeader "Day / Night Mode" + force dark toggle + night mode controls (moved to System/Theme)
- The `Connections` block for force_dark_mode (moved to ThemeSettings)

**SystemSettings.qml** (SET-05, SET-09, SET-10):
Rewrite to contain:
- SectionHeader "General"
- Left-hand drive toggle: `SettingsToggle` with `configPath: "identity.left_hand_drive"`
- Clock 24h toggle: `SettingsToggle` with `label: "24-Hour Clock"`, `configPath: "display.clock_24h"` (moved from Display)
- Force dark mode toggle: `SettingsToggle` with `label: "Always Use Dark Mode"`, `configPath: "display.force_dark_mode"` plus `Connections` block to sync ThemeService.forceDarkMode (intentional duplicate of Theme page — same config path auto-syncs)
- SectionHeader "Day / Night Mode"
- Night mode controls: copy the entire night mode block from DisplaySettings.qml (the opacity/enabled wrapper Item containing nightModeControls ColumnLayout with source picker, day/night start times, GPIO pin, GPIO active high). These are disabled when forceDarkMode is true.
- SectionHeader "Software"
- ReadOnlyField for software version: `configPath: "identity.sw_version"`
- Spacer Item
- Close App ElevatedButton + ExitDialog (keep from current SystemSettings)

Remove entirely (SET-09, SET-10):
- SectionHeader "Identity" + all identity ReadOnlyFields (moved to Information)
- SectionHeader "Hardware" + hardware/touch ReadOnlyFields (moved to Information)
- SectionHeader "About" + about text (SET-09 — removed entirely, version shown above)
- SectionHeader "Debug: AA Protocol" + all AA protocol test buttons + status text (SET-10 — moved to Debug)
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. 2>&1 | tail -3 && make -j$(nproc) 2>&1 | tail -10</automated>
  </verify>
  <done>AASettings has exactly 3 controls (resolution, DPI, auto-connect). DisplaySettings has screen info, UI scale, brightness, navbar only. SystemSettings has LHD, clock, dark mode, night mode, version, close app — no identity, no about, no debug buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up Bluetooth page and delete dead files</name>
  <files>
    qml/applications/settings/ConnectionSettings.qml
    qml/applications/settings/SettingsMenu.qml
    src/CMakeLists.txt
  </files>
  <action>
**ConnectionSettings.qml** (stays as ConnectionSettings.qml — SettingsMenu already labels it "Bluetooth"):
Strip down to Bluetooth-only content:
- Keep SectionHeader "Bluetooth" (or remove header since it's the whole page — discretion)
- Keep BT Device Name ReadOnlyField (`connection.bt_name`)
- Keep Accept New Pairings toggle (the custom Item with BluetoothManager.pairable switch)
- Keep Paired devices Repeater with forget buttons

Remove entirely:
- SectionHeader "Android Auto" + auto-connect toggle + TCP port (duplicates of AA page / Debug)
- SectionHeader "Protocol Capture" + all capture controls (moved to Debug)
- SectionHeader "WiFi Access Point" + channel picker + band selector (moved to Debug as read-only)

**Delete dead files:**
```bash
rm qml/applications/settings/AboutSettings.qml
rm qml/applications/settings/ConnectivitySettings.qml
rm qml/applications/settings/VideoSettings.qml
```

**CMakeLists.txt:**
Remove the `set_source_files_properties` blocks and QML_FILES entries for:
- AboutSettings.qml
- ConnectivitySettings.qml (if referenced)
- VideoSettings.qml

Verify the 3 new files from plan 01 are already registered (they should be from plan 01 Task 2).

**SettingsMenu.qml** (minor — if needed):
Verify no remaining references to aboutPage or videoPage components. Remove any stale Component declarations if present.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && ! test -f qml/applications/settings/AboutSettings.qml && ! test -f qml/applications/settings/ConnectivitySettings.qml && ! test -f qml/applications/settings/VideoSettings.qml && echo "Dead files deleted" && cd build && cmake .. 2>&1 | tail -3 && make -j$(nproc) 2>&1 | tail -10 && ctest --output-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>ConnectionSettings contains only BT controls. AboutSettings, ConnectivitySettings, VideoSettings deleted. CMakeLists updated. Full build + tests pass. No duplicate controls exist across any settings page.</done>
</task>

</tasks>

<verification>
Full build and test:
```bash
cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc) && ctest --output-on-failure
```

Manual audit — grep for controls that should NOT appear on certain pages:
```bash
# FPS should not appear in any settings file
grep -r "video.fps" qml/applications/settings/
# About section should be gone
grep -r "About OpenAuto" qml/applications/settings/
# Protocol capture should only be in DebugSettings
grep -l "protocol_capture" qml/applications/settings/ | grep -v Debug
```
</verification>

<success_criteria>
- AASettings has exactly 3 controls: resolution picker, DPI slider, auto-connect toggle
- DisplaySettings has screen info, UI scale, brightness, navbar settings only
- SystemSettings has LHD, clock 24h, dark mode, night mode, SW version, close app
- ConnectionSettings has BT name, pairing toggle, paired devices only
- AboutSettings.qml, ConnectivitySettings.qml, VideoSettings.qml are deleted
- No FPS selector anywhere in UI
- No About section anywhere
- No AA protocol buttons on System page
- All tests pass, clean build
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-restructure/02-02-SUMMARY.md`
</output>
