---
phase: 02-settings-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qml/applications/settings/ThemeSettings.qml
  - qml/applications/settings/InformationSettings.qml
  - qml/applications/settings/DebugSettings.qml
  - qml/applications/settings/SettingsMenu.qml
  - src/CMakeLists.txt
autonomous: true
requirements: [SET-01, SET-04, SET-06, SET-07]

must_haves:
  truths:
    - "Settings top-level shows 9 categories in correct order (AA, Display, Audio, Bluetooth, Theme, Companion, System, Information, Debug)"
    - "Theme page shows theme picker, wallpaper picker, delete theme, and force dark mode toggle"
    - "Information page shows all identity fields and hardware profile as read-only"
    - "Debug page shows codec/decoder selection, protocol capture controls, TCP port, WiFi AP info, and AA protocol test buttons"
  artifacts:
    - path: "qml/applications/settings/ThemeSettings.qml"
      provides: "Theme picker, wallpaper, dark mode toggle, swipe-to-delete"
      min_lines: 40
    - path: "qml/applications/settings/InformationSettings.qml"
      provides: "Read-only identity and hardware fields"
      min_lines: 30
    - path: "qml/applications/settings/DebugSettings.qml"
      provides: "Codec/decoder, protocol capture, WiFi AP read-only, AA buttons"
      min_lines: 100
    - path: "qml/applications/settings/SettingsMenu.qml"
      provides: "9-category routing"
      contains: "Theme"
  key_links:
    - from: "qml/applications/settings/SettingsMenu.qml"
      to: "ThemeSettings.qml"
      via: "Component + openPage routing"
      pattern: "ThemeSettings"
    - from: "qml/applications/settings/SettingsMenu.qml"
      to: "InformationSettings.qml"
      via: "Component + openPage routing"
      pattern: "InformationSettings"
    - from: "qml/applications/settings/SettingsMenu.qml"
      to: "DebugSettings.qml"
      via: "Component + openPage routing"
      pattern: "DebugSettings"
---

<objective>
Create the 3 new settings pages (Theme, Information, Debug) and update SettingsMenu to route to all 9 categories.

Purpose: Establish the new 9-category settings structure with all new pages functional before stripping controls from old pages.
Output: ThemeSettings.qml, InformationSettings.qml, DebugSettings.qml, updated SettingsMenu.qml and CMakeLists.txt
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-restructure/02-CONTEXT.md

@qml/applications/settings/SettingsMenu.qml
@qml/applications/settings/DisplaySettings.qml
@qml/applications/settings/SystemSettings.qml
@qml/applications/settings/AASettings.qml
@qml/applications/settings/ConnectionSettings.qml
@src/CMakeLists.txt

<interfaces>
<!-- Reusable controls available for settings pages -->
- SettingsToggle { label: string; configPath: string; restartRequired?: bool }
- SettingsSlider { label: string; configPath: string; from: real; to: real; stepSize: real }
- ReadOnlyField { label: string; configPath?: string; value?: string; placeholder?: string }
- SectionHeader { text: string }
- FullScreenPicker { label: string; configPath: string; options: list; values: list; onActivated?: fn }
- SegmentedButton { label: string; configPath: string; options: list; values: list }
- ElevatedButton { text: string; iconCode?: string; buttonEnabled?: bool; onClicked: fn }
- MaterialIcon { icon: string (codepoint); size: real; color: color }
- ExitDialog { parent: Item; anchors.centerIn: parent }

<!-- Services available in QML context -->
- ConfigService.value(path) / setValue(path, val) / save() / onConfigChanged(path, value)
- ThemeService.forceDarkMode, availableThemeNames, availableThemes, availableWallpaperNames, availableWallpapers
- ThemeService.setTheme(id), setWallpaperOverride(id), refreshWallpapers(), isUserTheme(id), deleteTheme(id)
- ThemeService colors: onSurface, onSurfaceVariant, surface, surfaceContainer, surfaceContainerHigh, primary, error, etc.
- UiMetrics: marginPage, marginRow, spacing, gap, rowH, touchMin, fontBody, fontSmall, fontCaption, fontHeading, fontTitle, iconSize, iconSmall, radius, headerH, backBtnSize, animDuration, animDurationFast, sectionGap, tileH
- CodecCapabilityModel: rowCount(), codecName(i), isEnabled(i), setEnabled(i, bool), setHardwareMode(i, bool), setSelectedDecoder(i, name), isHwDecoder(i, name)
- AAOrchestrator.connectionState (3 = connected), sendButtonPress(keycode)
- BluetoothManager.pairable, setPairable(bool), forgetDevice(addr)
- PairedDevicesModel: model roles — name, address, connected
- DisplayInfo.screenSizeInches, computedDpi
- DisplayService.hasHardwareBrightness, setBrightness(val)
- NavbarController.setEdge(edge)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThemeSettings, InformationSettings, and DebugSettings QML files</name>
  <files>
    qml/applications/settings/ThemeSettings.qml
    qml/applications/settings/InformationSettings.qml
    qml/applications/settings/DebugSettings.qml
  </files>
  <action>
Create three new QML settings page files. All pages use the same Flickable + ColumnLayout pattern as existing settings pages.

**ThemeSettings.qml** (SET-04):
- Theme picker: `FullScreenPicker` with `configPath: "display.theme"`, options from `ThemeService.availableThemeNames/availableThemes`, `onActivated` calls `ThemeService.setTheme()`
- Delete theme row: copy the existing delete-theme Item from DisplaySettings.qml (the `deleteThemeRow` block with double-tap confirmation). Per user decision, this should use swipe-to-delete instead of double-tap, but keep the existing double-tap pattern for now — swipe-to-delete is a discretion item that can be refined later.
- Wallpaper picker: `FullScreenPicker` with `configPath: "display.wallpaper_override"`, from ThemeService wallpaper lists, `onActivated` calls `ThemeService.setWallpaperOverride()`, `Component.onCompleted: ThemeService.refreshWallpapers()`
- Force dark mode toggle: `SettingsToggle` with `label: "Always Use Dark Mode"`, `configPath: "display.force_dark_mode"` plus the `Connections` block on ConfigService to sync `ThemeService.forceDarkMode` (copy from DisplaySettings.qml lines 272-284)
- No section headers needed — it's a short page, controls are self-explanatory

**InformationSettings.qml** (SET-06):
- SectionHeader "Identity"
- ReadOnlyField for: Head Unit Name (`identity.head_unit_name`), Manufacturer (`identity.manufacturer`), Model (`identity.model`), Car Model (`identity.car_model`, placeholder "(optional)"), Car Year (`identity.car_year`, placeholder "(optional)")
- SectionHeader "Hardware"
- ReadOnlyField for: Hardware Profile (`hardware_profile`), Touch Device (`touch.device`, placeholder "(auto-detect)")
- No software version here (that goes on System per user decision)

**DebugSettings.qml** (SET-07):
- SectionHeader "Video Decoding"
- Copy the entire codec Repeater block + decoder picker Dialog + helper functions (`codecDisplayName`, `saveCodecConfig`, `saveDecoderConfig`, `Component.onCompleted` codec loader) from AASettings.qml. This is the full Video Decoding section (lines 42-447 of AASettings.qml).
- SectionHeader "Protocol Capture"
- Copy protocol capture controls from AASettings.qml: Enable Capture toggle (`connection.protocol_capture.enabled`), Format segmented button, Include Media Frames toggle, Capture Path read-only field
- SectionHeader "Connection Info"
- ReadOnlyField for TCP Port (`connection.tcp_port`, placeholder "5277") — read-only info
- SectionHeader "WiFi Access Point" with a small note text below: "Configured at install time. Edit YAML config to change." using `font.pixelSize: UiMetrics.fontCaption` and `color: ThemeService.onSurfaceVariant`
- Copy WiFi AP controls from ConnectionSettings.qml but make them informational: Channel as ReadOnlyField (`connection.wifi_ap.channel`), Band as ReadOnlyField (`connection.wifi_ap.band`)
- SectionHeader "AA Protocol Test"
- Copy the AA protocol test buttons section from SystemSettings.qml (the `aaConnected` property, both RowLayouts of ElevatedButtons, and the status Text) — lines 101-181
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && test -f qml/applications/settings/ThemeSettings.qml && test -f qml/applications/settings/InformationSettings.qml && test -f qml/applications/settings/DebugSettings.qml && echo "All 3 new files exist"</automated>
  </verify>
  <done>ThemeSettings.qml has theme picker + wallpaper + dark mode toggle. InformationSettings.qml has identity + hardware read-only fields. DebugSettings.qml has codec/decoder + protocol capture + WiFi AP info + AA protocol buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Update SettingsMenu routing and CMakeLists for 9 categories</name>
  <files>
    qml/applications/settings/SettingsMenu.qml
    src/CMakeLists.txt
  </files>
  <action>
**SettingsMenu.qml:**
Update the ListModel to have 9 entries in order:
1. Android Auto — icon "\ue531", pageId "aa"
2. Display — icon "\ueb97", pageId "display"
3. Audio — icon "\ue050", pageId "audio"
4. Bluetooth — icon "\ue1a7", pageId "connection" (keep pageId as "connection" for now — rename happens in plan 02)
5. Theme — icon "\ue40a" (palette), pageId "theme"
6. Companion — icon "\ue324", pageId "companion"
7. System — icon "\uf8cd", pageId "system"
8. Information — icon "\ue88e" (info), pageId "information"
9. Debug — icon "\ue868" (bug_report), pageId "debug"

Add Component declarations:
```
Component { id: themePage; ThemeSettings {} }
Component { id: informationPage; InformationSettings {} }
Component { id: debugPage; DebugSettings {} }
```

Update the `titles` and `pages` objects in `openPage()` to include the 3 new entries:
- "theme": "Theme" / themePage
- "information": "Information" / informationPage
- "debug": "Debug" / debugPage

**CMakeLists.txt:**
Add `set_source_files_properties` blocks for the 3 new files (ThemeSettings, InformationSettings, DebugSettings) following the existing pattern. Add the 3 files to the `qt_add_qml_module` QML_FILES list. Do NOT remove old files yet — that happens in plan 02 to avoid breaking the build mid-restructure.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. 2>&1 | tail -5 && make -j$(nproc) 2>&1 | tail -10</automated>
  </verify>
  <done>SettingsMenu shows 9 categories. Tapping Theme/Information/Debug navigates to the new pages. Project builds cleanly.</done>
</task>

</tasks>

<verification>
Build the project and verify no compile errors:
```bash
cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && make -j$(nproc)
```
Run existing tests to ensure no regressions:
```bash
cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && ctest --output-on-failure
```
</verification>

<success_criteria>
- 3 new QML files created with correct controls
- SettingsMenu lists 9 categories in specified order
- Navigation to all 9 pages works (no QML errors on push)
- Project compiles cleanly on Qt 6.4
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-restructure/02-01-SUMMARY.md`
</output>
