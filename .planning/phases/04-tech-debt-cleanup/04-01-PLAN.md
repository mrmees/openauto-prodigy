---
phase: 04-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/services/ThemeService.hpp
  - src/core/services/ThemeService.cpp
  - config/themes/default/theme.yaml
  - config/themes/ember/theme.yaml
  - config/themes/ocean/theme.yaml
  - config/themes/amoled/theme.yaml
  - tests/data/themes/default/theme.yaml
  - qml/controls/SegmentedButton.qml
  - qml/applications/settings/ConnectivitySettings.qml
  - qml/applications/settings/ConnectionSettings.qml
  - qml/controls/Tile.qml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ICON-03
  - NAV-01

must_haves:
  truths:
    - "NavStrip active icon color resolves to a real ThemeService property (no undefined binding)"
    - "SegmentedButton and settings dividers use ThemeService.dividerColor (no descriptionFontColor+opacity hack)"
    - "Tile.qml has no dead tileSubtitle property"
    - "REQUIREMENTS.md traceability table has no stale Pending entries"
  artifacts:
    - path: "src/core/services/ThemeService.hpp"
      provides: "highlightFontColor Q_PROPERTY declaration"
      contains: "highlightFontColor"
    - path: "src/core/services/ThemeService.cpp"
      provides: "highlightFontColor accessor with fallback"
      contains: "highlightFontColor"
    - path: "config/themes/default/theme.yaml"
      provides: "highlight_font color values"
      contains: "highlight_font"
    - path: "qml/controls/SegmentedButton.qml"
      provides: "Divider using dividerColor"
      contains: "dividerColor"
  key_links:
    - from: "qml/components/NavStrip.qml"
      to: "src/core/services/ThemeService.hpp"
      via: "Q_PROPERTY binding"
      pattern: "ThemeService\\.highlightFontColor"
    - from: "qml/controls/SegmentedButton.qml"
      to: "src/core/services/ThemeService.hpp"
      via: "Q_PROPERTY binding"
      pattern: "ThemeService\\.dividerColor"
---

<objective>
Close all audit-identified tech debt from the v0.4.3 milestone: add the missing highlightFontColor ThemeService property so NavStrip active icons resolve correctly, replace divider color hacks with ThemeService.dividerColor, remove the dead tileSubtitle property, and verify REQUIREMENTS.md traceability is clean.

Purpose: Eliminate undefined bindings and dead code so the codebase is clean before the next milestone.
Output: All 5 audit items resolved, build passing, tests passing.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tech-debt-cleanup/04-RESEARCH.md

<interfaces>
<!-- ThemeService existing pattern for adding color properties -->

From src/core/services/ThemeService.hpp:
```cpp
Q_PROPERTY(QColor dividerColor READ dividerColor NOTIFY colorsChanged)
Q_PROPERTY(QColor pressedColor READ pressedColor NOTIFY colorsChanged)

QColor dividerColor() const;
QColor pressedColor() const;
```

From src/core/services/ThemeService.cpp:
```cpp
QColor ThemeService::dividerColor() const
{
    QColor c = activeColor("divider");
    if (c == QColor(Qt::transparent))
        return QColor(255, 255, 255, 38);  // ~15% white
    return c;
}
```

NavStrip.qml references (lines 39, 72, 186):
```qml
color: navStrip.homeActive ? ThemeService.highlightFontColor : ThemeService.normalFontColor
color: isActive ? ThemeService.highlightFontColor : ThemeService.normalFontColor
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add highlightFontColor ThemeService property + fix divider hacks + remove dead code</name>
  <files>
    src/core/services/ThemeService.hpp,
    src/core/services/ThemeService.cpp,
    config/themes/default/theme.yaml,
    config/themes/ember/theme.yaml,
    config/themes/ocean/theme.yaml,
    config/themes/amoled/theme.yaml,
    tests/data/themes/default/theme.yaml,
    qml/controls/SegmentedButton.qml,
    qml/applications/settings/ConnectivitySettings.qml,
    qml/applications/settings/ConnectionSettings.qml,
    qml/controls/Tile.qml
  </files>
  <action>
    **1. Add highlightFontColor to ThemeService (C++):**

    In `ThemeService.hpp`:
    - Add Q_PROPERTY: `Q_PROPERTY(QColor highlightFontColor READ highlightFontColor NOTIFY colorsChanged)`
    - Add accessor declaration: `QColor highlightFontColor() const;`
    - Place both next to the existing `pressedColor`/`dividerColor` declarations.

    In `ThemeService.cpp`:
    - Add accessor implementation following the established pattern:
      ```cpp
      QColor ThemeService::highlightFontColor() const
      {
          QColor c = activeColor("highlight_font");
          if (c == QColor(Qt::transparent))
              return activeColor("background");
          return c;
      }
      ```
    - Place next to existing `dividerColor()`/`pressedColor()` implementations.

    **2. Add highlight_font to all theme YAMLs:**

    Add `highlight_font` key to both `day:` and `night:` sections in all 5 theme YAML files.
    Use the background color value from each theme (dark canvas contrasts with bright highlight):

    | Theme | Day highlight_font | Night highlight_font |
    |-------|--------------------|----------------------|
    | default | `"#1a1a2e"` | `"#0a0a14"` |
    | ember | `"#1e1712"` | `"#0f0c09"` |
    | ocean | `"#1b2838"` | `"#0d141c"` |
    | amoled | `"#000000"` | `"#000000"` |

    For `tests/data/themes/default/theme.yaml`, use the same values as the default theme.

    **3. Fix SegmentedButton divider color:**

    In `SegmentedButton.qml` line 98: change `color: ThemeService.descriptionFontColor` to `color: ThemeService.dividerColor`.
    Remove the `opacity: 0.3` on line 99 — `dividerColor` already has alpha baked in from the YAML value.

    **4. Fix ConnectivitySettings divider hacks:**

    In `ConnectivitySettings.qml`:
    - Line 53: change `color: ThemeService.descriptionFontColor; opacity: 0.15` to `color: ThemeService.dividerColor` (remove opacity)
    - Line 96: same change

    In `ConnectionSettings.qml`:
    - Line 109: change `color: ThemeService.descriptionFontColor; opacity: 0.15` to `color: ThemeService.dividerColor` (remove opacity)
    - Line 169: same change

    **5. Remove dead Tile property:**

    In `Tile.qml` line 9: delete the line `property string tileSubtitle: ""`.

    **6. pressedColor decision:** Keep pressedColor as-is. VIS-05 says ThemeService "provides" it. Zero runtime cost. No changes needed.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy && mkdir -p build && cd build && cmake .. -DBUILD_TESTING=ON 2>&1 | tail -5 && cmake --build . -j$(nproc) 2>&1 | tail -10 && ctest --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <done>
    - highlightFontColor Q_PROPERTY exists in ThemeService, NavStrip bindings resolve (no undefined)
    - All 5 theme YAMLs have highlight_font in day and night sections
    - SegmentedButton divider uses ThemeService.dividerColor (no opacity hack)
    - ConnectivitySettings and ConnectionSettings dividers use ThemeService.dividerColor (no opacity hack)
    - Tile.qml has no tileSubtitle property
    - Build succeeds, all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify REQUIREMENTS.md traceability + update STATE.md decisions</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    **1. Verify REQUIREMENTS.md traceability table:**

    Confirm no entries have status "Pending" in the traceability table. Research already confirmed this is clean (all entries show "Complete"). If any are found stale, update to "Complete".

    **2. Update STATE.md with phase 4 decisions:**

    Add to Decisions section:
    - `04-01: pressedColor kept (not removed) — VIS-05 says "provides", zero runtime cost, removal would be perverse`
    - `04-01: ConnectivitySettings/ConnectionSettings divider hacks fixed alongside SegmentedButton — same anti-pattern, same fix`
    - `04-01: highlightFontColor falls back to activeColor("background") — no IThemeService change needed, QML accesses ThemeService directly`
  </action>
  <verify>
    <automated>grep -c "Pending" /home/matt/claude/personal/openautopro/openauto-prodigy/.planning/REQUIREMENTS.md; echo "Expected: 0"</automated>
  </verify>
  <done>
    - REQUIREMENTS.md traceability table has zero "Pending" entries
    - STATE.md documents pressedColor decision and divider cleanup scope
  </done>
</task>

</tasks>

<verification>
1. `cd build && cmake --build . -j$(nproc)` — builds without warnings related to highlightFontColor
2. `cd build && ctest --output-on-failure` — all tests pass
3. `grep highlightFontColor src/core/services/ThemeService.hpp` — property exists
4. `grep highlight_font config/themes/default/theme.yaml` — YAML key exists
5. `grep -c "descriptionFontColor.*opacity" qml/controls/SegmentedButton.qml qml/applications/settings/ConnectivitySettings.qml qml/applications/settings/ConnectionSettings.qml` — all return 0
6. `grep tileSubtitle qml/controls/Tile.qml` — returns nothing
7. `grep Pending .planning/REQUIREMENTS.md` — returns nothing
</verification>

<success_criteria>
- NavStrip active icons use ThemeService.highlightFontColor which resolves to a real color (no undefined binding)
- All divider Rectangles in SegmentedButton, ConnectivitySettings, and ConnectionSettings use ThemeService.dividerColor
- No dead tileSubtitle property in Tile.qml
- REQUIREMENTS.md traceability is clean
- Build and all 47+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-tech-debt-cleanup/04-01-SUMMARY.md`
</output>
