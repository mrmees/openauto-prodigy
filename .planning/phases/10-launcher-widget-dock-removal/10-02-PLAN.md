---
phase: 10-launcher-widget-dock-removal
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/ui/LauncherModel.hpp
  - src/ui/LauncherModel.cpp
  - qml/components/LauncherDock.qml
  - qml/applications/launcher/LauncherMenu.qml
  - src/CMakeLists.txt
  - src/main.cpp
  - src/core/YamlConfig.hpp
  - src/core/YamlConfig.cpp
  - qml/applications/home/HomeMenu.qml
  - docs/config-schema.md
  - docs/development.md
  - docs/plugin-api.md
  - tests/test_yaml_config.cpp
autonomous: true
requirements: [NAV-02, NAV-03]

must_haves:
  truths:
    - "LauncherDock bottom bar is gone from the shell -- no overlay consuming vertical space"
    - "LauncherModel class no longer exists in the codebase"
    - "LauncherMenu full-page view no longer exists"
    - "Application builds and all tests pass without any launcher references"
    - "Stale launcher documentation is cleaned up"
  artifacts:
    - path: "src/CMakeLists.txt"
      provides: "No references to LauncherModel.cpp, LauncherDock.qml, or LauncherMenu.qml"
    - path: "src/main.cpp"
      provides: "No LauncherModel include, construction, or context property registration"
    - path: "src/core/YamlConfig.cpp"
      provides: "No launcherTiles() or setLauncherTiles() methods"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "build system"
      via: "Removed source files no longer referenced"
      pattern: "LauncherModel|LauncherDock|LauncherMenu"
    - from: "src/main.cpp"
      to: "runtime"
      via: "No LauncherModel construction or QML registration"
      pattern: "LauncherModel"
---

<objective>
Delete LauncherDock, LauncherModel, and LauncherMenu entirely. Remove all references from build system, main.cpp, YamlConfig, HomeMenu, and documentation.

Purpose: Complete the transition from fixed bottom-bar navigation to widget-based navigation. The singleton widgets on the reserved page (from Plan 01) now provide all navigation previously handled by the dock.

Output: Four files deleted, all references cleaned from build/runtime/config/docs, clean build with full test suite passing.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-launcher-widget-dock-removal/10-CONTEXT.md
@.planning/phases/10-launcher-widget-dock-removal/10-RESEARCH.md
@.planning/phases/10-launcher-widget-dock-removal/10-01-SUMMARY.md

<interfaces>
<!-- Files to DELETE (verify they exist before deletion): -->
<!-- src/ui/LauncherModel.hpp -->
<!-- src/ui/LauncherModel.cpp -->
<!-- qml/components/LauncherDock.qml -->
<!-- qml/applications/launcher/LauncherMenu.qml -->

<!-- References to REMOVE (from RESEARCH.md): -->
<!-- src/CMakeLists.txt: LauncherModel.cpp (line ~26), LauncherDock QML props (lines ~248-250), LauncherDock in QML_FILES (line ~384), LauncherMenu QML props (lines ~276-278), LauncherMenu in QML_FILES (line ~390) -->
<!-- src/main.cpp: #include "ui/LauncherModel.hpp" (line ~52), LauncherModel construction (line ~604), context property (line ~697) -->
<!-- qml/applications/home/HomeMenu.qml: LauncherDock block (lines ~561-570) -->
<!-- src/core/YamlConfig.hpp: launcherTiles()/setLauncherTiles() declarations (lines ~103-104) -->
<!-- src/core/YamlConfig.cpp: launcherTiles() impl (lines ~572-587), setLauncherTiles() (lines ~589+) -->
<!-- docs/config-schema.md: launcher.tiles refs (lines ~177, 329-330) -->
<!-- docs/development.md: LauncherMenu in directory tree (line ~258) -->
<!-- docs/plugin-api.md: "Return to launcher" at line ~121 -->
<!-- tests/test_yaml_config.cpp: config.launcherTiles() test at line ~195 -->
<!-- src/core/YamlConfig.cpp: root_["launcher"]["tiles"] default population at line ~112 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete launcher files and remove all references</name>
  <files>
    src/ui/LauncherModel.hpp,
    src/ui/LauncherModel.cpp,
    qml/components/LauncherDock.qml,
    qml/applications/launcher/LauncherMenu.qml,
    src/CMakeLists.txt,
    src/main.cpp,
    src/core/YamlConfig.hpp,
    src/core/YamlConfig.cpp,
    qml/applications/home/HomeMenu.qml,
    docs/config-schema.md,
    docs/development.md
  </files>
  <action>
    **Step 1 — Delete files:**
    ```bash
    git rm src/ui/LauncherModel.hpp src/ui/LauncherModel.cpp
    git rm qml/components/LauncherDock.qml
    git rm qml/applications/launcher/LauncherMenu.qml
    ```
    Also remove the `qml/applications/launcher/` directory if it becomes empty after LauncherMenu deletion.

    **Step 2 — CMakeLists.txt:** Remove all references to deleted files:
    - Remove `ui/LauncherModel.cpp` from the source file list
    - Remove LauncherDock-related `set_source_files_properties` block (QML_SKIP_CACHEGEN etc.)
    - Remove `components/LauncherDock.qml` from QML_FILES
    - Remove LauncherMenu-related `set_source_files_properties` block
    - Remove `applications/launcher/LauncherMenu.qml` from QML_FILES
    - If there's a `LauncherModel.hpp` in any header list, remove it

    **Step 3 — main.cpp:** Remove three things:
    - `#include "ui/LauncherModel.hpp"` (near top)
    - LauncherModel construction (e.g., `auto launcherModel = new LauncherModel(...)` or similar)
    - QML context property registration (e.g., `rootContext->setContextProperty("LauncherModel", launcherModel)`)
    - Any other references to LauncherModel or LauncherMenu

    **Step 4 — YamlConfig.hpp:** Remove `launcherTiles()` and `setLauncherTiles()` method declarations. Remove any associated member variables (e.g., cached tiles list).

    **Step 5 — YamlConfig.cpp:** Remove `launcherTiles()` implementation and `setLauncherTiles()` implementation entirely. ALSO remove the `root_["launcher"]["tiles"]` default population block (around line 112 in the constructor/defaults method) — without this, new configs would still emit stale launcher config even after the launcher is removed. Leave the YAML key orphaned in existing user configs (benign -- YAML is schema-less, stale keys don't cause errors per RESEARCH.md Pitfall 5).

    **Step 5b — test_yaml_config.cpp:** Remove the launcherTiles test (around line 195: `auto tiles = config.launcherTiles()`) and any related assertions. Without this, the test suite will fail because `launcherTiles()` no longer exists.

    **Step 6 — HomeMenu.qml:** Remove the `LauncherDock { ... }` instantiation block (around line 561-570, z=10 overlay). The grid now uses the full vertical space with no dock overlay.

    **Step 7 — Documentation cleanup:**
    - `docs/config-schema.md`: Remove `launcher.tiles` configuration references (key description, example entries)
    - `docs/development.md`: Remove `LauncherMenu.qml` from the directory tree listing. Remove any mention of LauncherDock or LauncherModel in descriptions.
    - `docs/plugin-api.md`: Update line 121 — replace "Return to launcher (deactivate current plugin)" with "Return to home (deactivate current plugin)" or similar. The `app.home` IPC action still exists, it just no longer returns to a "launcher."
    - Search for any other stale references: `grep -rn "LauncherDock\|LauncherModel\|LauncherMenu\|launcher\.tiles\|launcher_tiles" src/ qml/ docs/ tests/ --include="*.cpp" --include="*.hpp" --include="*.qml" --include="*.md" --include="*.py"` and clean up any remaining hits (excluding git history and this plan).

    **Step 8 — Verify clean build:**
    ```bash
    cd build && cmake -DCMAKE_PREFIX_PATH=/opt/qt/6.8.2/gcc_64 .. && cmake --build . -j$(nproc)
    ```
    The build must succeed with no unresolved references to deleted files.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake -DCMAKE_PREFIX_PATH=/opt/qt/6.8.2/gcc_64 .. && cmake --build . -j$(nproc) && ctest --output-on-failure && ! grep -rn "LauncherDock\|LauncherModel\|LauncherMenu" src/ qml/ --include="*.cpp" --include="*.hpp" --include="*.qml" | grep -v "PLAN\|SUMMARY\|CONTEXT\|RESEARCH"</automated>
  </verify>
  <done>Four launcher files deleted. No references to LauncherDock, LauncherModel, or LauncherMenu remain in source code, QML, tests, or docs. YamlConfig launcher accessors AND default population removed. test_yaml_config launcher test removed. plugin-api.md updated. Documentation cleaned. Build succeeds. Full test suite passes (82+ tests).</done>
</task>

<task type="auto">
  <name>Task 2: Codex review gate</name>
  <files>None (review only)</files>
  <action>
    Run Codex review on the diff produced by Task 1. Generate the diff with:
    ```bash
    git diff HEAD~1..HEAD
    ```

    Submit to Codex for review. This is primarily a deletion plan, so Codex should verify:
    - No dangling references to deleted types/files
    - No orphaned QML imports or property references
    - YamlConfig removal is clean (no partial method bodies left)
    - HomeMenu.qml dock removal didn't break surrounding layout

    If Codex flags issues, fix them and re-run the test suite.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake --build . -j$(nproc) && ctest --output-on-failure</automated>
  </verify>
  <done>Codex review completed, all findings addressed (if any), clean build and full test suite passes.</done>
</task>

</tasks>

<verification>
1. `ls src/ui/LauncherModel.* 2>/dev/null` returns nothing (files deleted)
2. `ls qml/components/LauncherDock.qml 2>/dev/null` returns nothing
3. `ls qml/applications/launcher/LauncherMenu.qml 2>/dev/null` returns nothing
4. `grep -rn "LauncherModel\|LauncherDock\|LauncherMenu" src/ qml/ --include="*.cpp" --include="*.hpp" --include="*.qml"` returns nothing
5. Build succeeds: `cmake --build build -j$(nproc)`
6. Full test suite: `cd build && ctest --output-on-failure` -- all pass
7. `grep -n "launcher.tiles\|launcherTiles\|setLauncherTiles" src/core/YamlConfig.*` returns nothing
</verification>

<success_criteria>
- LauncherModel.hpp, LauncherModel.cpp, LauncherDock.qml, LauncherMenu.qml are deleted from the repo
- No source code or QML references to any deleted file remain
- YamlConfig no longer has launcherTiles/setLauncherTiles methods
- HomeMenu no longer instantiates LauncherDock (full vertical space for grid)
- main.cpp no longer constructs or registers LauncherModel
- CMakeLists.txt no longer references deleted files
- Documentation cleaned of stale launcher references
- Clean build, full test suite passing
</success_criteria>

<output>
After completion, create `.planning/phases/10-launcher-widget-dock-removal/10-02-SUMMARY.md`
</output>
