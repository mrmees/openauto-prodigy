---
phase: 03.2-companion-theme-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/services/ThemeService.hpp
  - src/core/services/ThemeService.cpp
  - config/themes/default/theme.yaml
  - config/themes/amoled/theme.yaml
  - config/themes/ocean/theme.yaml
  - config/themes/ember/theme.yaml
  - config/themes/connected-device/theme.yaml
  - tests/data/themes/default/theme.yaml
  - tests/test_theme_service.cpp
autonomous: true
requirements: [CTI-01, CTI-02, CTI-05]

must_haves:
  truths:
    - "ThemeService exposes all 34 M3 color roles as Q_PROPERTYs accessible from QML"
    - "All bundled themes specify all 34 M3 roles in both day and night maps"
    - "Existing QML property names (textPrimary, red, onRed, yellow, onYellow) still work unchanged"
    - "importCompanionTheme creates a named theme dir, saves YAML + wallpaper, auto-switches"
    - "deleteTheme removes user themes but refuses to delete bundled themes"
    - "Deleting the active theme falls back to default"
    - "scanThemeDirectories can be re-triggered internally after import/delete"
  artifacts:
    - path: "src/core/services/ThemeService.hpp"
      provides: "34 M3 Q_PROPERTYs + importCompanionTheme + deleteTheme + stored search paths"
      contains: "primaryContainer"
    - path: "src/core/services/ThemeService.cpp"
      provides: "importCompanionTheme implementation, deleteTheme, expanded knownM3Keys"
      contains: "importCompanionTheme"
    - path: "config/themes/default/theme.yaml"
      provides: "Full 34-role default theme"
      contains: "primary-container"
  key_links:
    - from: "src/core/services/ThemeService.hpp"
      to: "config/themes/*/theme.yaml"
      via: "Q_PROPERTY accessors calling activeColor with hyphenated keys matching YAML keys"
      pattern: "activeColor.*primary-container"
    - from: "src/core/services/ThemeService.cpp"
      to: "~/.openauto/themes/{slug}/"
      via: "importCompanionTheme writes theme.yaml + wallpaper.jpg to slug dir"
      pattern: "mkpath.*themes.*slug"
---

<objective>
Expand ThemeService from 16 AA wire tokens to the full 34 Material Design 3 color role system, update all bundled themes to specify all 34 roles, add importCompanionTheme() and deleteTheme() methods, and store search paths for internal rescan capability.

Purpose: The companion app sends a full 34-role M3 palette. ThemeService must be able to store and expose all 34 roles, and manage companion-imported themes as first-class named entries.

Output: ThemeService with 34 M3 Q_PROPERTYs, 5 bundled themes updated, companion theme import/delete methods, unit tests.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-companion-theme-import/03.2-CONTEXT.md
@.planning/phases/03.2-companion-theme-import/03.2-RESEARCH.md
@docs/plans/2026-03-09-companion-theme-import.md

<interfaces>
<!-- Current ThemeService interface that gets expanded -->

From src/core/services/ThemeService.hpp:
```cpp
// Current: 16 base + 5 derived Q_PROPERTYs
// Accessors follow pattern: QColor name() const { return activeColor("hyphenated-key"); }
// Colors stored in QMap<QString, QColor> dayColors_, nightColors_

void applyAATokens(const QMap<QString, uint32_t>& dayTokens, const QMap<QString, uint32_t>& nightTokens);
void scanThemeDirectories(const QStringList& searchPaths);
bool setTheme(const QString& themeId);
void setWallpaperOverride(const QString& override);
```

From src/core/services/ThemeService.cpp:
```cpp
// knownAATokenKeys (line 275): Set of 17 hyphenated keys used to filter applyAATokens
// persistConnectedDeviceTheme (line 362): YAML emitter pattern for writing theme files
// scanThemeDirectories (line 127): Takes QStringList, discovers theme.yaml in subdirs
// Search paths are NOT stored — constructed in main.cpp then passed once
```

Current bundled theme YAML format (config/themes/default/theme.yaml):
```yaml
id: default
name: Default Theme
version: 2.0.0
font_family: "Lato"
day:
  background: "#1a1a2e"
  primary: "#e94560"
  # ... 17 keys total
night:
  # ... same keys with dimmer values
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand ThemeService to 34 M3 roles + bundled theme updates</name>
  <files>
    src/core/services/ThemeService.hpp,
    src/core/services/ThemeService.cpp,
    config/themes/default/theme.yaml,
    config/themes/amoled/theme.yaml,
    config/themes/ocean/theme.yaml,
    config/themes/ember/theme.yaml,
    config/themes/connected-device/theme.yaml,
    tests/data/themes/default/theme.yaml,
    tests/test_theme_service.cpp
  </files>
  <behavior>
    - Test: new M3 properties (primaryContainer, secondary, tertiary, error, errorContainer, surfaceDim, surfaceBright, etc.) return valid colors from loaded theme
    - Test: old property names (textPrimary, red, onRed, yellow, onYellow) still return correct colors (backward compat)
    - Test: scrim is now stored in YAML (not hardcoded) — loading a theme with scrim key returns that value
    - Test: importCompanionTheme creates theme dir under ~/.openauto/themes/{slug}/, writes theme.yaml with all 34 roles + wallpaper.jpg, returns true
    - Test: importCompanionTheme with same name overwrites existing theme
    - Test: importCompanionTheme auto-switches to the new theme (currentThemeId() == slug)
    - Test: deleteTheme removes user theme directory, returns true
    - Test: deleteTheme on bundled theme (not under ~/.openauto/themes/) returns false
    - Test: deleteTheme on active theme switches to "default" first
    - Test: scanThemeDirectories picks up companion themes with source: companion and shows them in picker (not hidden like connected-device)
  </behavior>
  <action>
    **Part A — ThemeService.hpp expansion:**

    Add 18 new Q_PROPERTY declarations for the new M3 roles, grouped by M3 category:
    - Primary group: onPrimary, primaryContainer, onPrimaryContainer
    - Secondary group: secondary, onSecondary, secondaryContainer, onSecondaryContainer
    - Tertiary group: tertiary, onTertiary, tertiaryContainer, onTertiaryContainer
    - Error group: error, onError, errorContainer, onErrorContainer
    - Surface group: surfaceDim, surfaceBright, surfaceContainerLowest, surfaceContainerHigh, surfaceContainerHighest
    - Inverse group: inversePrimary
    - Neutral group: onBackground, onSurfaceVariant, shadow

    Add aliases: `error` reads from "error" key (NEW), `onError` reads from "on-error" (NEW). Keep `red`/`onRed` reading from "red"/"on-red" (existing). Both sets exist in YAML — companion themes populate M3 names, bundled themes populate both.

    Promote `scrim` from derived constant to stored value: change `scrim()` from returning hardcoded `QColor(0,0,0,180)` to `activeColor("scrim")` with a fallback to opaque black `#000000` if key missing. QML usage sites that need transparency should apply alpha themselves.

    Add new accessor methods following the pattern: `QColor name() const { return activeColor("hyphenated-key"); }`

    Add `importCompanionTheme(name, seed, dayColors, nightColors, wallpaperJpeg)` method declaration returning bool.

    Add `deleteTheme(themeId)` method declaration (Q_INVOKABLE) returning bool.

    Store search paths: add `QStringList searchPaths_` private member. Modify `scanThemeDirectories` to store paths to `searchPaths_` before scanning. Add private `rescanThemes()` that calls `scanThemeDirectories(searchPaths_)`.

    **Part B — ThemeService.cpp implementation:**

    Expand `knownAATokenKeys` to a new `knownM3Keys` set containing all 34 hyphenated keys: primary, on-primary, primary-container, on-primary-container, secondary, on-secondary, secondary-container, on-secondary-container, tertiary, on-tertiary, tertiary-container, on-tertiary-container, error, on-error, error-container, on-error-container, background, on-background, surface, on-surface, surface-variant, on-surface-variant, surface-dim, surface-bright, surface-container-lowest, surface-container-low, surface-container, surface-container-high, surface-container-highest, outline, outline-variant, inverse-surface, inverse-on-surface, inverse-primary, scrim, shadow. Keep `knownAATokenKeys` for the `applyAATokens` method (unchanged). Add the custom tokens (text-primary, text-secondary, red, on-red, yellow, on-yellow) to the accepted set for YAML loading (they're not M3 but we keep them).

    Implement `importCompanionTheme`:
    - Generate slug: `name.toLower().trimmed()`, replace `[^a-z0-9]+` with "-", trim leading/trailing hyphens, fallback to "companion-theme"
    - Create/overwrite `~/.openauto/themes/{slug}/`
    - Write wallpaper.jpg if wallpaperJpeg not empty
    - Write theme.yaml using yaml-cpp Emitter (pattern from persistConnectedDeviceTheme): id=slug, name=name, version="2.0.0", source="companion", seed=seed, font_family=current font, day={all colors}, night={all colors}
    - Call `rescanThemes()` to pick up the new theme
    - Call `setTheme(slug)` to auto-switch
    - Clear wallpaper override so theme's own wallpaper applies
    - Return true on success

    Implement `deleteTheme`:
    - Look up themeId in themeDirectories_
    - Verify it's under `~/.openauto/themes/` (reject bundled themes)
    - If active theme, call setTheme("default") first
    - QDir(themeDir).removeRecursively()
    - Call rescanThemes()
    - Return true

    Update `persistConnectedDeviceTheme` to use `knownM3Keys` union `knownAATokenKeys` for filtering (so it persists all 34 roles if present).

    Update `scanThemeDirectories`: store searchPaths to searchPaths_ at the start. Keep the `connected-device` hidden from picker. Companion themes (source: companion) ARE shown in picker.

    Change `scrim()` from hardcoded to `activeColor("scrim")` with fallback: if transparent, return `QColor(0, 0, 0, 255)` (opaque black per M3 spec).

    **Part C — Bundled theme YAML updates:**

    For each of the 5 bundled themes (default, amoled, ocean, ember, connected-device) and test theme, add all 34 M3 role keys to both day and night sections. Manually pick reasonable values:
    - `on-primary`: typically white (#ffffff) for dark themes, or white for saturated primaries
    - `primary-container`: lighter tint of primary (day) or darker tint (night)
    - `on-primary-container`: contrasting text on container
    - `secondary`: muted complement of primary
    - `on-secondary`: contrasting text
    - `secondary-container/on-secondary-container`: lighter/darker variants
    - `tertiary/on-tertiary/tertiary-container/on-tertiary-container`: third accent
    - `error` = copy existing "red" value; `on-error` = copy existing "on-red" value
    - `error-container/on-error-container`: lighter/darker variants of error
    - `on-background`: copy existing "text-primary" value
    - `on-surface-variant`: copy existing "text-secondary" value
    - `surface-dim`: darker than surface
    - `surface-bright`: lighter than surface
    - `surface-container-lowest/surface-container-high/surface-container-highest`: gradient between surface and surface-container-low
    - `inverse-primary`: complementary primary for inverse surfaces
    - `scrim`: "#000000" (opaque black — alpha applied at usage)
    - `shadow`: "#000000"

    For AMOLED theme: many surface roles are pure black (#000000).

    Keep existing keys (text-primary, text-secondary, red, on-red, yellow, on-yellow) alongside the M3 equivalents.

    Also update tests/data/themes/default/theme.yaml to match.

    **Part D — Tests:**

    In test_theme_service.cpp, add tests for:
    1. New M3 properties return valid colors from default theme
    2. Backward compat: textPrimary, red, onRed, yellow, onYellow still work
    3. scrim returns stored value (not hardcoded)
    4. importCompanionTheme creates dir, YAML, wallpaper, auto-switches
    5. importCompanionTheme overwrites on same name
    6. deleteTheme removes user theme, returns true
    7. deleteTheme rejects bundled theme
    8. deleteTheme on active theme falls back to default
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. -DBUILD_TESTING=ON && cmake --build . -j$(nproc) && ctest -R test_theme_service --output-on-failure</automated>
  </verify>
  <done>
    - ThemeService.hpp has 34 M3 Q_PROPERTYs (18 new + 16 existing base) + 5 derived + importCompanionTheme + deleteTheme
    - All 5 bundled themes have all 34 M3 roles in both day/night sections
    - scrim promoted from derived to stored
    - All existing + new tests pass
    - No QML breakage (old property names unchanged)
  </done>
</task>

</tasks>

<verification>
- `cd build && cmake --build . -j$(nproc)` compiles without errors
- `ctest -R test_theme_service --output-on-failure` — all tests pass
- Verify ThemeService.hpp has Q_PROPERTY declarations for all 34 M3 roles
- Verify each bundled theme YAML has all 34 keys in both day and night sections
</verification>

<success_criteria>
ThemeService exposes the full M3 34-role palette as Q_PROPERTYs, all bundled themes specify all 34 roles, importCompanionTheme and deleteTheme are implemented and tested, and all existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-companion-theme-import/03.2-01-SUMMARY.md`
</output>
