---
phase: 07-edit-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/WidgetGridModel.hpp
  - src/ui/WidgetGridModel.cpp
  - tests/test_widget_grid_model.cpp
  - qml/controls/Toast.qml
  - src/CMakeLists.txt
autonomous: true
requirements:
  - EDIT-05
  - EDIT-10

must_haves:
  truths:
    - "WidgetGridModel exposes min/max constraint roles so QML delegates can access widget size limits"
    - "findFirstAvailableCell returns the first open grid position that fits a given widget size"
    - "Toast component can display a transient message and auto-dismiss"
  artifacts:
    - path: "src/ui/WidgetGridModel.hpp"
      provides: "MinColsRole, MinRowsRole, MaxColsRole, MaxRowsRole enum values and findFirstAvailableCell Q_INVOKABLE"
      contains: "MinColsRole"
    - path: "src/ui/WidgetGridModel.cpp"
      provides: "Role data() cases and findFirstAvailableCell implementation"
      contains: "findFirstAvailableCell"
    - path: "tests/test_widget_grid_model.cpp"
      provides: "Tests for constraint roles and auto-place"
      contains: "testConstraintRoles"
    - path: "qml/controls/Toast.qml"
      provides: "Reusable toast notification component"
      contains: "function show"
  key_links:
    - from: "src/ui/WidgetGridModel.cpp"
      to: "src/core/widget/WidgetRegistry.hpp"
      via: "registry_->descriptor() for constraint lookup"
      pattern: "registry_->descriptor"
    - from: "qml/controls/Toast.qml"
      to: "qml/controls/NormalText.qml"
      via: "NormalText for toast message rendering"
      pattern: "NormalText"
---

<objective>
Extend WidgetGridModel with constraint roles and auto-place logic needed by edit mode QML, and create a reusable Toast component for feedback messages.

Purpose: Edit mode's resize handles need widget min/max span constraints per delegate (not available via current roles). The FAB add-widget flow needs auto-placement at the first available cell. The "no space" feedback needs a toast component.

Output: Extended WidgetGridModel with 4 new roles + findFirstAvailableCell(), Toast.qml control, unit tests for both.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-edit-mode/07-CONTEXT.md
@.planning/phases/07-edit-mode/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/ui/WidgetGridModel.hpp:
```cpp
enum Roles {
    WidgetIdRole = Qt::UserRole + 1,
    InstanceIdRole, ColumnRole, RowRole,
    ColSpanRole, RowSpanRole, OpacityRole,
    QmlComponentRole, VisibleRole
};

Q_INVOKABLE bool canPlace(int col, int row, int colSpan, int rowSpan,
                           const QString& excludeInstanceId = {}) const;
```

From src/core/widget/WidgetTypes.hpp:
```cpp
struct WidgetDescriptor {
    int minCols = 1; int minRows = 1;
    int maxCols = 6; int maxRows = 4;
    int defaultCols = 1; int defaultRows = 1;
};
```

From src/core/widget/WidgetRegistry.hpp:
```cpp
std::optional<WidgetDescriptor> descriptor(const QString& widgetId) const;
QList<WidgetDescriptor> widgetsFittingSpace(int availCols, int availRows) const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add constraint roles and findFirstAvailableCell to WidgetGridModel</name>
  <files>src/ui/WidgetGridModel.hpp, src/ui/WidgetGridModel.cpp, tests/test_widget_grid_model.cpp</files>
  <behavior>
    - Test: MinColsRole returns descriptor.minCols for a placed widget (default 1)
    - Test: MaxColsRole returns descriptor.maxCols for a placed widget
    - Test: MinRowsRole returns descriptor.minRows for a placed widget
    - Test: MaxRowsRole returns descriptor.maxRows for a placed widget
    - Test: Constraint roles return fallback values (1/6/1/4) when registry has no descriptor
    - Test: findFirstAvailableCell(2,2) returns {0,0} on empty 6x4 grid
    - Test: findFirstAvailableCell(2,2) skips occupied cells and returns first valid position
    - Test: findFirstAvailableCell(7,1) returns {-1,-1} when no space fits (widget wider than grid)
    - Test: findFirstAvailableCell(1,1) returns {-1,-1} when grid is completely full
  </behavior>
  <action>
1. In WidgetGridModel.hpp, add to Roles enum after VisibleRole: MinColsRole, MinRowsRole, MaxColsRole, MaxRowsRole, DefaultColsRole, DefaultRowsRole.

2. Add Q_INVOKABLE: `Q_INVOKABLE QVariantMap findFirstAvailableCell(int colSpan, int rowSpan) const;`
   Returns `{"col": N, "row": N}` on success or `{"col": -1, "row": -1}` when no space. QVariantMap works cleanly from QML.

3. In WidgetGridModel.cpp data(), add cases for the new roles. Each looks up `registry_->descriptor(p.widgetId)` and returns the appropriate field. Fallback: minCols=1, minRows=1, maxCols=6, maxRows=4, defaultCols=1, defaultRows=1.

4. In roleNames(), add: `{MinColsRole, "minCols"}, {MinRowsRole, "minRows"}, {MaxColsRole, "maxCols"}, {MaxRowsRole, "maxRows"}, {DefaultColsRole, "defaultCols"}, {DefaultRowsRole, "defaultRows"}`.

5. Implement findFirstAvailableCell: iterate row-major (r=0..rows-1, c=0..cols-1), call canPlace(c, r, colSpan, rowSpan) for each, return first success. Return {-1,-1} if none found.

6. Write tests in test_widget_grid_model.cpp exercising the behaviors listed above.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. -DBUILD_TESTING=ON && cmake --build . -j$(nproc) --target test_widget_grid_model && ctest -R widget_grid --output-on-failure</automated>
  </verify>
  <done>WidgetGridModel exposes 6 new data roles (minCols, minRows, maxCols, maxRows, defaultCols, defaultRows) and findFirstAvailableCell() Q_INVOKABLE. All new tests pass alongside existing 20 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable Toast.qml control</name>
  <files>qml/controls/Toast.qml, src/CMakeLists.txt</files>
  <action>
1. Create `qml/controls/Toast.qml` with this structure:
   - Rectangle anchored to bottom-center of parent with bottomMargin of `UiMetrics.gap * 2`
   - Background color: `ThemeService.inverseSurface`, text color: `ThemeService.inverseOnSurface`
   - Rounded corners: `UiMetrics.radius`
   - `property string message: ""`
   - `function show(msg, durationMs)` — sets message, animates opacity to 1.0, starts hide timer
   - Internal Timer that sets opacity to 0 on trigger (default 2500ms)
   - `Behavior on opacity { NumberAnimation { duration: 200 } }` for fade in/out
   - Width: `toastText.implicitWidth + UiMetrics.spacing * 2`
   - Height: `toastText.implicitHeight + UiMetrics.spacing`
   - NormalText centered inside with the message
   - Initial opacity: 0 (hidden by default)

2. Register Toast.qml in `src/CMakeLists.txt` in the QML_FILES list alongside other controls. Add set_source_files_properties with QT_QML_SOURCE_TYPENAME "Toast" and QT_RESOURCE_ALIAS "Toast.qml". No need for SKIP_CACHEGEN since Toast is loaded via the module system, not via Loader URL.

3. Verify build compiles with the new QML file.
  </action>
  <verify>
    <automated>cd /home/matt/claude/personal/openautopro/openauto-prodigy/build && cmake .. && cmake --build . -j$(nproc) 2>&1 | tail -5</automated>
  </verify>
  <done>Toast.qml exists in qml/controls/, registered in CMakeLists.txt, build succeeds. Component exposes `show(msg, durationMs)` function with auto-dismiss behavior.</done>
</task>

</tasks>

<verification>
- `cd build && ctest -R widget_grid -j$(nproc) --output-on-failure` — all widget grid tests pass (existing + new)
- `cd build && cmake --build . -j$(nproc)` — full build clean
- Toast.qml file exists at qml/controls/Toast.qml with show() function
</verification>

<success_criteria>
- WidgetGridModel.data() returns constraint values for MinColsRole through DefaultRowsRole
- findFirstAvailableCell(colSpan, rowSpan) returns first valid position or {-1,-1}
- Toast.qml is a self-contained component with show(msg, duration) and auto-hide
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-edit-mode/07-01-SUMMARY.md`
</output>
